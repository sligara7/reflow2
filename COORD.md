# COORD.md — who's working on what

The claim board for reflow2. Two people, two agents (Claude Code and grok build), one repo,
coordinating through git.

Its only job is **avoiding collisions**: knowing what someone else already has in hand, so two
people don't build the same thing or edit the same module at once. It is not a plan, a spec, or
a status report —

| Question | Where |
|---|---|
| Who has what right now | **this file** |
| What the work *is*, and why | [docs/backlog.md](docs/backlog.md) |
| Is the code meeting the docs | [docs/requirements-coverage.md](docs/requirements-coverage.md) |
| What changed and when | [CHANGELOG.md](CHANGELOG.md) |

## For agents reading this

**Before anything else: `git pull --rebase`.** A claim board you haven't pulled is a claim board
from the past — you'll claim something that's already taken and find out at merge time. Pull,
then read.

**Before starting work:** read the In-progress list. If someone holds the item, or holds
something that touches the same files, pick something else or say so — don't quietly work in
parallel.

**When you start:** add one line under **In progress** with the handle, the date, and roughly
what you're touching. Commit that line *before* the work, not after — a claim nobody can see is
not a claim.

**When you finish:** move the line to **Recently finished** with the commit, and push.

**Keep it one line per item.** Two agents editing this file will eventually collide in git; a
line-per-item makes the conflict trivial to resolve, a table or a paragraph does not.

**Don't restate the backlog here.** Reference the id (`BL-4`) and let
[docs/backlog.md](docs/backlog.md) carry the description, so there's one source of truth for what
the work actually is.

## Handles

| Handle | Who | Agent | Usually working on |
|---|---|---|---|
| `@ajs` | Anthony | Claude Code | core, schema, detectors |
| `@bro` | (brother) | grok build | using it for real; consumer kit feedback |

Add yourself if you're new here.

## In progress

*Format: `- BL-n or short title — @handle — since YYYY-MM-DD — files/areas touched`*









- Brownfield trial on ophyd-service — @ajs — since 2026-07-18 — docs/trials-private/2026-07-18-brownfield-ophyd-service.md (private) (findings log; no code yet)
- Greenfield trial on aidrone — @ajs — since 2026-07-18 — docs/trials-private/2026-07-18-greenfield-aidrone.md (private) (running findings log; design lives in ~/projects/aidrone)






## Blocked / waiting

- **~~Release follow-up (v0.5.0 re-dispatch)~~ SUPERSEDED 2026-07-21** — v0.5.0's release run
  (29785834848) sat stuck in GitHub's macos-x86_64 queue for 11h and never published any
  binaries, so v0.5.0 never actually reached a user. Rather than re-dispatch a stale, buggy
  build (main was 27 commits ahead with the deep-review criticals — HEAL node-deletion,
  installer clobber, install.sh silent death), **v0.6.0 was cut from current main** instead
  (user-approved). The stuck v0.5.0 run was cancelled. BL-15 verification rides on the v0.6.0
  release run under the new draft-then-publish release.yml.

- **Standalone-repo conversation** — was gated on the v0.5.0 Release node (done 2026-07-20);
  counterpart Decision with reopening conditions, CRUD skills, and search are all in place
  for it. Wants the user.

## Recently finished

Trimmed periodically; the durable history is [CHANGELOG.md](CHANGELOG.md) and `git log`.

- **The seam is computed now, not hand-wired — and the design has ZERO open gaps** — LANDED. `req:complementary-pairing` (accepted 2026-07-27, the last open gap) delivered by `cap:complementary-pairing`. **THE MISSING HALF WAS THE SUBSCRIBE SIDE**: `Interface.designation` could say `published`/`internal`, so a design could state what it OFFERED but not what it NEEDED in a form anything could match against. Now `published`/`required`/`both`/`internal`, role on the INTERFACE not the node (a component does both, so a per-node role collapses to `both` and pairs with everything — `dec:pairing-role-placement`). `pair_designs` matches COMPLEMENTS, never like-with-like. **THE KEY IS THREE AXES — medium + transport_security + auth — and the third one is the whole reason**: the dynograph-foundation trial refuted role-plus-medium from the provider's side, because their design has three `medium: REST` boundaries and one is public and unauthenticated BY DESIGN (an orchestrator's liveness probe cannot hold a credential), so "I require REST" paired against it. Test pins that exact case; mutation confirms narrowing the key back fails it. Five outcomes: paired / conflicts / unmet needs / dead surface / duplicate providers. **Conflicts report EVERY refusing axis**, not the first — otherwise you fix `transport_security`, redeploy, and rediscover `auth`. Uncertain names are CANDIDATES to ask about, never actions. Unclassified boundaries are counted and NAMED because `internal` is the default and cannot tell deliberate from unlabelled. `seam_report` unchanged and complementary — pairing says WHICH correspond, seam_report says whether the contracts agree; its doc comment predicted this since July. 19 seam tests green, two mutations caught by the right tests, toolsnap 121. — @ajs — 2026-07-30 — schema/structure.yaml, crates/reflow2-core/src/{graph,seam}.rs, crates/reflow2-core/tests/seam.rs, crates/reflow2-mcp/src/service.rs, docs/design/reflow2.json

- **The open-requirement list was scrutinised and nine were retired** — LANDED. `dec:proposed-requirements-pruned`, on Anthony's word ("if some of them are not value added and just cause confusion... we can retire them as considered, but not accepted"). 26 proposed → 15. Seven DROPPED as not value-adding (two record a doubt rather than a need and say so in their own names; two are backlog hygiene dressed as product requirements; one is answered by `cap:bounded-reads`; one purely speculative; one redundant with the delivered `req:coverage-visible`), two FOLDED into `req:defaults-do-not-assert`. Dropped, never deleted — full statements and final-state snapshots kept, because several were found by measurement and deleting them would let the same captures be re-made next quarter by someone who could not see they had been weighed. **THE CORRECTION WORTH KEEPING**: `req:supporting-is-not-conflict` was proposed as a third fold because it had ONE edge and looked like an orphan — that edge was an incoming SATISFIES from a REALIZED capability, i.e. it was DELIVERED and its status had never moved off proposed. Now `accepted`. **Low edge count is not low value, and for delivered work it is exactly backwards.** Structural isolation is evidence about WIRING, not worth, in both directions: it over-accused four of Anthony's own parked planning ideas and under-accused one already shipped. Islands fell 6 → 3; the remaining three are the planning cluster (`epochs-can-be-planned`, `plans-move-honestly`, `graph-indexes-snapshots`, `version-combination-is-an-object`) which want WIRING, not retiring. — @ajs — 2026-07-30 — docs/design/reflow2.json, CHANGELOG.md

- **The governance mode became a choice, and reflow2 made one** — LANDED. `req:mode-is-chosen-and-changeable` (accepted), `cap:governance-mode`, on Anthony's word. `Project.mode` gates ONE thing — `apply_heal` applies structural repairs in `flexible`, proposes and stops in `rigid` — and until now was settable only at genesis, so **every design ever made carried the `flexible` default and could never move off it**. `set_project_mode` fixes that (unknown mode refused by schema validation, previous choice left intact, other props preserved — mutation-checked, since `create_node` REPLACES props at the core layer while the MCP tool MERGES). **reflow2's own project is now `rigid`**: `apply_heal` merges and deletes nodes and this repo was already bitten once by an auto-apply corrupting a graph (the chained-duplicate guard), so structural edits to the design brain now want a human. Cost: HEAL repairs here are two steps. Also narrowed the schema description, which claimed "design is the source of truth" while the code gates one function — an instance of `req:schema-prose-is-checked` fixed at the source. `set_project_mode` is toolsnap 120. — @ajs — 2026-07-30 — crates/reflow2-core/src/graph.rs, crates/reflow2-mcp/src/service.rs, crates/reflow2-mcp/tests/tools.rs, schema/core.yaml, docs/design/reflow2.json

- **A standing judgement stopped expiring every time the design grew** — LANDED. `req:set-scoped-acknowledgement-keys-on-its-rule` + `dec:aggregate-gap-keyed-on-rule`, on Anthony's word. `gap_id` hashes the affected set, which is right per-node and wrong for an AGGREGATE gap whose affected set IS the population the rule ranges over: `unvalidated_capability` had been re-acknowledged **~20 times** (33, 34, 35 … 65, 67, 68 capabilities) with the same disposition, and ~20 of those reasons called the churn a finding without it ever becoming intent. Now keyed on the rule via an explicit exhaustive `GapSource::is_aggregate()`. **THE TRAP, worth knowing: it is NOT `GapScope::Project`** — `unsatisfied_requirement` and `status_contradiction` are project-scoped but per-requirement, so keying on scope would collapse every unsatisfied requirement into one gap sharing one judgement; a test pins exactly that and a mutation proves it fails when taken. **A CORRECTION worth carrying**: the "close sibling" framing that prompted this was half wrong — `heal.rs`'s `issue_id` hashes its affected set exactly as `gap_id` did, so the defect side has no cleverer keying; what it has is the `retired` REPORTING that `reviewed_gaps` already had too. Mirroring the sibling alone would have fixed nothing. One-time migration: the rollup id moved `80f8bc457bfe9e16` → `0a77650b58242054` and was acknowledged once more, the last it should need; the ~20 historical acks are LEFT ALONE, not withdrawn, because withdrawal would claim the judgement was revoked — `reviewed_gaps` reports them `retired`, which is what happened. — @ajs — 2026-07-30 — crates/reflow2-core/src/detect.rs, crates/reflow2-core/tests/gap_review.rs, docs/design/reflow2.json

- **rmcp 2 → 3.0.1, and seat identity made to survive a transport with no sessions** — LANDED. Two changes, one clock: `req:seat-identity-survives-stateless-mcp` had an external deadline and rmcp v3 is the release that deletes protocol sessions. **The upgrade was one generic bound** (`StreamableHttpService::new` narrowed from `S: Service<RoleServer>` to `S: ServerHandler`); of the eight breaking-change areas in the v3.0.0 notes exactly one reached this code, because `#[tool_handler]` absorbed the MRTR response-enum change and MSRV 1.88 is under the workspace's 1.94. **THE FINDING WAS THAT EVERY GATE STAYED GREEN**: core, workspace, smoke_mcp, shared-session, remote-session, stale-seat and degraded-server suites all passed while seat identity was already broken, because every test client in this repo negotiates `2025-06-18`, `2025-11-25` or `2024-11-05` and rmcp's own `ProtocolVersion::LATEST` is still `V_2025_11_25` — **nothing here speaks `2026-07-28`**, so nothing touched the sessionless path. Same shape as coverage row 3AX-3 the day before: a tick only as wide as the case its evidence exercises, twice in 48 hours. Measured instead with `tools/stateless_seat_probe.py` (one client, two claims, count distinct seats): stdio 1, legacy HTTP 1, **2026-07-28 HTTP 2 — one per request**. Then FIXED on Anthony's word, option (a) of `dec:stateless-seat-handle`: `mint_seat` returns a durable handle, `claim_region` takes it as `seat`, and on a sessionless transport omitting it is **REFUSED by name** rather than answered with a seat that would change on the next call — the backstop is load-bearing, because minting silently *succeeds* while the owner drifts (`req:no-silent-fallback` applied to identity). Options (b) derive-from-headers, (c) one-seat-per-process and (d) refuse-the-whole-claim-family rejected on the record. **Existing callers are untouched**: `seat` stays optional and `claim_region`'s schema shape did not move; only 2026-07-28 clients must carry one. Probe now exits ZERO and checks both halves, `ver:stateless-seat` is `passing` on its own evidence, and `decision:ack:350d55cf9c8c3247` was WITHDRAWN rather than left standing — its own expiry condition. Mutation-checked: disable the guard and `a_claim_with_no_seat_is_refused_when_identity_is_per_request` fails. Also captured, NOT built: `req:set-scoped-acknowledgement-keys-on-its-rule` — the `unvalidated_capability` rollup has been re-acknowledged ~20 times because its acknowledgement expires whenever the capability set grows, and every one of those notes called the churn a finding without anyone recording it as intent. Four shapes named; the closest sibling is defect-side expiry-by-shape, which already works. `mint_seat` is toolsnap 119. — @ajs — 2026-07-30 — crates/reflow2-mcp/{Cargo.toml,src/main.rs,src/service.rs,tests/tools.rs}, tools/{stateless_seat_probe.py,skill_lint.py,toolsnaps/}, getting-started/skills/parallel-work, AGENTS.md, docs/design/reflow2.json

- **req:snapshot-per-revision-not-per-epoch BUILT — the design stops losing its own history** (`dec:snapshot-id-per-revision`, `ver:snapshot-per-revision`, `epoch:snapshot-per-revision` at sequence 230). The finding from 52658dd, unparked on Anthony's word ("finish what's in flight"). **Option (a) taken, in the form that keeps existing ids stable**: the FIRST capture in an epoch keeps `snap:{epoch}:{node}` — existing graphs and committed exports carry those ids and a test pins one, and suffixing everything would have rewritten history to fix a bug about not rewriting history — while a genuine second revision appends `:r2`, `:r3`. **An IDENTICAL re-capture returns the existing snapshot**, which is the mirror of the bug being fixed: snapshotting a node that has not moved is a no-op, and minting one would make the history claim edits that never happened (it also keeps `record_change` safe to retry). **THE FIRST DRAFT OF THAT IDEMPOTENCE WAS A HALF-FIX, caught by reading it back before committing**: it compared against ANY existing snapshot in the chain, so a node edited A → B → A inside one epoch matched the first A-capture on its third revision and recorded two — hiding an edit that DID happen, the same class of loss as the overwrite, just quieter. Now compared against the TAIL only, which also makes `:rN` order readable as the order the revisions occurred in — the only ordering a reader has, since every snapshot in an epoch is pinned to that one epoch and carries no sequence of its own. Mutation-checked: reverted to first-match, `returning_to_an_earlier_state_still_mints_a_revision` fails. Ceiling of 64 per (epoch, node), an error not a storage limit, and checked only when a capture is actually needed so an idempotent retry against a full chain still succeeds — an epoch bounds a round of WORK, and a node revised that often means the epoch stopped meaning anything. Options (b) refuse-the-second-call and (c) one-snapshot-per-epoch-as-a-no-op both rejected on the record: (c) preserves the original but still discards the intermediate, which is losing history on purpose rather than by accident. **THE DECISION IS `proposed`, NOT `accepted`** — the requirement itself said the fix shape was a choice and none of the options was obviously right; Anthony directed the work be finished, not this particular shape, so the shape awaits his word and reopening supersedes rather than un-accepts. 5 new cases (13 in the suite), core suite green, `cargo test --workspace` green, clippy 0, fmt. `snapshot_id`'s own doc comment was corrected in the same pass — it advertised *"idempotent (create-or-replace)"* as a virtue, and replace is precisely what destroyed the earlier revision; `docs/requirements-coverage.md` row **3AX-3** ("never overwrite the past") read ✅ throughout, because the test it cited only ever revised a node once per epoch — a ✅ is only as wide as the case its evidence exercises. **ALSO FOUND, and it is why the gate was red**: `art:graph` was never reconciled after `da50ae8`, so **main's CI had failed for three commits** on that one finding (runs 03:33, 03:47, 03:54 on 2026-07-29) — accepted `design_holds`, since `req:no-silent-fallback` already said it and the code was brought to meet it. Its registered checksum was also the full 64-char digest where every sibling registers 16; harmless (reconcile truncates to the registered dialect) but it is why the mismatch read oddly. **Two process gotchas worth keeping**: `ChangeEvent.change_type` still has no value meaning "a defect fix accepted and delivered" — `test_failure_fix` used as least-wrong and said so, the same gap recorded at `chg:anchor-decision-second-hint`; and **exporting TWICE before committing severs the lineage chain** — the second export links to the first, which will never be committed, and the gate catches it. The remedy in its own message works: `git checkout docs/design/reflow2.json`, then export straight onto it, once. — @ajs — 2026-07-29 — crates/reflow2-core/src/temporal.rs, crates/reflow2-core/tests/temporal.rs, CHANGELOG.md, docs/design/reflow2.json

- **A dangling edge was accepted, stored, and reported by nothing** — LANDED in da50ae8. A `DEPENDS_ON` naming a capability nobody created was persisted and found by NOTHING: `detect_defects` zero, `detect_gaps` only unrelated phase gaps, target resolving to `None`. Refused at write now, matching `snapshot_node`/`add_change_event`. Found by comparing reflow2 against the original reflow's `system_of_systems_graph_v2.py`, whose functional-mode detector has an `unmet_dependencies` bucket for exactly this — a 2,146-line NetworkX engine [docs/reflow-audit.md](docs/reflow-audit.md) never assessed. **The first draft red-lighted the workspace gate** and the reason is worth keeping: the endpoint check ran before the edge TYPE was evaluated, so guessing `PACKAGES` between two absent ids reported only the missing id and never that `PACKAGES` is not an edge type — burying the vocabulary error under the instance one and silently breaking `edge_error`, whose whole purpose is the blind trial's complaint ("tells me I'm wrong without telling me what's right"). **Vocabulary errors before instance errors**, with a regression test pinning the order. Sibling commit 06f8a39 is unrelated build config: `systemd-oomd` was killing the terminal (VTE scope = shell + agent + MCP server together, invisible to `dmesg`), fixed with `jobs = 2` + `debug = "line-tables-only"`; `target/` had reached **98 GB** and `cargo clean` reclaimed 91 GB. Expect the `target/` growth to recur — it is the third time this repo has hit it. — @ajs — 2026-07-28 — crates/reflow2-core/src/graph.rs, crates/reflow2-core/tests/write_side.rs, Cargo.toml, .cargo/config.toml

- **MCP 2026-07-28 spec removes protocol sessions — and reflow2's seat-per-client rests on them. Plus a founding-requirement violation found by doing the work** — `req:seat-identity-survives-stateless-mcp` and `req:snapshot-per-revision-not-per-epoch` (both `proposed`, parked). **THE EXTERNAL CLOCK**: the [2026-07-28 MCP spec](https://modelcontextprotocol.io/specification/2026-07-28/changelog) removes protocol-level sessions and `Mcp-Session-Id` (SEP-2567) and the `initialize` handshake (SEP-2575); the prescribed replacement is *"server-minted handles passed as ordinary tool arguments"*. `service.rs:86` states the exposure in its own words — *"rmcp BUILDS A SERVICE PER SESSION"* — so `req:seat-per-client`, and with it `cap:shared-sessions`, `cap:remote-sessions`, `cap:claim-liveness`, `cap:stale-seat-refusal` and `claim_report`, rest on a thing being deleted. 12-month lifecycle window; pinned at `rmcp = "2"` and migration lands with v3, still beta. **Already aligned**: the launch wrapper logs to stderr (exactly the Logging-deprecation migration), toolsnaps pin deterministic `tools/list` order, and — the interesting one — **the new MRTR pattern (SEP-2322) is the shape `gap_to_prompt` and `ingest_step` already implement by hand**, with no server-side session state. reflow2 solved stateless elicitation before the spec standardised it. **THE OTHER FINDING, and it deserves unparking first**: two `record_change` calls against the same node in one epoch **silently overwrite the first snapshot** — the id is deterministic per (epoch, node). Observed by amending `req:build-loop-is-bounded` twice today; `compare_designs` showed the snapshot's `base` as the original and `other` as the intermediate, so the pre-amendment text survives only in git. That contradicts `req:intent-preserved` ("the past is never overwritten") **while reporting success**, and breaks revise-design's own closing promise — *"without git archaeology"*. Three candidate fixes recorded, none obviously right; the SILENCE is not open. — @ajs — 2026-07-28 — docs/design/reflow2.json

- **Anthony: "reflow was my own experience and training — don't take it as canon." Four captures REASSESSED, two new findings that come from neither system** — `req:correspondence-rules-are-stateable` and `req:assumptions-are-first-class` (new, `proposed`, parked), plus four amendments on the record. **The self-criticism that prompted it**: every requirement written during the review carried a section header reading "THE PRIOR ART", and that framing quietly made *"reflow did X"* the argument for X. **Reassessed as too credulous**: `req:drift-rolls-up-to-a-score` → renamed *"…but probably NOT from a single scalar"* (collapsing heterogeneous drift into one number destroys what decides the response; reflow's 0.3/0.5 thresholds are invented; **reflow2's per-event `design_holds`/`design_updated` plus `confirm.rs`'s honesty audit is already better and must not regress**); `req:framework-is-chosen-not-defaulted` → renamed *"…a framework registry is probably the wrong fix"* (terminology mapping is cosmetic; *"framework choice determines which analyses are available"* is a **smell** — analyses should key off what the graph CONTAINS, not a self-declared label); `req:context-is-a-modelled-quantity` → renamed *"Context cost is MEASURED and budgeted, not estimated"* (static token estimates go stale, are model-specific, and miss what dominates real consumption); `req:build-loop-is-bounded` → renamed *"…a HARD STOP is one option, not the requirement"* (phase-shaped, and halting is exactly the move that gets a discipline abandoned in continual-evolution work). In each case **the defect stayed and the borrowed mechanism was demoted**. **THE TWO NEW ONES come from standards and practice, not from reflow, which had neither**: 42010's **correspondence rules** — reflow2 has `dec:views-are-projections` (the projection half, stronger than anything in reflow) but every consistency rule it enforces is hardcoded in Rust and unextendable, and the missing stakeholder→concern→viewpoint chain is *why* `Requirement.concern` is dead; and **assumptions** — `Constraint` binds, `Decision` chooses, `RISKS` warns, and nothing says *"this holds only while X remains true, and X is not ours to decide"*, which in a multi-organisation ecosystem is what actually breaks. — @ajs — 2026-07-28 — docs/design/reflow2.json

- **phases/ reviewed, closing the workflow review — AND CORRECTING bad32c7** — two amendments on the record (`chg:mode-prior-art-corrected`, `chg:iteration-budget-was-elicited`, both at `epoch:reflow-workflow-review`, prior states snapshotted). **THE CORRECTION**: `bad32c7` claimed reflow's `development_mode` was "set by no workflow step anywhere" — a dangling prerequisite. **That was wrong**, and the error was method: the search covered `workflows/*.json` and did not recurse into `workflows/phases/`. `phases/P0_setup/phase_definition.json` carries a `development_mode_configuration` block described as *"This is where the critical development mode choice is made"*, putting a labelled question to the user — *"What type of project is this?"* — with flexible (*"Personal / Exploratory / Agile"*) and rigid (*"Enterprise / Approved / Regulated — architecture is locked after approval"*), then TWO follow-ups: acceptable drift level if rigid (0% / 10% / threshold-only) and *"How many development/test iterations before forced architecture review?"* (default 3, **range 1–10**). So all three governing values — mode, drift tolerance, iteration budget — were ELICITED per project. The `P3_P4_loops = 3` cited in `req:build-loop-is-bounded` is a DEFAULT of a question actually asked, not a constant, which is why that node was amended too. **The correction strengthens the reflow2 findings rather than weakening them**: the comparison is no longer "both systems have the same hole" but "reflow asked three questions before letting a project proceed; reflow2 asks none and cannot be told the answers afterwards." ALSO FOUND, bearing on the phase/QualityGate concern: reflow's own `P5_operations` exit criteria state *"P5 is typically ongoing"* and route production issues `back_to_P3_P4` as *"a new development cycle"* — so even the phase model reflow2 inherited did NOT assume termination, which is recorded in `req:build-loop-is-bounded` as supporting evidence. Exit criteria across all seven phases are machine-checkable conditions (`"All functions allocated to services"`, `"Dependencies form a valid DAG (no cycles)"`) that reflow2 already COMPUTES as detectors (`unallocated_capability`, `circular_dependency`) but does not bind to a gate. — @ajs — 2026-07-28 — docs/design/reflow2.json

- **00-setup + 00a-basic_setup reviewed: the governance mode is a default nobody chose, and it is READ** — `req:mode-is-chosen-and-changeable`, `req:scenarios-are-modelled`, `req:one-design-or-several-is-asked` (all `proposed`, parked). **THE SHARP ONE**: `Project.mode` decides whether design or implementation is the source of truth. `genesis.rs` takes it OPTIONALLY (its own comment: *"schema applies mode=flexible… when unset"*), `"mode"` appears NOWHERE in `service.rs` so there is no setter at all, and **reflow2's own Project carries `flexible`** — the mode meaning *"the implementation drives the architecture"*, in a project whose thesis is that the design is the memory. Unlike the ten properties in the original sweep, which are asserted and UNREAD, this one is asserted **and acted upon** (`heal.rs:431`). reflow arrived at the same place by the opposite route: `development_mode` is defined in `workflow_master`, REQUIRED as the P0→P1 prerequisite, READ by `resync_protocol`, TESTED by `97` — and **set by no workflow step anywhere**; its own `META-04H` check HA-005 ("Transition Prerequisites Satisfiability") exists to catch that and didn't. Also captured: `req:scenarios-are-modelled` (no `scenario`/`use_case`/`persona`/`journey` vocabulary exists — which is WHY the 67 capabilities were acknowledged as validation-tracked-in-prose the same day: validation has no node to point at; `Flow` is fully live in `flow.rs` but `Actor` appears ONCE in the whole core, at `report.rs:35`, counted and nothing more, with `INTERACTS_WITH` unread) and `req:one-design-or-several-is-asked` (reflow asked "is this a system-of-systems?" once at setup; reflow2 settles the boundary by wherever init was run — must become a detector, not a genesis prompt, or it undoes `req:no-setup-per-project`). **THREE MORE DANGLING REFERENCES found in reflow's own orchestrator**, same class as `req:schema-prose-is-checked`: `phase_summary` names `02c-bayesian_optimization.json` which **does not exist**; `01e` and `01f` are live substantial workflows assigned to **no phase**; and the `development_mode` prerequisite above. Its own HA-004/HA-005 cover exactly these. — @ajs — 2026-07-28 — docs/design/reflow2.json

- **01-systems_engineering reviewed: superseded, and the split LOST things — which is itself the finding** — `req:decomposition-covers-its-parent` (new, `proposed`, parked) + `req:intent-is-reinjected` AMENDED on the record. The monolith is not referenced by `workflow_master`'s `phase_summary` and its 17 steps are the union of `01a`–`01d`, so it is dead — but it carries two top-level blocks, `context_management` and `self_improvement`, present in **all six monolithic workflows** (`00-setup`, `01`, `02`, `03`, `04`, `feature_update`) and absent from **every one** of `00a`, `01a`, `01b`, `01c`, `01d`, `01e`, `01f`. Against the live phase assignments, `context_management` survives in ~2 of ~16 live workflows. **THE GENERAL CASE**: a decomposition by SUBJECT drops what belongs to no single subject — cross-cutting content has no natural child to land in, so it lands in none, and nothing noticed for months. That turns a risk identified analytically this morning into a field observation: `report.rs:566` rolls delivery up a decomposition (parent delivered iff EVERY child is) and **never asks whether the children cover the parent**, which is why `DECOMPOSES` was rejected as the edge type for two captures earlier today. Parked because the honest form is a QUESTION at decomposition time with the answer recorded, not a computed refusal — nothing can mechanically decide whether two children amount to a parent, and must apply to `decomposed` lineage only, never `derived`. **AMENDMENT** (`chg:reinjection-trigger-answered`, prior state snapshotted): `context_management` answers the open trigger question inside `req:intent-is-reinjected` — an operations counter (threshold 5, matching `workflow_master`'s `every_N_iterations`, so the cadence was chosen once not twice) AND a named list of observable degradation signals, the part with no reflow2 counterpart: *"Asking about system name"*, *"Forgetting current step"*, *"Implementing features not in service_architecture.json"* (drift caught behaviourally, from what the agent is ABOUT to do). Plus a pre-operation checklist line worth keeping verbatim: *"Verify not about to generate report/summary (do real work)."* Counter and signals are complementary — a counter alone fires on a healthy session and misses a fast collapse; signals alone never catch slow drift that has no symptom yet. — @ajs — 2026-07-28 — docs/design/reflow2.json

- **99-meta_analysis reviewed, closing the PM trio: context was a MODELLED QUANTITY and reflow2's slot for it is empty** — `req:context-is-a-modelled-quantity` (`proposed`, parked). Most of `99` is already covered: `META-02/03/04` is what `tools/build_design_graph.py` does (its docstring: *"dogfooding in the strong sense"*), `META-04H`'s phase-DAG checks map onto HEAL, `META-08`'s functional-coverage target onto `coverage_report`. **THE FINDING**: reflow made agent context a first-class value — a function attribute marked *"CRITICAL for AI agent analysis"*, **the primary edge weight** (*"Context consumption as edge weight enables bottleneck analysis"*), with estimation guidelines so numbers were comparable — then computed **cumulative context along all paths** against hard thresholds, CRITICAL >160k tokens / WARNING >140k. So *"an agent cannot get through this path"* was answerable AT DESIGN TIME. reflow2 handles context only at runtime and reactively (`cap:bounded-reads`, `cap:tool-search`) — good mitigations, applied per call, after the cost is incurred; nothing predicts. **AND THE SLOT EXISTS, EMPTY**: `DEPENDS_ON` declares the coupling-weight facet with the schema comment *"weight is a STRENGTH… invert to a cost for shortest-path/closeness"*, and measured over reflow2's own 3,250 edges — **232 carry `weight_basis`, ZERO carry `weight`, `frequency` or `data_volume`**. That EXPLAINS `req:edge-defaults-do-not-assert` rather than repeating it: `weight_basis` asserts "estimated" over an absent weight because the weights were MEANT to be there, and reflow shows what for. The two must be decided together — fixing the default in isolation could delete the slot this needs. Open and non-mechanical: granularity (per capability / tool call / skill), whether estimates are authored or measured (`weight_basis` already distinguishes estimated/evidence/measured — more evidence the facet was designed for this), and what a threshold means with no fixed window, since reflow's 160k was its own environment's number and recording it as universal would be the asserting default this family exists to prevent. — @ajs — 2026-07-28 — docs/design/reflow2.json

- **98-reflow_feature_update reviewed: mostly ground reflow2 already covers BETTER, plus one real finding** — `req:schema-prose-is-checked` (`proposed`, parked). reflow2 wins twice here and it is worth recording: RFU-01A determined a feature's impact from a **hardcoded lookup table** (`P2_architecture_allocation: "Changes to architecture workflows (01a, 01b, 01c, 02, 02b)…"`) where `propagate_change` computes the blast radius from actual edges — a static table goes stale the moment a file is added; and RFU-05A's hierarchical-integrity checks only fire when workflow 98 is invoked, where reflow2's equivalents (57 suites, 112 toolsnaps, skill lint, nudge-path, stale-seat, degraded, merge-driver, loop-nudge, render-views, doc versions) run in CI on **every commit**. RFU-05's context-bottleneck sharpening is covered by `cap:bounded-reads` + `cap:tool-search` + toolsnaps. **THE FINDING**: `tools/validate_schema.py` enforces exactly two rules (edge endpoints are declared types; `fulltext` only on strings) and says so itself — *"Everything else (extra keys like `description`) is accepted, matching serde's non-strict parse."* So `extraction_hint` is free text no check reads — **and `describe_schema` serves it VERBATIM as the discovery surface**. Unvalidated prose is not a comment; it is an instruction delivered to every agent meeting the schema. The Anchor case proves it: `structure.yaml:108` and `verify.yaml:63` jointly assert an `Anchor`↔`DriftEvent` `DEPENDS_ON` with zero instances and no code path, and **both pass validate_schema cleanly**. reflow had this check one level over — `validate_workflow_files.py` verified *"tool references point to existing tools"* and *"template references point to existing templates"*. A mechanical check is available and specified in the requirement (do the types named in a hint exist; does an asserted relationship have a declared edge type), needs no LLM, and would have caught Anchor. Parked because it is coupled to `dec:anchor-retire-or-implement` — writing the linter before deciding Anchor's fate either red-lights a type about to be removed or blesses prose about to be rewritten. Same family as the three property findings, one layer up: those are DATA the schema asserts without warrant; this is PROSE, and prose is the layer agents actually read. — @ajs — 2026-07-28 — docs/design/reflow2.json

- **97-GAN-inspired-test reviewed: reflow tested ITSELF on a cadence, blind, against a baseline** — `req:friction-has-a-severity-bar` + `req:friction-has-a-baseline` (both `proposed`, parked; the second DEPENDS_ON the first). reflow ran a generator/discriminator loop: one agent drove the workflows **blind** (*"Agent B should NOT see expected outputs"*), a second read the transcript, classified findings into 12 categories, prioritised them against stated criteria (P0 "cannot proceed without manual intervention"; P1 "5+ minutes wasted or happens frequently"; P2 polish), aggregated to metrics, compared to a stored baseline — *"if new version performs worse than baseline, that's a P0 issue"* — and converted the pile to work at a stated bar: `p0 > 0 OR (p1 > 3 AND friction_overhead > 15%)`. **reflow2's own skill file concedes the gap**: report-friction says *"ordinary use produces better evidence and CURRENTLY LOSES ALL OF IT."* `phase_trial.py` (13/13) and the erosion trial regression-gate the DETECTORS; nothing measures whether a person can get THROUGH the process, so reflow2 could get materially harder to use with every gate green. **Two corroborations found in this repo, not in the workflow**: the eight brainmaker friction items in `feedback.md` are still not in the backlog; and friction with nowhere to live migrates into node NAMES — `ver:kit-manifest-agrees` is named *"… (friction 2026-07-25: reflow2's own manifest is four releases stale and nothing noticed; unwritten)"*. "unwritten" inside a Verification name is a backlog entry in hiding. WHERE reflow2 IS AHEAD and must not lose it: report-friction's redaction discipline (tool names and argument SHAPES, node TYPES not ids, counts not contents) has no counterpart in reflow's loop — reflow's ran on its own repo, reflow2 installs into *other people's* projects. HELD for Anthony: whether reflow2 should AUTO-TRIGGER its own fixes (`GAN-06` had the loop editing itself when the gate fired) — an autonomy question sitting against `dec:report-dont-judge`. Also noticed, unverified: no friction Capability turned up in the design at all — `cmp:skills` carries brainstorm/change-an-existing-feature/delta-reconcile/onboarding/parallel-work and not report-friction; may be a search miss, worth a direct check. — @ajs — 2026-07-28 — docs/design/reflow2.json

- **resync_protocol + phase_transitions reviewed; reflow2 turns out to be AHEAD on the main mechanism** — worth recording because a review that only finds gaps is not a review. reflow resolved drift by GLOBAL project mode (flexible → update the architecture; rigid → realign the implementation), so one setting decided every divergence. reflow2 does it PER DRIFT EVENT (`design_holds` / `design_updated`), so you can hold on one and update on another in the same session — reflow structurally could not. And `confirm.rs` audits whether the dispositions are honest: *"five `design_holds` claims with zero design edits is a very different confirmation history from one `design_updated`"*. reflow had no equivalent; a rigid-mode user could claim realignment forever unchecked. TWO GAPS CAPTURED, both `proposed` and parked: `req:drift-rolls-up-to-a-score` (reflow emitted a scalar 0.0–1.0 that decided how far the work retreated — `<0.3`→P3, `0.3–0.5`→P2, `≥0.5`→P1 — and also gated P4→P5; reflow2 has per-event `DriftEvent.severity` and no rollup. **Deferred for a hard reason**: that severity is one of the WRITTEN-never-consulted properties the sweep found, so a rollup would compute a confident number from a base the code already disagrees with — fix the single-source question first); and `req:blocking-is-partial` (reflow's rigid Option B *"BLOCK implementation of drifted features, continue with non-drifted work"* — `reflow2_check.py` thresholds WHICH findings count but answers with one `sys.exit(1)` and a wholly red build; inputs already exist via `propagate_change` / `claim_region` / `status: proposed`, so what is missing is a reader). NOT taken: reflow's `change_requests.json` side-file, already solved better by `status: proposed` living in the graph. `dec:anchor-retire-or-implement` AMENDED on the record (epoch:reflow-workflow-review seq 220, `chg:anchor-decision-second-hint`, prior state snapshotted first): `DriftEvent`'s own hint at `schema/verify.yaml:63` claims *"References the Anchor it diverged from via DEPENDS_ON"*, so the dead type is advertised from BOTH ends of a relationship with zero instances — whichever option is chosen, two hints move, not one. **Vocabulary gap noticed while doing it**: `ChangeEvent.change_type`'s ten values contain nothing meaning "evidence added, conclusion unchanged"; `refactor` used as least-wrong and said so in the record. — @ajs — 2026-07-28 — docs/design/reflow2.json

- **reflow's master orchestrator reviewed: the build loop was BOUNDED and the design intent was re-read into it** — `req:build-loop-is-bounded` + `req:intent-is-reinjected` (both `proposed`, parked) and `dec:anchor-retire-or-implement` (OPEN). reflow's `workflow_master.json` set `P3_P4_loops: 3` and on exhaustion triggered a resync to P2, escalating to P1 above `drift_score > 0.5` — so work HALTED and went back up. reflow2 **measures** that decay (the erosion trial is a regression gate) and never bounds it; `cap:budget` is resource budgets against Constraints, not iteration budgets on the process. Separately, reflow re-injected the architecture anchor at phase transitions, at resync, and **every 5 iterations**, to *"remind yourself of design intent and constraints"* — reflow2's periodic re-injection (`cap:read-loop-hint`, the Stop nudge) carries loop DEBT instead: what you owe, not what you were trying to build. **STRAIGHT DEFECT FOUND**: the `Anchor` node type (`schema/structure.yaml:108`) has no `node::ANCHOR`, no reader, no writer and **zero instances** — superseded by `Snapshot` (14 instances; `temporal.rs:62` says it "generalizes the P2→P3 Anchor") — yet `describe_schema` still advertises it, with a hint claiming *"DriftEvents reference the Anchor they diverged from"* when all 37 reference something else. Same defect as the property sweeps at **node-type granularity**, which neither sweep examined. HELD for Anthony: what `rigid` should actually mean — reflow gave each mode seven behavioural commitments (drift_response, new_functionality_policy, change_request_required…) and routed mid-build discoveries to `change_requests.json` with a STOP; reflow2's `Project.mode` is live but drives exactly one thing, `heal.rs:431` "in rigid mode HEAL only proposes". **Wiring lesson applied**: connecting to an already-connected node (`req:drift-has-direction`) via an edge in `structural_rule` kept all three new nodes OUT of any island — `detect_defects` stayed at 6. — @ajs — 2026-07-28 — docs/design/reflow2.json

- **The schema sweep had a hole: it measured NODE properties and never looked at EDGES** — `req:edge-defaults-do-not-assert`, `proposed`, parked with the rest. Nineteen edge properties carry a schema default; four are single-valued wherever they appear (`ALLOCATED_TO.weight_basis` estimated on 96/96, `DEPENDS_ON.optional` false on 123/123 — asserting that every dependency in the design is MANDATORY). **The sharp one is a worse defect than the node sweep found**: 225 edges carry `weight_basis` ("how the weight was set") and **ZERO carry a `weight`** — they state the provenance of a coupling strength nobody recorded. So the node sweep's test, *"a property that never varies is not carrying information"*, is necessary but NOT sufficient on edges: `DEPENDS_ON.weight_basis` genuinely varies (evidence 70 / estimated 53) and still refers to nothing. Ten more are latent on edge types with zero instances — `VIOLATES_RULE.severity` defaults to `high`, so the first violation anyone records claims high severity unprompted. Found by tripping over it: a `DEPENDS_ON` created with NO properties came back `{"optional": false, "weight_basis": "estimated"}`, while capturing a requirement about exactly this. Open underneath it, and Anthony's call: should `weight_basis` default at all, or be absent until a weight exists — the answer `ver:interface-spec` already gave for `Interface.medium`. — @ajs — 2026-07-28 — docs/design/reflow2.json

- **The original reflow's SE workflows reviewed against reflow2; four findings captured, none built** — all four land `proposed`, none satisfied, and that is the honest state. `req:functional-vocabulary-computes` (`is_entry_point`/`is_exit_point`/`inputs`/`outputs` declared and read by nothing — three of reflow's five FUNCTIONAL-mode detectors are unimplementable purely because of it; the SERVICE mode came over and in better form). `req:framework-is-chosen-not-defaulted` (reflow's `framework_registry.json` + `00b-framework_selection.json` made framework choice a user-confirmed architectural decision whose LESSON-01 reads *"Do NOT default to UAF!"* because the choice GATED which analyses ran; reflow2's vestige is `Project.domain`, written by genesis and read by nothing). `req:version-combination-is-an-object` (reflow's SE-08 Mixed-Version Validation named and re-validated an N-way version combination — the mechanism `req:epochs-can-be-planned` asks for and reflow2 has only pairwise). `req:adopt-says-how-much-it-got` (RE-06 computed extraction coverage and gated at 80%; adopt cannot say what it did not see). **HELD for Anthony, not captured**: behavioural equivalence between two implementations has no home in the Verification vocabulary — `method` and `kind` say how you looked and whether you checked spec or intent, neither says "the new thing behaves like the old thing", which is the premise of reflow's whole 01f migration workflow and of evolving a live stack incrementally. Two edge-modelling lessons on the record: `CONTRADICTS` is NOT in `structural_rule`, so corroboration edges are invisible to every HEAL detector; and `DECOMPOSES` triggers delivery roll-up (`report.rs:566` — parent delivered iff EVERY child is), so decomposing a 10-instance parent into 2 children would report it delivered with 8 outstanding. — @ajs — 2026-07-28 — docs/design/reflow2.json

- **Install once per machine — no project needs setting up** — LANDED in 69d9a4d, RELEASED in v0.18.0. `req:no-setup-per-project` + `dec:install-once-per-machine`, both Anthony's word: the installer registers reflow2 at USER scope (server, ten commands, loop hooks, a `reflow2` command on PATH) and `install.sh` does it itself, so `cd anywhere && claude` + `/genesis` is the whole story and no project is set up at all. Made safe by `--only-if-present` / `cap:latent-surface`: where no design has been started, one tool is served and NOTHING is created — without it a user-scope server drops a RocksDB store into every directory a session ever opens in. Also: `/genesis` and `/adopt` ship (the previous eight commands were all mid-loop, so a new project had no discoverable way in); the SessionStart line stopped asserting a design graph exists; two `reflow2_init.py` one-liners (`--check` crashed on an empty dir; the pre-update backup used `--export` where a shared server holds the lock, now falls back to `--export-snapshot`). **Session gotcha worth keeping**: `/mcp` does NOT replace a running stdio MCP server — a process from two days earlier served this whole session, reporting a contradiction defect fixed hours before and stamping every export 0.13.0. Check `ps -eo pid,lstart` and read the START DATE. — @ajs — 2026-07-28 — getting-started/{commands,SETUP.md,README.md}, .claude/commands/, tools/{loop_nudge,test_loop_nudge,test_nudge_path,reflow2_install,reflow2_init,install.sh}, crates/reflow2-mcp/src/{latent.rs,lib.rs,main.rs}, crates/reflow2-mcp/tests/latent_mode.rs, .github/workflows/release.yml

- **v0.18.0 CUT AND DEPLOYED 2026-07-28** (rel:v0180 deployed, rel:v0170 + rel:v0160 retired, epoch:v0180 at sequence 210) — **the release that makes install once-per-machine real**: the first published assets carrying `req:no-setup-per-project` (v0.17.0's install.sh promised it; its assets predated it, so the curl install died at its last step on every machine). Kit now ships `reflow2_install.py`; binary accepts `--only-if-present` and serves the latent surface (verified live: one tool in a bare dir, nothing created; 117 in a directory with a design). No schema move (28/56), no pin move, no upgrade action. rel:v0180 records 147 INCLUDES (v0.17.0's 142 + art:latent, art:machine-install, art:seam, art:seam-test, art:upgrading-v0180). Also on the record: v0.17.0 was cut without epoch/deploy/retire bookkeeping — brought honest this cut (rel:v0170 built→retired, rel:v0160's stale `deployed` closed out). Deploy observed, not asserted: installer re-run on the maintainer's machine, 0.18.0 binary confirmed, shim from the morning's outage deleted. ONE ANOMALY, ~~unreproduced~~ SOLVED same day: the "94 findings" runs were `reflow2_check.py` invoked with cwd outside the repo — `--root` defaults to `.`, so every registered artifact resolved missing. Operator error, not a flake; run the gate from the repo root or pass `--root`. Cut verified on CI (runs 30372895578-ish cut + 30373321251 release); this machine has no Rust toolchain, deliberately. — @ajs — 2026-07-28 — Cargo.{toml,lock}, CHANGELOG.md, AGENTS.md, reflow2.toml, docs/{overview.md,upgrading-to-v0.18.0.md,design/reflow2.json}, tag v0.18.0

- **CI red on main since seam_report landed: its `design` param advertised no type (BL-28)** — LANDED across e7f897b (retype as `Map<String, JsonValue>`, the MirrorSurfaceReq idiom), 3683608 (fmt + the design gate's own drift catch, reconciled two-sided as `design_holds`, chg:accept-975fcb51ce6ec113), 31453f1 (toolsnap golden regenerated — the surface change made reviewable, as BL-76 intends). Run 30371232790 green. Notable: this machine has no Rust toolchain, so the reconcile ran through the released 0.17.0 binary against a scratch import, and the toolsnap diff was applied from CI's own output. Main is releasable again — the machine-wide installer (69d9a4d) still needs a cut release to actually reach a user. — @ajs — 2026-07-28 — crates/reflow2-mcp/src/service.rs, docs/design/reflow2.json, tools/toolsnaps/seam_report.json

- **BL-12 rung: sharing becomes the DEFAULT (`--shared`)** — LANDED in 280397c ("Sharing a design between sessions becomes the default, and needs no setup"). A session finds the server holding its graph, starts a detached one if there is none, and proxies to it, so N concurrent sessions read and write one design with no setup. The claim line outlived the work: it was held by @api-boss from the StoryFlow fleet on ANOTHER machine, which is not a seat that works this repo — cleared by @ajs 2026-07-28 on Anthony's word. *If you are reading this because you claimed something here and cannot find your line: a claim from a seat that has left the machine is stale by definition, and clearing it is not the same as taking the work.* — @api-boss — 2026-07-27

- **v0.16.0 CUT 2026-07-27** (rel:v0160 deployed, rel:v0150 retired, epoch:v0160 at sequence 190) — **the release that makes a pile of documents into a design**, and the FIRST since v0.11.0 that genuinely locks an older reflow2 OUT: `PERFORMED_IN` takes the schema 55→56 edge types and the stamp counts edge TYPES, so machines sharing a graph must upgrade together ([docs/upgrading-to-v0.16.0.md](docs/upgrading-to-v0.16.0.md) leads with that; the enum growth alongside it does NOT do this, and the doc says why). **SP-3b/BL-7 CLOSED after months** — `ingest_step`, the repeated-round handshake agent.rs specified at SP-2 and nothing had driven; nothing written until the last round (prepare replays against a throwaway graph, so an abandoned handshake strands nothing) and NO server-side session state. **Rationale and test evidence now extracted**, both landing unasserted — a recovered Decision `proposed`, a recovered Verification `planned` — which required tightening `unverified_capability` to demand a PASSING check, since attaching a planned one had silenced the very question the detect-and-ask skill always said it should not. **Near-matches asked about, not guessed**: the two schema thresholds nothing had ever read, plus storyflow's token-subset pass for what scoring cannot reach (`Gateway` vs `API Gateway` = 74, below every threshold). **BL-95 coverage** — what the design has never been TOLD about, rolled up and ranked, never a score. **req:design-the-simulator ACCEPTED** and its first rung built: a check records WHERE it ran, so `proven only in simulation` is sayable. **BL-107** — the export lineage chain is now gate-checked, found because I broke it for six commits while gate, loop and detect_gaps all stayed green. THE DISK WAS 100% FULL at cut time: `target/` held **319 GB** (897 files >100MB in debug/deps, 239 copies of the test binary — cargo never GCs old hashed artifacts and this binary is ~460MB with RocksDB static). `cargo clean` freed it; expect this to recur. Manifest: **78 artifacts** with frozen cut-time checksums plus cmp:docs, cmp:skills, cmp:claims; `built_capabilities_not_covered` EMPTY. Gate OK with **3 notes**, all three the ideas Anthony captured at cut time and deliberately left `proposed` — interface-spec completeness, seam incompatibility, and declared design dependencies (BL-109). 57 suites, clippy 0 both crates, fmt, schema, 112 toolsnaps, smoke, skill lint, installer, nudge-path, stale-seat, degraded, merge-driver, loop-nudge, gate suite, render-views, doc versions — @ajs — 2026-07-27 — Cargo.toml, CHANGELOG.md, AGENTS.md, README.md, schema/{operate,verify}.yaml, docs/upgrading-to-v0.16.0.md, crates/reflow2-core/src/{coverage,verify,ingest,agent,detect}.rs, crates/reflow2-mcp/src/service.rs, docs/design/reflow2.json

- **v0.15.0 CUT 2026-07-26** (rel:v0150 deployed, rel:v0140 retired, epoch:v0150 at sequence 180) — a MINOR bump because the schema moved, [docs/upgrading-to-v0.15.0.md](docs/upgrading-to-v0.15.0.md) written, and its honest headline is **there is nothing to do**. TWO THINGS SHIP. (1) **`Verification.method` gains `demonstration` and `observation`** (Anthony's taxonomy): test, analysis, inspection and DEMONSTRATION are the four canonical methods in DoD/INCOSE practice and reflow2 carried three, so *"we showed it working"* — how a great deal of acceptance actually closes — had to be miscoded as `test`; `observation` is the as-fielded method, watching a system run unchanged, which is neither inspecting an artifact nor running a contrived example. `review`/`simulation` KEPT, because removing a value strands every node carrying it. **The safety claim was PROVEN not asserted**: a binary built with the previous value set reads a graph containing `demonstration` and reports it faithfully — validation runs on write, the stamp counts node/edge TYPES (28/55, unchanged). Two tests: every method accepted through the real constructor, and an invented one REFUSED rather than silently defaulted. (2) **THE v0.14.0 REGRESSION FIXED** (BL-105) — found the same day it shipped, by hand, by no detector: graph held + `--http` served the degraded surface over STDIO with nothing on the port, so every session at that URL got connection-refused, indistinguishable from reflow2 never being configured. `serve_http` is now generic over the service it carries; the startup line distinguishes the surfaces; **4 new cases MUTATION-CHECKED** (reverted against v0.14.0 all four fail with *"nothing ever listened on the port the caller asked for"*). ALSO RECORDED, built or not: **dec:read-while-held-mechanism DECIDED** (read-only view as of open, not a catchable-up secondary — staleness bounded and knowable, which is what req:stale-seat-knows just bought for exports) and **req:verification-keeps-up ACCEPTED** + cap:verification-freshness planned (BL-106) — the general form of this release's own bug, and the finding behind it is that `Verification.last_run_at` is written by verify.rs and read by NOTHING. TWO INCIDENTAL FINDINGS: `reflow2_cli.py` prefers `target/release` and silently served a stale v0.5.0 binary mid-verification (docstring corrected); and `art:schema` points at schema.rs not the YAML it embeds, so a pure schema-file edit produces NO drift — harmless here, unfiled. Manifest: **72 artifacts** with frozen cut-time checksums plus cmp:docs, cmp:skills, cmp:claims; `built_capabilities_not_covered` EMPTY, **detect_gaps ZERO**. 57 suites, clippy 0 both crates, fmt, schema, 108 toolsnaps, skill lint, smoke, installer, nudge-path, stale-seat, degraded (13), merge-driver, loop-nudge, gate suite, render-views, doc versions, design gate 0 notes, loop CLEAN — @ajs — 2026-07-26 — Cargo.toml, CHANGELOG.md, AGENTS.md, schema/verify.yaml, docs/upgrading-to-v0.15.0.md, crates/reflow2-core/src/verify.rs, crates/reflow2-mcp/src/{main,service}.rs, tools/test_degraded_server.py, docs/design/reflow2.json

- **v0.14.0 CUT 2026-07-26** (rel:v0140 deployed, rel:v0130 retired, epoch:v0140 at sequence 170) — **the release that lets several sessions share one design, including across machines**, cut on Anthony's word so he can use it on his other machine. A minor bump with NO schema change and no upgrade doc: nothing breaks, nothing in a consumer repo changes, the version moves because capabilities were added. **THE VALUE IS THE THREE CASES BEING TOLD APART RATHER THAN BLURRED**: different projects on one machine never needed anything (own graph dir, own server, they never meet — measured, not assumed); the same project on one machine needs a SERVER, because the store is single-writer PER PROCESS; the same project from ANOTHER machine additionally needs the host NAMED, because the transport answers only allowlisted `Host` headers and with no authentication that allowlist is the only guard. **`req:sessions-across-machines` ACCEPTED, `cap:remote-sessions` VERIFIED.** `--http-allow-host` EXTENDS the default rather than replacing it (naming a remote machine must never lock out the loopback sessions already on that server), and a reachable bind with no named host now WARNS instead of refusing every remote session with a bare 403 and saying nothing. Proven with the Host header a remote session actually sends — no mocks, since that header IS what makes a request remote to this transport: unnamed host refused FOR THAT REASON (a bare status assertion would have passed on an unrelated 403 and made the differential prove nothing), named host completing a whole session, a remote seat and a local seat sharing one design, advisory firing on a wildcard bind and silent on loopback. 5 new cases, 9 in the suite, in CI. docs/collaborating.md now says live sharing HAS A CENTRE — the server's machine cannot sleep and neither side can work offline — so it composes with the git route rather than replacing it. Manifest: 71 artifacts with frozen cut-time checksums plus cmp:docs, cmp:skills, cmp:claims; `built_capabilities_not_covered` EMPTY and **detect_gaps ZERO**. **THE GATE EARNED ITS KEEP AGAIN**: cargo fmt reordered an import after the checksum baseline was accepted, and the drift was caught rather than shipped. THE EXPORT'S STAMP was set to 0.14.0 without a reconnect, and it is not a fudge: the running server is the pre-bump binary, so `--export-snapshot` was taken with the NEW binary and proved byte-identical to the authoritative export apart from that one value — which also, incidentally, retires the "best-effort snapshot might lack unflushed writes" caveat for this document by measurement. A post-reconnect re-export must come back byte-identical. 57 suites, clippy 0 both crates, fmt, schema, 108 toolsnaps, skill lint, smoke, installer, nudge-path, stale-seat, degraded, merge-driver, loop-nudge, gate suite, render-views, doc versions, design gate 0 notes, loop CLEAN — @ajs — 2026-07-26 — Cargo.toml, CHANGELOG.md, AGENTS.md, crates/reflow2-mcp/src/main.rs, tools/test_shared_sessions.py, docs/collaborating.md, docs/design/reflow2.json

- **SEVERAL SESSIONS NOW SHARE ONE DESIGN, LIVE** (cap:shared-sessions; req:sessions-share-a-graph + req:seat-per-client accepted; **dec:central-host ACCEPTED** on Anthony's word: *"I want to have multiple sessions running and being able to use the same reflow2 graph"*). `--http 127.0.0.1:8787` serves the design to many client sessions from one process. THE CONSTRAINT IS SATISFIED, NOT WORKED AROUND: the store is single-writer PER PROCESS, so one server with six sessions still has exactly one writer — which is why this needed a transport rather than a storage change. rmcp builds a service per session via its factory, and `ReflowService::share` draws the line: the graph and write-generation are shared (properties of the design), the seat and read-hint memory are fresh (properties of whoever connected). TWO UNDERLYING CHANGES: the graph moved Mutex→**RwLock** so reads stop queueing behind each other — **and the compiler audited the read/write split for free: all 32 read sites genuinely need only `&DesignGraph`, first try**; and **seats are minted per SESSION, not per process** (without it a shared server reports every client as the same owner and claim_report tells six sessions they are each other — silent). 11 new pure-Rust crates (hyper + hyper-util, no axum: routing and extractors we would not use), nowhere near the RocksDB scale, so unconditional rather than feature-gated. Proven over real HTTP: distinct sessions, A's write visible to B immediately, DISTINCT SEATS, four concurrent readers all finish, and the served kit reaches an HTTP client identically (4 cases, in CI). ALSO CONFIRMED BY MEASUREMENT: different projects on one machine always worked — two servers, two graphs, two identities, alive at once. NO AUTHENTICATION — the flag help and startup line both say bind loopback or a tailnet. docs/collaborating.md now leads with the server and keeps worktrees as the different-machines answer. 57 suites, clippy 0, fmt, 108 toolsnaps, smoke, loop CLEAN — @ajs — 2026-07-26 — crates/reflow2-mcp/src/{main,service,skills}.rs, crates/reflow2-core/src/identity.rs, tools/test_shared_sessions.py, docs/collaborating.md, docs/design/reflow2.json

- **v0.13.0 CUT 2026-07-25** (rel:v0130 deployed, rel:v0120 retired, epoch:v0130 at sequence 160) — **the release that makes concurrency honest**, and every piece of it came from Anthony's own fleet: design identity (a name minted with zero coordination, adopted for existing graphs, refused rather than guessed), claims that name their session with liveness COMPUTED from the OS, and the loop's safety net both PROVEN (the registered hook run as registered) and INSTALLED (reflow2_init registers it into `.claude/settings.local.json`, pointing at the kit). Minor: `CLAIMS.seat` is additive, and [docs/upgrading-to-v0.13.0.md](docs/upgrading-to-v0.13.0.md) is written. Manifest: **68 artifacts** with frozen cut-time checksums plus cmp:docs, cmp:skills, cmp:claims — and `detect_gaps` went to **ZERO** as a result, because the five components built since v0.12.0 are now genuinely released rather than acknowledged. **THE SMOKE TEST EARNED ITS KEEP AT CUT TIME**: the CLI import→export round trip stopped being byte-identical, because a fresh graph minted its own id — so importing a whole design into an EMPTY store now adopts that design's name (a restore is the same design in a new store), while a store that already holds a design keeps its own (which is what protects the stale-seat remedy). That is a design decision the cut forced into the open, not a patch. 57 suites, clippy 0, fmt, schema, 108 toolsnaps, smoke, installer 45, nudge-path 9, stale-seat, degraded, merge-driver, loop-nudge, gate suite, doc versions, loop CLEAN, gate 0 notes — @ajs — 2026-07-25 — Cargo.toml, CHANGELOG.md, AGENTS.md, docs/upgrading-to-v0.13.0.md, crates/reflow2-core/src/{identity,graph}.rs, crates/reflow2-mcp/src/main.rs, docs/design/reflow2.json

- **req:nudge-path-proven BUILT — four of the fleet-lessons five done** (cap:nudge-path-proven, cmp:nudge). The Stop hook is the only trigger that fires when the agent has stopped calling anything, and nothing verified it: `test_loop_nudge.py` covers the script given its inputs and passed the whole time nobody had checked the hook was REGISTERED. `tools/test_nudge_path.py` now reads `.claude/settings.json`, takes the command the harness would run, and runs THAT with a real Stop payload — asserting the JSON block decision the harness consumes, silence when the loop was checked, and once-only firing via `stop_hook_active`. **THE TEST CAUGHT ITSELF FIRST**: v1 asserted a non-zero exit code and failed against a working hook, because Stop hooks block by printing `{"decision":"block"}` and exiting 0 — a test that encodes the wrong contract is worse than none, since it gets "fixed" by breaking the thing it guards. THE BACKSTOP MATTERS MORE THAN THE PROOF: the server reports installed | absent | **broken** (registered but the script missing — the dangerous middle case, since the settings file looks right) | unknown (never reported as absent), and when it is missing the advisory rides the HANDSHAKE INSTRUCTIONS — the one channel every session reads unasked — with `loop_status` carrying it machine-readably. Not theoretical: **reflow2_init installs no hooks, so every consumer project has no session-end nudge**, and until now nothing said so. Verified against a real consumer install: handshake carries the warning, `loop_status.nudge = absent`. **AND THE INSTALLER NOW REGISTERS IT** (Anthony: "yes, have the installer install the hook") — into `.claude/settings.local.json` rather than the shared `settings.json`, because the command carries an absolute path to YOUR kit and a collaborator inheriting it gets precisely the silently-broken hook this work exists to catch; it points at the KIT's script so it updates with the package and nothing in the consumer repo goes stale; merged never clobbered, a repointed hook left alone and reported, idempotent on re-run. Verified end to end on a fresh install: server reports `installed`, no nag, and the registered command fires. 9 path cases + 4 detection + 5 installer cases (45 installer total). A TEST ASSERTION HAD TO CHANGE HONESTLY: the no-churn test asserted `.claude` must not exist, which was a proxy for "no copied kit files" — now `.claude/settings.local.json` legitimately exists, is gitignored, and cannot churn the repo, so the assertion says what it means instead. 57 suites, clippy 0, fmt, 108 toolsnaps, smoke, loop CLEAN, gate 0 notes — @ajs — 2026-07-25 — crates/reflow2-mcp/src/{nudge.rs,service.rs}, tools/test_nudge_path.py, .github/workflows/ci.yml, docs/design/reflow2.json

- **req:claims-have-owners BUILT** (cap:claim-liveness) — three of the fleet-lessons five now done. Claims record a SEAT (`machine:pid:mint`, minted once per process, zero coordination — the same doctrine as the design's own name), and `claim_report` **computes** liveness by asking the OS whether that process exists: nothing writes "I am alive", so nothing can be stale. THE CONSEQUENCE THAT MATTERS: a claim whose session exited is `gone`, listed in `stale` with its note intact, and **excluded from overlaps** — a collision with nobody is not a collision, and reporting it as one is how an advisory report starts lying, because people wait for somebody who left. Another machine, or a pre-seat claim, is `unknown` and STILL counts as a possible collision: reading it as free would invite taking work in progress, which is the expensive direction to be wrong in. Proven end to end over stdio with a genuinely exited server: `gone` vs `live`, zero false overlaps, ghost named. 15 claims cases (6 new). Schema `CLAIMS.seat`, additive. CAUGHT MYSELF: wrote schema/core.yaml's hash onto art:schema (which points at schema.rs) and corrected it in the same pass — the two-sided accept records both the mistake and the fix rather than quietly restoring it. 57 suites, clippy 0, fmt, schema, 108 toolsnaps, smoke, loop CLEAN, gate 0 notes — @ajs — 2026-07-25 — crates/reflow2-core/src/{identity,claims}.rs, crates/reflow2-core/tests/claims.rs, crates/reflow2-mcp/src/service.rs, schema/core.yaml, docs/design/reflow2.json

- **req:design-identity BUILT** (cap:design-identity, cmp:identity) — the second of the fleet-lessons five. A design's id is established on first open from `<graph>.id.json`, a SIBLING of the store because the id namespaces every stored key and must be known before the design can be read; minted with zero coordination (nanosecond + pid + absolute path through fnv1a — no dependency added for sixteen hex chars); the label is a changeable layer on top; an unreadable identity is REFUSED, never defaulted, because defaulting opens a different design at the same path and says nothing. **THE MIGRATION IS THE FEATURE**: a store already holding a design under the old shared id ADOPTS it forever — minting would leave the design on disk and open an empty one beside it, and `graph_id` is inside the export's content hash, so adoption is also what keeps every committed export and chain link valid. Verified against the real graph before trusting it: a snapshot of this repo's store adopted `reflow2` and its content hash matched the committed export exactly. **TWO BUGS FOUND BY VERIFYING RATHER THAN ASSUMING**: (1) the snapshot copies the store but not the sidecars, so a snapshot of an identified graph minted a fresh id and exported an EMPTY design as a success — caught by the degraded suite the hour identity landed, and it would have started returning empty designs at the next reconnect; (2) `test_nothing_is_left_behind` stripped only the one sidecar suffix it knew about, so it passed while `.id.json` residue accumulated in /tmp — both fixed, and cleanup now globs every sidecar rather than naming them one at a time. THE PAYOFF, as a test: two independently created ON-DISK designs mirroring each other's published surface, which no user could do before. 7 cases + design_identity on the surface (108 toolsnaps). 57 suites, clippy 0, fmt, smoke, installer 35, stale-seat, degraded 9, loop CLEAN, gate 0 notes — @ajs — 2026-07-25 — crates/reflow2-core/src/{identity,graph}.rs, crates/reflow2-mcp/{src/skills.rs,src/main.rs,tests/design_identity.rs}, docs/design/reflow2.json

- **v0.12.0 CUT 2026-07-25** (rel:v0120 deployed, rel:v0110 retired, epoch:v0120 at sequence 150) — cut so Alex can upgrade. **The release that ends consumer-repo churn**: skills AND the ~20 KB instruction document are compiled into the binary and served (list_skills / get_skill / get_instructions), so an install leaves a 2.4 KB pointer, the MCP configs and `.reflow2/` — measured 2,405 bytes in-repo against 19,792 served. First release that REMOVES files from a project, so [docs/upgrading-to-v0.12.0.md](docs/upgrading-to-v0.12.0.md) is written and says what `--check` will show. Also ships the six-session fleet fixes (degraded server + `--export-snapshot`), **req:stale-seat-knows** (an export that would DELETE what the shared file holds is refused, naming the ids and the remedy), `canonical_checksum`, `import_graph --path`, and the accepted fleet-lessons decisions. Manifest: 63 artifacts with frozen cut-time checksums plus cmp:docs and cmp:skills — the second release to record documentation as a deliverable. **THE GATE EARNED ITS KEEP AT CUT TIME AGAIN**: `check_doc_versions` caught two "shipping at v0.11.0" claims in AGENTS.md that the bump had made false, which is exactly the drift class it was built for. 56 suites, clippy 0 both crates, fmt, schema, 107 toolsnaps, skill lint, smoke, installer (27), stale-seat (5), degraded (9), merge-driver, loop-nudge, gate suite, design gate 0 notes, loop CLEAN. The export was re-stamped 0.12.0 after the `/mcp` reconnect — verified beforehand with `--export-snapshot` (identical content hash, so the re-export could only be the stamp) and the diff came out as exactly that one line, chain intact — @ajs — 2026-07-25 — Cargo.toml, CHANGELOG.md, AGENTS.md, docs/design/reflow2.json

- **req:thin-install FINISHED — the instructions follow the skills into the server** (cap:skills-served extended, chg:instructions-served). Anthony asked whether Alex's ask was actually done; checking rather than answering from memory found it was NOT: a fresh install laid down 5 files and no skills, but an upgrade still rewrote a **19.8 KB AGENTS.md**, proven by simulating an older install and running `--check` (1 change: update AGENTS.md). Worse, `test_an_upgrade_touches_nothing_in_the_project` only re-ran the SAME version, so it passed while the requirement was unmet — a test whose name overstated what it proved. Now: `build.rs` embeds the instruction document beside the skills, `get_instructions` serves it, the handshake says to call it first, and what an install leaves is **getting-started/POINTER.md → 2.4 KB** naming the three tools. Measured end to end: repo 2,405 bytes, served 19,792 bytes, 107 tools. The pointer deliberately keeps the graph-text-is-data rule, because that one governs reading the graph at all and must hold for an agent that never calls a tool. New test `test_a_new_release_touches_nothing_in_the_project` simulates a NEWER reflow2 against an older project and asserts no skill or instruction content moves — the claim the upgrade doc makes, now actually backed. 27 installer cases, 6 served-skills cases, loop CLEAN, gate 0 notes — @ajs — 2026-07-25 — getting-started/POINTER.md, crates/reflow2-mcp/{build.rs,src/skills.rs,src/service.rs}, tools/reflow2_init.py, docs/upgrading-to-v0.12.0.md, docs/design/reflow2.json

- **req:stale-seat-knows BUILT — the silent data-loss path is closed** (cap:stale-seat-refusal, cmp:sync). Anthony after reading the collaboration guide: *"sounds like we need to finish req:stale-seat-knows"* — the guide having to teach a workaround was the argument for removing the need. THE CHECK IS DELIBERATELY NARROW, and that is the design: not "did the file change" (which fires on every ordinary export and would be passed by habit inside a day) but **"would this write DROP something the file holds"**. The file where this seat left it → silent, one hash against a marker in `<graph-path>.sync.json`; moved but nothing lost → written and reported; would delete nodes or edges → REFUSED with the ids, the three-step remedy and `accept_divergence` for discarding on purpose. So the loud case is exactly the lossy case. THE MARKER IS ITS OWN SIDECAR ON PURPOSE — folding it into `.meta.json` would have changed the stamp's shape, and every existing graph would then fail to open with "the version stamp is not readable". FOUND WHILE WRITING THE REFUSAL: it told people to `import_graph` from a path, which the tool could not do — so import gained `path` (and recording the sync on import, or the named remedy would not clear the condition it names). 7 core cases + **5 end-to-end with two real graphs and one shared file**, including a fresh empty graph being stopped from wiping the committed design, and the import-then-export remedy actually clearing the refusal. docs/collaborating.md's workaround section is now a description of what reflow2 does for you; the PDF was regenerated. loop_status CLEAN, gate OK 0 notes. 56 suites green, clippy 0, fmt, 106 toolsnaps, smoke, skill lint, installer, merge-driver, degraded, doc-version — @ajs — 2026-07-25 — crates/reflow2-core/src/{sync,provenance}.rs, crates/reflow2-core/tests/stale_seat.rs, crates/reflow2-mcp/src/service.rs, tools/test_stale_seat.py, docs/collaborating.md, docs/design/reflow2.json

- **SKILLS ARE SERVED, NOT INSTALLED — and the loop is CLEAN with a 0-note gate** (cap:skills-served; req:thin-install ACCEPTED, authored by Alex; dec:skills-served ACCEPTED on Anthony's word: *"we need to get rid of the issue of a stale release. if going with the catalogue-in-instruction is the cost, I think it is worth that"*). Alex's feedback was that setup should be a paragraph plus an MCP entry, after which updates stay inside the reflow2 package — and the installer's own docstring already conceded the defect ("the kit is copied into your project, so it otherwise freezes at install time while reflow2 keeps moving"), with the proof sitting unnoticed in reflow2's own manifest: **0.8.0/12 skills against a 0.11.0/15 project, four releases stale**. THE LITERAL ASK WAS IMPOSSIBLE AND IS RECORDED AS SUCH: no harness discovers skills from MCP (Claude Code/OpenCode/Copilot read `.claude/skills/`, Grok reads `.grok/skills/`), so the substance was delivered instead — `build.rs` compiles the kit into the binary, `list_skills`/`get_skill` serve it, and the CATALOGUE RIDES THE HANDSHAKE INSTRUCTIONS because that is the only channel a client puts in the agent's context unasked. The installer now copies nothing and removes what an older kit left: untouched files deleted WITH THE REASON, edited files kept and reported as SHADOWING the served skill — the dangerous case, since a harness does auto-load those and an edited copy would silently win over every future release. THE TRADE IS WRITTEN DOWN RATHER THAN BURIED (docs/upgrading-to-v0.12.0.md): a served skill is never auto-matched by the harness, so the catalogue is progressive disclosure rebuilt by hand. 5 Rust cases hold the served set byte-identical to the source (ver:kit-manifest-agrees finally has an owner) + 23 installer cases including `test_an_upgrade_touches_nothing_in_the_project`. ALSO THIS PASS: Anthony approved the fleet-lessons recommendations wholesale — **dec:identity-out-of-band and dec:advisory-concurrency ACCEPTED**, five requirements accepted (stale-seat-knows, design-identity, claims-have-owners, nudge-path-proven, read-while-held), every open question answered on the record, and the resulting "approved but not yet built" gaps acknowledged with that reason so the open list keeps meaning *needs a decision* rather than *needs typing*. **loop_status CLEAN, gate OK with 0 notes.** 55 suites green, clippy 0 both crates, fmt, 106 toolsnaps, smoke, skill lint, installer, merge-driver, doc-version — @ajs — 2026-07-25 — crates/reflow2-mcp/{build.rs,src/skills.rs,src/service.rs,tests/served_skills.rs}, tools/reflow2_init.py, docs/upgrading-to-v0.12.0.md, SKILLS.md, docs/design/reflow2.json

- **THE FLEET OUTAGE FIXED, AND THE FLEET'S OWN PROTOCOL MINED** (req:never-silently-absent ACCEPTED on Anthony's word — "need to fix this") — reported from his six-session StoryFlow fleet on 2026-07-25, where the single-writer lock left five sessions with **no reflow2 at all** and one of them reported the project as having no design brain, because the good diagnosis went to stderr and died with the process. Now: any open failure serves a handshake with the reason **in the server instructions** plus one tool `reflow2_unavailable` (reason + remedies + an explicit *do not conclude reflow2 is missing*), and `--export-snapshot` reads a graph another seat is holding so a locked-out session can still start the merge workflow. 9 cases against a **real** held lock, wired into CI's `full` job. **FOUND BY THIS SESSION'S OWN GATE**: four artifacts registered from raw `sha256sum` output read as 100% drift while identical on disk, because drift is a string comparison and the gate speaks `sha256:<hex>` — `canonical_checksum` fixes it, 2 tests. Then the extraction Anthony asked for: [docs/fleet-lessons.md](docs/fleet-lessons.md), seven mechanisms from a working 15-seat fleet, of which **`dec:identity-out-of-band`** (names assigned with zero coordination, never derived — the fleet DELETED a whole mint-race guard by removing the derivation) and **`dec:advisory-concurrency`** (claims announced by being pushed; crossings settled by what git can prove, never by exclusion) are proposed for his word, and three requirements are filed proposed: **req:stale-seat-knows** (the one that matters — a seat writing over a moved record is silent data loss in the architecture we already chose), req:claims-have-owners, req:nudge-path-proven. Plus req:read-while-held (blocked upstream: `open_as_secondary` is not exposed by dynograph-storage). AGENTS.md now names which grants carry across sessions and which do not — reversible standing, outward-facing per-session — the fleet's clearest rule. 54 suites green, clippy 0 both crates, fmt, 104 toolsnaps, smoke, skill lint, gate suite, degraded suite, gate OK 7 notes — @ajs — 2026-07-25 — crates/reflow2-mcp/src/{degraded,main}.rs, crates/reflow2-core/src/artifact.rs, tools/test_degraded_server.py, docs/fleet-lessons.md, .github/workflows/ci.yml, docs/design/reflow2.json

- **dec:nested-graphs DECIDED — option (c), a graph per ownership boundary** (Anthony, 2026-07-25) — and its first rung built: cap:mirror-surface. A design is its own graph when something is separately OWNED, RELEASED or SHARED; hierarchy does not decide, authority does. Edges cannot cross stores, so designs link by MIRRORING the other's published surface as local `imported` nodes, with the coordinate (mirror_of / mirror_content_hash / mirrored_at) on the mirrored Project — after which our components provides/consumes their Interface with ORDINARY LOCAL EDGES and the whole golden thread works unchanged. Crossing the seam reports as a published-boundary crossing. **COLLISIONS REFUSED not merged** (upsert would otherwise overwrite our design with theirs); a colliding edge is dropped rather than rewired; self-mirroring refused. Schema: Project + 3 mirror properties, additive. 7 tests. **THE BUILD EXPOSED rule:no-foreclosure item 5 for real**: every graph carried the same hardcoded graph_id, so no design could tell another from itself — `open_in_memory_as` fixes library/test use, and the durable case is filed as **req:design-identity** (proposed, gap left OPEN = the gate's 1 note) rather than half-built, because the id namespaces every key and a graph reopened under the wrong name would present as empty. Also recorded: (a) was not rejected but is a SUBSET of (c), and docs/org-scale-vision.md's "one graph the whole company plans in" is now a road not taken. 54 suites green, clippy 0, fmt, 104 toolsnaps, smoke, skill lint, merge-driver, installer, doc-version, gate 1 note — @ajs — 2026-07-25 — schema/core.yaml, crates/reflow2-core/src/{export,graph}.rs, tests/mirror_surface.rs, crates/reflow2-mcp/src/service.rs, docs/design/reflow2.json

- PUBLISHED-SURFACE EXPORT BUILT (cap:publish-surface / `export_surface`) — the first piece of req:design-composes, and deliberately the piece EVERY answer to dec:nested-graphs needs. Published Interfaces + their SPECIFIES/REALIZES contract artifacts + both sides + the project; requirements, capabilities, decisions, verifications and history stay home; nodes exported AS STORED (a trimmed node would import as a lie). **Counts and NAMES what it withheld** — a recipient cannot tell a small design from a filtered one — and an undesignated design gets an EMPTY SURFACE warning instead of a quietly empty file. Not chained (a derived view must not read as an ancestor). **IMPACT-CHECK EARNED ITS KEEP THIS TIME**: running propagate_change BEFORE coding put dec:export-hash-chain in the direct ring, which is what stopped the surface joining the chain — the practice I audited myself as having skipped 7 of 8 times today. **A TEST FOUND**: link_artifact writes REALIZES while the schema's contract-artifact intent is SPECIFIES, so an ICD registered the ordinary way was missing; both edges honoured now. 7 tests incl. the round trip (the other side imports cleanly, gets the contract, does not get our requirements). Also: reflow2's own boundaries DECLARED — ifc:mcp-tools + ifc:graph-export published, ifc:core-api internal. **STILL BLOCKED, unchanged**: the composition MECHANISM (coordinate, roll-up, freshness) waits on dec:nested-graphs, which is Anthony's call. 53 suites green, clippy 0, fmt, 102 toolsnaps, smoke, skill lint, merge-driver, doc-version, gate 0 notes — @ajs — 2026-07-25 — crates/reflow2-core/src/export.rs, tests/surface_export.rs, crates/reflow2-mcp/src/service.rs, docs/design/reflow2.json

- **v0.11.0 CUT 2026-07-25** (rel:v0110 deployed, rel:v0101 retired, epoch:v0110 anchored at the cut-time export hash) — a MINOR bump: two schema changes with [docs/upgrading-to-v0.11.0.md](docs/upgrading-to-v0.11.0.md) written, so a consumer reads before updating rather than updating blindly. **The manifest is the first to record documentation as a deliverable**: 45 artifacts with frozen cut-time checksums plus cmp:skills AND cmp:docs, which closes the unreleased_component gaps for docs/claims/sanitize/scope that dec:intent-preserved said to start from the next cut rather than backfill. Also in this cut: **reviewed defects built** (req:reviewed-defects) — acknowledge_defect / reviewed_defects / withdraw_defect_acknowledgement, the gap side's bargain finally kept on the defect side, and reflow2's own six accepted SPOFs migrated onto it, so `detect_defects` now returns EMPTY and loop_status can reach clean for the first time. FOUND WHILE BUILDING: the doc-version test pinned the literal version string, so it broke on release day — fixed to derive it, because a test that breaks every cut is one people edit rather than trust. 52 suites green, clippy 0, fmt, 101 toolsnaps, skill lint, smoke, merge-driver, installer, loop-nudge, doc-version and gate (0 notes) all green — @ajs — 2026-07-25 — Cargo.toml, CHANGELOG.md, AGENTS.md, crates/reflow2-core/src/{heal,detect}.rs, tests/reviewed_defects.rs, crates/reflow2-mcp/src/service.rs, tools/test_check_doc_versions.py, docs/design/reflow2.json

- KEY INTERFACES BUILT + add_decision DEFAULT FLIPPED (cap:key-interfaces, req:decision-status-not-asserted) — both on Anthony's word. `Interface.designation` internal|published (default internal: publishing is a commitment) + `set_interface_designation`, and the computation that earns it: `propagate_from` reports `boundary_crossings` NAMED, in radius and summary, plus `crosses_published_boundary` per node. Severability computed, not asserted. **A TEST CAUGHT A REAL BUG**: the first version counted a boundary one hop PAST the depth bound as crossed. Also pinned: seeding ON a boundary is not crossing it; withdrawal removes the crossing. 8 tests. **SCHEMA CHANGED TWICE** → v0.11.0 is a MINOR bump with [docs/upgrading-to-v0.11.0.md](docs/upgrading-to-v0.11.0.md) written: Decision.status default accepted→proposed (two of our own tests failed, both had leaned on the default instead of stating intent; existing graphs unaffected) and Interface.designation added. Also: **first real run of report-friction** → four findings filed as GRAPH ELEMENTS not backlog prose (reflow2-friction-2026-07-25.md holds the report): req:decision-status-not-asserted (fixed), req:reviewed-defects (accepted, gap left OPEN = the gate's 1 note), and two planned Verifications for missing checks — tools rejecting unknown args, and the kit manifest agreeing with the kit. **THE KIT MANIFEST IS FOUR RELEASES STALE** (.reflow2/kit-version.json says 0.8.0 / 12 skills; we are 0.10.1 / 15) and nothing noticed. 51 suites green, clippy 0 both crates, fmt, 98 toolsnaps, skill lint, smoke, merge-driver, installer, doc-version all green; gate OK 1 note — @ajs — 2026-07-25 — schema/structure.yaml, crates/reflow2-core/src/{graph,propagate}.rs, tests/key_interfaces.rs, crates/reflow2-mcp/src/service.rs, docs/upgrading-to-v0.11.0.md, docs/design/reflow2.json

- BRAINSTORMS PROMOTED (dec:nested-graphs, dec:mosa-conformance) — Anthony: "let's promote both". Four requirements, all accepted: **req:analyse-at-any-level** (already delivered by cap:scoped-analysis — it names the intent behind what was just built), **req:design-composes** (federation/multi-repo-linked-by-interfaces — accepted but BLOCKED on dec:nested-graphs, acknowledged with that reason), **req:key-interfaces** and **req:modularity-computed** (both from the MOSA guidebook; gaps deliberately LEFT OPEN because they are real work owed and not blocked — the gate's 2 notes). **Both decisions stay `proposed` on purpose**: promoting an idea to intent is not the same act as choosing between three architectures, and collapsing them would assert shapes Anthony never chose. What each still holds: nested-graphs holds subtree-vs-graph-per-system with federation's four hard parts and the org-scale tension (docs/org-scale-vision.md says one graph for the whole company; req:design-composes now points the other way, on the record, undecided). MOSA holds only the narrow product question of whether to ship a MOSA-BRANDED report — the requirements themselves are domain-neutral by construction, so the vocabulary stays neutral (req:design-anything) and the acquisition words live in a view. SEQUENCING: key-interfaces first, because severability is only checkable once the design knows where a boundary is. 39/39 requirements user-confirmed. Gate OK with 2 notes — @ajs — 2026-07-25 — docs/design/reflow2.json, CHANGELOG.md

- SCOPED DETECTION BUILT (cap:scoped-analysis) — Anthony picked option (a) from dec:nested-graphs. `detect_gaps`/`detect_defects` take `scope` + `depth`; four buckets that sum to the total (in_scope / out_of_scope / unanchored / project_level) so a narrowed answer can never imply the rest of the program is clean. **TWO FINDINGS FROM FAILING TESTS: (1) a scope is NOT a blast radius** — CONTAINS is deliberately not a traceability edge, which is right for impact and wrong for ownership, so a scope = containment closure (unbounded) + traceability radius (bounded); **(2) the first filter silently dropped unanchored phase gaps**, the exact drop the feature exists to prevent. RECORDED NOT FIXED: claim_region still uses the radius alone, so claiming a segment does not claim its subsystems — Anthony's call, since it changes what two people believe they hold. New cmp:scope under sys:coherence-loop with real DEPENDS_ON edges to cmp:propagate and cmp:detect. 7 tests on a satellite fixture (space/ground/control segments, laser-comms team). Also captured as BRAINSTORMING for him: dec:nested-graphs (mini-graphs per system — his idea, with the finding that levels already exist and the discriminator being ownership rather than depth) and dec:mosa-conformance (from the DoD MOSA guidebook he supplied — most of Table D-1's conformance criteria are already computed here; what is missing is designating a KEY INTERFACE, which BL-45 independently found from the other direction). 55 suites green, clippy 0, fmt clean, 97 toolsnaps, skill lint green, smoke green, merge-driver suite green, gate OK 0 notes — @ajs — 2026-07-25 — crates/reflow2-core/src/scope.rs, tests/scope.rs, crates/reflow2-mcp/src/service.rs, detect-and-ask skill + mirrors, tools/skill_lint.py, docs/design/reflow2.json

- BRAINSTORM SKILL BUILT (cap:brainstorm / dec:exploratory-staging) — his brother's rubber-ducking ask, as the KIND Anthony reframed it into: ideas land immediately as `proposed` Decisions named as open questions, options in the user's words with counter-arguments beside them, ending in a PROMOTION step not a commit. **THE FINDING: the mechanism already existed** — `undecided_decision_point` fires only on a proposed Decision with >=2 REGISTERED alternatives (detect.rs `alts.len() < 2`), so prose options are silent by construction and the nudge arrives exactly when register_alternative turns an option into a fork. No label, no schema change; dec:exploratory-staging's vocabulary question shrinks from 4 arguments to 2 small ones (retirement candidates, at-a-glance readability) and stays PROPOSED for Anthony. Guards are all prose because none is machine-checkable (an agent creating a Requirement mid-brainstorm is the failure, and the same call is correct one message later once the user says yes) — recorded honestly in the component-granularity ack, and the capability is `realized` not `verified` until a person brainstorms and disagrees with what got written down. 15 skills / 8 commands; art:brainstorm-skill registered + examined; skill lint green, gate OK 0 notes — @ajs — 2026-07-25 — getting-started/skills/brainstorm/ + mirrors, .claude/commands/brainstorm.md, tools/skill_lint.py, SKILLS.md, README.md, docs/design/reflow2.json

- MERGE DRIVER + PARALLEL-WORK SKILL BUILT (cap:git-merge-driver, cap:parallel-work) — Anthony's doubt was the finding: claiming nodes says nothing about the REPO, and the export is one JSON file git merges by lines. `reflow2-mcp --merge-driver %O %A %B` + `.gitattributes` (needs one `git config` per clone — see AGENTS.md) makes disjoint design work merge itself; a real conflict exits non-zero WITHOUT touching %A and names the ids + the --merge-apply command. **Tested by git on real branches** (tools/test_merge_driver.py, 4 cases, wired into CI's full job) rather than by another client we wrote. parallel-work skill = worktree + own graph + claim + export-before-commit + driver merge + release, and it states the limits: claims are advisory and invisible until a pull, CODE still merges as before, never resolve a design conflict with --ours/--theirs. Also this session: Anthony's word recorded on all three open questions — rule:no-foreclosure stays ADVISORY (VIOLATES_RULE has no detector, so declaring it gate-blocking would promise a check that does not exist), and both hosting requirements are ACCEPTED-and-blocked on dec:central-host with 'contribute' settled as propose-then-accept. 48 suites green, clippy 0, fmt clean, 97 toolsnaps, skill lint green, merge-driver suite green, design gate OK 0 notes — @ajs — 2026-07-25 — crates/reflow2-mcp/src/main.rs, tools/test_merge_driver.py, .gitattributes, .github/workflows/ci.yml, getting-started/skills/parallel-work/ + mirrors, AGENTS.md, COORD.md, SKILLS.md, docs/design/reflow2.json

- GITHUB-MCP STUDY + 3 IMPORTS BUILT — [docs/github-mcp-nuggets.md](docs/github-mcp-nuggets.md) (README, all 14 docs, the HTTP/auth/lockdown/sanitize/inventory code) with a ranked import list and an explicit do-NOT-import list. **Built:** `cap:sanitize-ingress` (new `sanitize.rs` + wired into INGEST's choke point; strips tag/bidi/hidden characters and REPORTS the class+count into IngestReport.warnings — GitHub sanitises silently, we cannot; ZWJ kept, no HTML stripping), `cap:bounded-reads` (scan_nodes answers with a page and names `total`/`omitted`/`next_offset`/`capped_by`, plus `brief`; closes the 96,000-character truncation that hit this session twice), `cap:tool-search` (`find_tools` over tool_router.list_all() — 97 tools is past what fits in context). **Captured, awaiting Anthony:** `rule:no-foreclosure` (6 shortcuts not to take, `enforced: false` pending his call), `req:trust-boundary-at-ingress`, `dec:concurrent-work-isolation` (his 4 worktree/websocket ideas as brainstorming), and dec:exploratory-staging REFRAMED — his brother's rubber-ducking is brainstorming ideas held AS ideas, not a staging gate. **Finding:** the honest fix for the new disconnected-cluster defect was a REAL edge (ingest DEPENDS_ON sanitize), which then made ingest an articulation point — accepted as dec:sanitize-spof-accepted, dissolved by the import path's second caller, not by fake redundancy. 48 suites green, clippy 0, fmt clean, 97 toolsnaps, skill lint green, smoke green, design gate OK (2 notes = the 2 questions owed) — @ajs — 2026-07-25 — crates/reflow2-core/src/sanitize.rs, ingest.rs, crates/reflow2-mcp/src/service.rs, tests/sanitize.rs, tests/tools.rs, docs/github-mcp-nuggets.md, where-am-i skill + mirrors, tools/skill_lint.py, docs/design/reflow2.json

- KPP CAPTURE HALF BUILT (cap:kpp-proposal / BL-96) — the **kpp-proposal** skill + `/kpp`: the agent notices the shape of inviolable intent (unit-bearing threshold, necessity language, a consequence the user called fatal) and asks the CONSEQUENCE question, never the label — and never promotes on its own, because criticality is a claim about mission/contract/money the graph does not hold. FOUND WHILE BUILDING: `add_constraint` had no `objective` parameter and never named the `kpp` category, so a confirmed KPP could only be recorded via generic `create_node` (which is how the violation half's own 9 fixtures are built) — a skill without that fix would have pointed at a door that was not there. 3 new tests incl. the one that matters: the objective is never read as the threshold. Also SKILLS.md rewritten as the skill/command catalogue (linked from README + kit) and stale counts refreshed (11→13 skills, 27/54→28/55 types). 47 suites green, clippy 0, fmt clean, 96 toolsnaps blessed (add_constraint), skill lint green, design gate OK — @ajs — 2026-07-25 — getting-started/skills/kpp-proposal/ + mirrors, .claude/commands/kpp.md, budget.rs, service.rs, tests/kpp.rs, tools/skill_lint.py, SKILLS.md, docs/design/reflow2.json

- CLAIMS LAYER BUILT (cap:cluster-claims / BL-44) — the scoping primitive dec:multi-writer-architecture committed to, and the thing that finally satisfies req:concurrent-planning. `claim_region`/`release_claim`/`claim_report` + a CLAIMS edge (Contributor -> any node, depth/claimed_at/note), new `cmp:claims` under sys:coherence-loop, art:claims registered. **A claim is NOT a lock** — no server exists to hold one, so nothing refuses a write and a second writer still merges correctly; pinned by a test, not a comment. Region COMPUTED from seed+depth (follows the design, does not freeze). Overlaps REPORTED not prevented, ranked by size, same-person overlaps excluded, and the advisory limit ships in the payload. CLAIMS deliberately NOT a traceability edge (like AUTHORED_BY) so people never appear in blast radii — also pinned. 9 tests; 47 suites green, clippy 0, fmt clean, 96 toolsnaps match. Schema: edge types 54 -> 55 — @ajs — 2026-07-25 — crates/reflow2-core/src/claims.rs, tests/claims.rs, crates/reflow2-mcp/src/service.rs, docs/design/reflow2.json

- DIRECTIONAL DRIFT BUILT (cap:drift-direction, from Anthony's storyflow field observation that "docs consistently understate what's built" — the founding problem) — `reconcile_artifacts` takes an optional per-observation `realizes` and compares it to the recorded REALIZES edges: **understated / overstated / diverged**, plus `unrecorded`/`unbuilt` naming WHAT the design has wrong. Direction judged independently of the checksum (a design can be wrong from day one); overstatement outranks understatement (a record that is wrong beats one that is behind); `realizes: None` = not assessed, never read as agreement; NO schema change (mapped onto existing undocumented_addition / spec_mismatch, BL-94 lesson). 7 new tests in tests/drift_direction.rs incl. the two that would fail a naive impl (a typo fix must NOT report understatement; an unchanged file still can). 45 test binaries green, clippy 0, fmt clean. art:drift reconciled design_updated via chg:drift-direction-built. NOT built: piece 3, write-back to the source documents — the graph can be brought up to date, the markdown people read cannot — @ajs — 2026-07-25 — crates/reflow2-core/src/drift.rs, crates/reflow2-core/tests/drift_direction.rs, docs/design/reflow2.json

- BL-104 BUILT — `delivery_coverage()` in report.rs: what is DELIVERED is now derived from the golden thread (satisfied AND the satisfying capability realized AND currently passing), never read from `Requirement.status`. Two properties pinned by tests: it goes BACKWARDS when a check starts failing (a derivation that only ratchets up is a stored claim with extra steps), and `inferred`-provenance requirements are excluded and counted apart, or a brownfield adopt would report itself fully delivered on arrival. Dropped requirements leave the denominator. Surfaced on graph_report + the Markdown view. 8 new tests in tests/delivery.rs, all 5 negative cases; 40 test binaries green, clippy 0, fmt clean. art:report reconciled design_updated via chg:bl104-built; cap:derived-completion verified by ver:derived-completion. **FIRST REAL ANSWER: 20 of 30 requirements delivered, 29 satisfied** — unanswerable an hour earlier, when zero carried `met`. Still to do from scope: SUPERSEDED vs DROPPED vs EVOLVED, and MET-BUT-REGRESSED as a TemporalFact — @ajs — 2026-07-24 — crates/reflow2-core/src/report.rs, crates/reflow2-core/tests/delivery.rs, docs/design/reflow2.json

- BL-104 CAPTURED **graph-first** — derive `met` from the golden thread: delivery is computed from evidence (satisfied AND the satisfying capability realized/verified with a passing check), never a hand-set field. `req:completion-computed` (proposed) + `cap:derived-completion` (planned, cmp:report, governed by dec:certainty-derived + dec:passing-is-verified), `chg:bl104-derived-completion` pinned. Measured: 0 of 28 requirements carry `met` though several are plainly shipped; 3 of 5 status values never used; EVOLVES_INTO/OBSOLETES/TemporalFact/Snapshot all at zero. **The backlog row is a PARITY CHECK ONLY, not the record** — per Anthony, it exists to compare against the graph until backlog items are demonstrably first-class there; if they disagree, the graph is right. Gate: 1 finding (the deliberate failing search check), 2 notes; live==committed 399n/1168e — @ajs — 2026-07-24 — docs/design/reflow2.json, docs/backlog.md

- Foundation pin bump to **v0.11.0** — the ranked-disjunction full-text search fix (dynograph-foundation #76/#77, released 2026-07-24). Search matched tokens as a conjunction, so one word the corpus never used collapsed a perfect match to zero hits — which made a natural-language question unusable and, because capture-intent treats no-hits as licence to create, manufactured duplicates. 5 pins in Cargo.toml + AGENTS.md prose; Cargo.lock resolves dynograph-text 0.11.0 at the tagged merge commit 1fbd81c. 213 workspace tests green (incl. all 6 reflow2 search tests, which assume ranked order and a revised node NOT being resurrected by dropped wording). ver:search-natural-language stays FAILING until an automated test proves it against the real graph — @ajs — 2026-07-24 — Cargo.toml, Cargo.lock, AGENTS.md

- BL-96..BL-103 FILED from Anthony + his brother's idea list (`~/Documents/reflow2_idea.txt`, 2026-07-24) — KPPs as first-class inviolable intent (BL-96, Anthony's call that KPP is its own vocabulary; `req:inviolable-intent` + `cap:kpp` captured, requirement left `proposed`), folder/bulk spec ingest (BL-97), 15-developer conflict identification (BL-98), real-time collaboration (BL-99), rubber-duck mode (BL-100), inherited-project onboarding (BL-101), README/architecture/use-cases/diagrams (BL-102), LangChain/LangSmith prior-art study (BL-103). Two findings recorded in the entries: the inviolable-intent vocabulary already exists in the schema and is 100% unused (0 Constraints / 0 DesignRules / 0 QualityGates against 22 Requirements — same shape as the BL-70 temporal finding), and BL-98 is the first field-sourced trigger to re-open `dec:repo-file-embedded`, whose rationale said concurrency was "hypothetical while there is one writer". Gate green, live==committed 374n/1115e — @ajs — 2026-07-24 — docs/backlog.md, docs/design/reflow2.json

- BL-95 SPEC'D + CAPTURED (no code) — the design cannot see what it was never told about: all 26 gap sources take an existing node as subject, none takes a file on disk, so a graph covering 30% of a codebase reports the same "0 open gaps" as one covering 100%. From Anthony's storyflow adopt pass. Shape: `coverage_report(observed_paths, exclusions)` in drift.rs, sibling of reconcile_artifacts, agent-supplied sweep (reflow2 does no file I/O), measuring CLAIMED REGIONS ranked by mass — never a file-count ratio, which would punish the coarse modelling adopt mandates. `req:coverage-visible` (left `proposed` — needs Anthony's read of the statement before it moves) + `cap:coverage` (planned, allocated to cmp:drift). `chg:bl95-coverage-capture` pinned to a fresh epoch. Gate green, live==committed 370n/1108e — @ajs — 2026-07-24 — docs/backlog.md, docs/design/reflow2.json

- BL-70 fork layer DESIGNED (graph + docs only, no Rust) — three decisions on the record: fork point = the coordinate Decision → epoch → export `content_hash` resolved against git (no native ref/DAG layer); re-opening a settled choice mints a superseding Decision rather than un-accepting the original; the epoch chain is backfilled from real release tags only. Epoch spine built (12 epochs, `PRECEDES`-chained, checksums where the export carries one), all 34 Decisions + 9 Releases anchored to the epoch the git evidence puts them in. `merge.rs`/`alternatives.rs` finally modelled (`cmp:merge`/`cmp:alternatives` under Time & History, capabilities moved off the `cmp:compare` stand-in, both registered as checksummed artifacts) — this dissolved the disconnected-community defect, 6 → 5 (the 5 accepted SPOFs). Found: the temporal axis was nearly unused (0 Snapshots, no Decision anchored, chain 4 days stale) because `add_change_event` was used where `record_change` was meant. Still to build: read-only `fork_point(decision_id)`, then `cap:revise-trigger`'s detector. Gate green, live==committed 365n/1100e — @ajs — 2026-07-24 — docs/design/reflow2.json, docs/backlog.md, CHANGELOG.md

- BL-80 #3 DONE — file-pure `--merge-apply` CLI, the apply half of the git-file merge workflow. `--merge` proposes (conflicts + ids); `--merge-apply base ours theirs --resolutions FILE` runs `resolve_merge` and prints the merged export document, opening no graph and refusing (non-zero, no output) until every conflict is decided. Reuses the tested core; `main.rs` adapter only. 4 CLI tests + end-to-end propose→decide→apply→`--diff`-identical. art:main design_updated (`chg:bl80-merge-apply-cli`), cap:merge-designs surface refreshed. workspace tests + clippy + fmt + gate green; live==committed 346n/1017e. Still open on BL-80: node-type/delete-modify rerere keys + rerere memory aging — @ajs — 2026-07-24 (ed37bc9) — crates/reflow2-mcp/src/main.rs, docs/design/reflow2.json, CHANGELOG.md, docs/backlog.md

- BL-86 DONE — set-based schema stamp (the real fix). `GraphStamp` records `node_type_names`/`edge_type_names` (serde-default, so legacy count-only stamps still parse); a `RETIRED_{NODE,EDGE}_TYPES` registry lets the upgrade refusal name the offending types and split retired (→ migrate the graph) from unknown (→ update reflow2) instead of the count-only hedge; legacy count stamps get a sharpened message. Fixes the case v0.10.0 made reachable (a pre-`VALIDATES`/`ENABLES` graph refused with "update your binary"). `provenance.rs` only; `art:provenance` reconciled design_updated (`chg:bl86`). 7 new tests; full core suite + clippy + fmt green, gate green, live==committed 344n/973e — @ajs — 2026-07-24 — crates/reflow2-core/src/provenance.rs + tests/provenance.rs, docs/design/reflow2.json, CHANGELOG.md, docs/backlog.md

- BL-91 DONE — read-side `loop_hint` (dec:read-hint-shape option C): the orientation reads (graph_report/graph_report_markdown/scan_nodes/search_design/get_node) attach a loop-debt pointer **only** when loop_status shows real debt and **only** when the owed-set changed since last surfaced (fire-on-change). Cost bounded by a service write-generation counter (owed-set moves only on a write; within a generation the first read computes loop_status once, later reads add nothing). Modeled cap:read-loop-hint (SATISFIES req:read-surfaces-debt, ALLOCATED_TO cmp:service, REALIZED art:service, VERIFIED ver:read-loop-hint) — closes the last open gap + dissolves the read-hint disconnected community; re-acknowledged the unvalidated-caps gap (now 33, validation via trials). **Caught+fixed** a brittle `{count,items}` exact-set unwrap in reflow2_check/reflow2_cli/smoke_mcp (now presence-match). `chg:bl91`; art:service (design_updated) + art:check (design_holds) reconciled. workspace tests + clippy + fmt + toolsnap(91) + skill_lint + gate + smoke all green; live==committed 340n/927e — @ajs — 2026-07-24 — crates/reflow2-mcp/src/service.rs + tests/tools.rs, tools/{reflow2_check,reflow2_cli,smoke_mcp}.py, docs/design/reflow2.json, CHANGELOG.md, docs/backlog.md

- BL-88 DONE — hermetic suites for the CI gate + view renderer, wired into ci.yml. `test_reflow2_check.py` (full job, real binary) pins the gate's exit-code contract (trio + both drift shapes + no_baseline); `test_render_views.py` (core job, pure-Python) pins the projection/confession doctrine. **Caught + fixed a real gate bug**: gate matched reconcile kind `"missing"` but reconcile emits `missing_artifact` (severity high) → a vanished registered artifact was a silent note, not a red build. Modeled render_views.py (art:render-views→cap:report) + registered both suites as passing Verifications (ver:gate-suite/ver:views-suite). `chg:bl88`, art:check reconciled. 13 new cases; gate + skill_lint + YAML valid; live==committed 335n/885e — @ajs — 2026-07-24 — tools/{test_reflow2_check,test_render_views}.py (new), tools/reflow2_check.py (fix), .github/workflows/ci.yml, docs/design/reflow2.json, CHANGELOG.md, docs/backlog.md

- BL-89 DONE — three adopt-scale tweaks: (B.1) adopt-skill granularity keys off contracts/capabilities not LOC (kit + .claude + .grok copies synced); (B.3) `describe_schema {required_only}` compact mode (new `describe_node_type_required` in core, toolsnap +1 param); (E.1) `unreleased_component` expands shipped set down CONTAINS so a Release including a subsystem covers its modules. 2 new tests; `chg:bl89`, 4 artifacts reconciled (art:detect/vocabulary/service/adopt-skill). 43 core+mcp suites + clippy + fmt + toolsnap + skill_lint green; gate green, live==committed 330n/878e — @ajs — 2026-07-23 — crates/reflow2-core/src/{detect,vocabulary}.rs, crates/reflow2-mcp/src/service.rs, adopt SKILL.md (×3), tools/toolsnaps/describe_schema.json, tests, docs

- BL-87 DONE — `import_graph` accepts an unstamped document (option b, Anthony's call): `GraphExport.stamp` → `Option<GraphStamp>` (sibling of content_hash), a stampless doc imports and `ImportReport.provenance_note` reports it (loud, not silent). import never gated on the stamp — it was pure friction. compare/merge read a new `reflow2_version()` accessor. Input schema unchanged (no toolsnap churn). `chg:bl87`, art:export/art:compare reconciled. 43 core+mcp suites + clippy + fmt green; gate green, live==committed 329n/870e — @ajs — 2026-07-23 — crates/reflow2-core/src/{export,compare,merge}.rs + tests/export.rs, docs/design/reflow2.json, CHANGELOG.md, docs/backlog.md

- BL-92 DONE — the critical detect↔verify cycle broken by relocating `fnv1a` from detect.rs to nodes.rs (the vocabulary/identity leaf; the homeless id-hash that manufactured the cycle — verify's only tie to detect). `dec:fnv1a-foundational` (break-over-accept), `chg:cycle-break`. Removed 6 fnv1a-only false `→cmp:detect` edges (agent/artifact/drift/fielded/heal/verify), added `cmp:agent→cmp:nodes`; report keeps its real GapCandidate dep. Verified on real self-model (`--analyse-only`): detect_defects zero critical, 7 warnings. Also reconciled the 9 drifted artifacts (BL-84's structure/heal edits had shipped drift — `chg:bl84`) so the gate is green again. Full core suite + clippy + fmt clean; live==committed 328n/866e — @ajs — 2026-07-23 — crates/reflow2-core/src/{nodes,detect,verify,agent,artifact,drift,fielded,heal,structure}.rs, docs/design/reflow2.json, CHANGELOG.md, docs/backlog.md

- BL-84 DONE — detector honesty, both halves. `disconnected_community` now skips a decomposition scaffold (an island reachable from the body through `CONTAINS`), the cluster-level twin of `dead_end`'s assembly exemption — the 8-node subsystem island from BL-83a cleared on the real self-model (detect_defects 9→8), genuine clusters (fork-alternatives, parked BL-91 intent) still fire. SPOF: `couples_only_as_a_library` also spares `data` medium; new `interface_is_foundation` spares a `library`/`data` Interface hub (`is_foundation_medium` the shared predicate). 3 new tests in tests/structural.rs pin both directions; the two live SPOF interfaces are REST so accepted SPOFs untouched. clippy+fmt clean, full core suite green — @ajs — 2026-07-23 — crates/reflow2-core/src/{heal,structure}.rs, tests/structural.rs, CHANGELOG.md, docs/backlog.md

- BL-90 DONE — loop_nudge's total-bypass backstop built to the fix shape: a second `PostToolUse` matcher (`Edit|Write|MultiEdit|NotebookEdit`) counts file edits, and the Stop hook blocks ONCE when a session edited ≥`REFLOW2_LOOP_NUDGE_EDIT_THRESHOLD` (default 3) files with zero reflow2 calls — any single reflow2 call (even a read) disarms it; never reads the graph; the two backstops are mutually exclusive. Closes `req:nudge-covers-bypass` via SATISFIES from `cap:loop-status` (which `art:loop-nudge` already realizes); `ver:loop-nudge` grew 6 cases (15 pass); `chg:bl90` on the record, artifact checksum re-baselined (design_updated). Export 323n/852e, live==committed, detect_gaps now 0 — @ajs — 2026-07-23 — tools/loop_nudge.py, tools/test_loop_nudge.py, .claude/settings.json, docs/design/reflow2.json, docs/backlog.md, CHANGELOG.md

- BL-90 raised + captured through capture-intent (req:nudge-covers-bypass, ACCEPTED by user same day, authored_by who:ajs): loop_nudge total-bypass blind spot — a session that edits code with ZERO reflow2 calls is never nudged, exactly the agent the trigger exists for; fix shape = second PostToolUse matcher on Edit|Write, Stop names the debt once, edit-count threshold vs false positives, nudge-not-wall vs ritual compliance. The one survivor of a user-supplied "force the agent to use tools" review; rejected on the record: MUST-language (trigger-not-virtue), tool_choice forcing (host-side knob MCP can't reach), aggressive descriptions across ~90 tools, transcript few-shots (skills are that layer). Requirement deliberately left unsatisfied — gap 5d199aa [0.60] IS the honest state until built. ALSO live-store catch-up: art:graph/art:provenance checksums had been reconciled only in the committed export (a28d0a4, that session's server was down) — accepted design_holds in the live graph (chg:accept-f74ab635/a2a03587), live==committed again. Export 318n/814e, chain advanced, reflow2_check OK. FOLLOW-UP same day: req accepted (d93ae39); user acknowledged the 4 pre-existing gaps on the record (fork-alternatives trio → "tracked under BL-70/BL-80", 32-caps-unvalidated → "validation happens through the trials program"; 4 Decision nodes via acknowledge_gap) — open list now = BL-90's deliberate gap only; export 322n/849e — @ajs — 2026-07-23 — docs/backlog.md, docs/design/reflow2.json

- BL-83 DONE — all three moves (a functional re-derivation / b adopt-on-a-copy / c compare). (b) ran `adopt` against a stripped copy at /home/ajs7/project/reflow2-bl83b (code+schema+kit, design record removed): recovered 143n/238e, no "The X module." vacuity, caught its own over-claim (status_contradiction), 7 gaps left correctly open. (c) `--diff` measured the finding: 117 added / 186 removed / 18 changed, 1 shared Component of ~42, capability count converged 33=33, the "why" layer (ChangeEvents/DriftEvents/Questions/Contributors/Epochs) absent in the recovered graph — adopt re-derives modules, the functions are a design layer the code doesn't carry (they relocate to Flow/Interface). Reproduced independently. Trial: docs/trials/2026-07-23-bl83b-adopt-dogfood.md. Dogfood surfaced + THIS SESSION FIXED two real bugs (stamp ordering f4109cb; refusal message dc9bf96/BL-86) and raised BL-84/87/88/89. Confabulation caution recorded (adopt invented a false cause for a real failing test) — @ajs — 2026-07-23 — docs/backlog.md, docs/trials/, crates/reflow2-core (fixes)

- BL-83 move (a) DONE (dec:bl83a-functional-decomposition): reflow2's self-model re-derived by FUNCTION, not file tree — genesis's own question run on reflow2 WITH the user. Seven functional subsystems (Vocabulary/Store/Coherence-Loop/Human-Channel/Time-History/Intake/Agent-Surface, level=subsystem under proj:reflow2 the system); 35 Rust modules re-parented under them; the 3 as-built crate components (cmp:core/mcp/kit) retired on record (recovered by move b's adopt, compared by move c). Recorded durably in tools/build_design_graph.py (SYSTEMS + SYSTEM_OF + retirement + dec:bl83a governed_by the 7 systems), not hand-edited; self-model 309→315 nodes, hierarchy 0. Measured against baseline: gaps unchanged (4→4, all pre-existing — cap:fork-alternatives unmodeled + orthogonality's unvalidated_capability); defects +2, both from 3a — ifc:core-api SPOF accepted (dec:core-api-spof-accepted, sole Store↔Surface bridge) and an 8-node "subsystem island" disconnected_community that is a BL-69-family DETECTOR FALSE POSITIVE → raised BL-84 (community/SPOF must skip pure-decomposition nodes). PREREQ done this session: the live 55-edge graph was migrated to the current 53-edge schema (rebuilt binary refused it — BL-19 working as designed) by re-importing the committed export; stale graph archived at .reflow2/graph.stale-55edge-*. live==committed (byte-identical). Remaining: BL-83 (b) adopt-on-a-copy, (c) compare_designs — @ajs — 2026-07-23 — tools/build_design_graph.py, docs/design/reflow2.json, docs/backlog.md

- BL-79 rung 1 done (dec:design-authorship-identity): the identity keystone, user-chosen as next-most-important. New `Contributor` node type (person/automated_agent/organization), separate from Actor (who-serves vs who-authors); `AUTHORED_BY` edge = the structured "who" behind provenance's "how", deliberately NOT in nodes.rs structural_rule so authorship never widens a blast radius (smoke asserts it). SCHEMA CHANGE: 28 node / 55 edge types — additive, so a new-binary graph is refused loudly by a pre-Contributor binary (count-based provenance, BL-19); no schema_version bump. core.yaml + nodes.rs constants + add_contributor/authored_by (core constructors + MCP tools, readOnlyHint=false → 82 tools, 2 new toolsnaps); counts fixed in schema.rs/vocabulary.rs/tools.rs/smoke; capture-intent skill records the driver once/session + attributes-when-captured (3 mirrors). Remaining rungs (each needs user vocab): CLAIMS (BL-44), ACTS_FOR = git's author/committer split, alt-authorship (BL-70), sign the content_hash (BL-41). All gates green (core+38 mcp tests, smoke, toolsnap, clippy, fmt, skill_lint, validate_schema). NOTE: session's MCP server predates the rebuild (BL-32), so the re-export stamp reads 27/54 cosmetically — self-corrects on next self-model build with a current binary; content is correct (291 nodes, +dec). Also captured this session: BL-78 (dependency freshness) and BL-76 (readOnlyHint+toolsnaps). git-vs-reflow2 synthesis pending capture — @ajs — 2026-07-22 — (this commit)

- BL-76 done (both rungs): all 80 MCP tools declare `annotations(read_only_hint = …)` on the `#[tool]` attr — classification derived from the graph borrow itself (`let g` = read → true, `let mut g` = write → false), so gap_to_prompt + the reconcile_* family are correctly false (they record); 26 read / 54 write. smoke_mcp gained the explicitness gate (every served tool must carry readOnlyHint — a new tool can't ship unclassified) plus a both-poles correctness spot-check. New tools/toolsnap.py freezes each tool's served schema as a committed golden (tools/toolsnaps/, 80 files), CI-diffs them, `--update` to bless; wired into the full CI job + AGENTS.md done-gates + mcp-crate "Adding a tool". Surface half of BL-12; the read-only transaction mode stays BL-12. Also: req:frictionless-update confirmed proposed→accepted (all 18 reqs now user-confirmed) — export re-run, live==committed, chain advanced, gaps 0. smoke/fmt/clippy/skill_lint green — @ajs — 2026-07-22 — (this commit)

- github-mcp-server comparison captured (user-directed read of the clone at ~/project/github-mcp-server): BL-12 gains the github notes block (hosted shape validated — one library, stateless streamable-HTTP front, their remote IS the OSS repo; read-only = per-tool ReadOnlyHint + filter, with the two-layer caveat that reflow2 also needs the RocksDB-secondary open; scope-filtered tools = sketch-2 in production; lockdown mode = BL-41-M's authority-keyed precedent). BL-76 raised (S+S, next up: ReadOnlyHint on all ~80 tools + explicitness gate in smoke, and toolsnaps — golden per-tool schema snapshots, the BL-28/32/48 bug family made a tripwire). BL-77 raised, parked (toolsets/verb-multiplexing when the 80-tool surface pinches; their removal of dynamic discovery is a data point). BL-51 addendum (server.json registry manifest, Docker). Analysis by scout agent over the clone; nothing built — @ajs — 2026-07-22 — (this commit)

- BL-75 done, closing the fleet-trial trio (dec:certainty-derived): requirement certainty DERIVED from status × provenance, never a stored third axis. accepted/met → user-confirmed; proposed+inferred → recovered; proposed+authored → asserted; deferred/dropped → settled out. Doctrine: every move off proposed records the USER's word (BL-12 sketch-2's example made real, cultural for now, mechanical stays BL-41) — stated in set_requirement_status's description ("promoting it yourself forges their signature"), capture-intent, adopt; where-am-i reads the new "Requirement certainty" snapshot line instead of caveating in prose (3 mirrors × 3 skills). requirement_certainty + breakdown in report.rs, GraphReport += requirement_certainty, snapshot renders it. skill_lint += accepted/met/proposed. 2 new report tests (matrix incl. confirmed-recovered + no-requirements silence); 41 suites green, smoke green. No schema change — second read-side-blind item this week. Drift accepted two-sided art:report/service/adopt-skill (chg:bl75-certainty-derived). Export 290n/752e, gaps 0, chain advanced, live==committed — @ajs — 2026-07-22 — (this commit)

- BL-73 done (dec:component-verified-computed): component-granularity verification as a computed third state. capability_verification (verify.rs) → verified / component_verified / unchecked; coverage line gains the clause ("N more at component granularity"); the N per-cap unverified alarms fold into ONE component_granularity_verification gap per carrying component at 0.35 (riding caps listed, acknowledgeable once); status_contradiction accepts component proof; loop_status counts it proven; failing suite carries nothing. Write side needed zero changes (VERIFIES→* all along). Adopt skill teaches suite-on-the-Component (3 mirrors). 7-case suite (component_verified.rs) replays the fleet trial's shape; 41 workspace suites green; smoke green. Self-derived DEPENDS_ON caught the new detect→verify + report→verify calls — the design tracked the code change by itself. Drift accepted two-sided on art:verify/detect/report/adopt-skill (chg:bl73-component-verified). Export 285n/742e, gaps 0, chain advanced, live==committed — @ajs — 2026-07-22 — (this commit)

- BL-74 rung a done, CLOSING BL-74: tools/loop_nudge.py — the trigger, wired to the harness's events. One stdlib script, three hooks: SessionStart orientation, PostToolUse counts reflow2 graph writes per session (loop_status/detect_gaps/detect_defects resets), Stop blocks ONCE with what to run when writes finish unchecked (stop_hook_active honoured — never a hostage). Never reads the graph (lock constraint): the hook counts events, loop_status answers what's owed. 9-case hermetic suite in CI; ships in the kit tarball; snippet in kit AGENTS.md step 0a; REFLOW2.md self-host mirror refreshed (kit stamp 0.6.1→0.8.0). Graph: art:loop-nudge REALIZES cap:loop-status, ver:loop-nudge passing (chg:bl74a-loop-nudge). Export 278n/687e, gaps 0, chain advanced, live==committed. The BL-74 ladder is complete (c: loop_status, b: hints, a: trigger); the remaining question is field evidence — arm the hook in the fleet and let the trial log answer whether the loop stays alive under load — @ajs — 2026-07-22 — (this commit)

- BL-74 rungs c+b done (dec:loop-status-state-not-history): loop_status tool — the loop's outstanding debt as one cheap call, computed from STATE never run history (no clock, looking-is-not-writing; phase nudges excluded as guidance-not-debt); unsurfaced anchored gaps / waiting + unwritten questions / defects / unproven realized caps / undispositioned drift / unexamined claims, each with a next-step line. Five write tools (add_requirement/capability/component/interface, link_artifact) carry a static loop_hint in their results — the pointer rides what the agent already reads. Skills: capture-intent step 6 + detect-and-ask pulse-check (3 mirrors); skill_lint += next. 3 core tests + tool test + 2 smoke checks; 40 workspace suites green. BONUS FIX: build_design_graph now exports through the tool's file seam — the first hashed rebuild came out chain-rootless because the script hand-wrote the payload; chain repaired to this morning's hash and verified held on re-run. Self-model 274n/682e, gaps 0, live==committed identical, reflow2_check OK. Rung a (kit hook firing loop_status on client events) stays open with its design constraint recorded (a hook can't reach the locked live graph) — @ajs — 2026-07-21 — (this commit)

- First extensive field feedback triaged (StoryFlow fleet improvement log — @ajs's own multi-agent fleet on another machine, CORRECTED 2026-07-22: earlier wording here misattributed it to @bro): saved verbatim to docs/trials-private/2026-07-21-storyflow-fleet-improvement-log.md (private — names the fleet). Raised BL-73 (component-granularity verification: 20 false unverified gaps, 21 acknowledges to close honestly; report understates a tested system), BL-74 (ADOPTION-CRITICAL: loop skills dropped under operational load — triggers beat exhortation; ladder is loop_status core op → nudges in tool results → kit hook recipe), BL-75 (requirement confirmation state). BL-12 updated: the lock cost is now measured (workers can't even read while the Boss holds it) — read-only secondaries promoted to cheapest high-value increment; their "clear lock error" ask is already built (BL-57) but verify it surfaces through MCP-spawned stderr. Positives kept as-is: scan Decision as the highest-value read, reviewed_gaps auditability, the one genuine unmotivated-capability catch — @ajs — 2026-07-21 — (this commit)

- AT-proto capture + export hash-chain done (user-directed): backlog gains the AT Protocol design-notes block under BL-12 (identity-decoupled hosting, labels-as-overlay, per-writer-repos+merge shape) and BL-72 (namespaced schema packs); the hash-chain built same day (dec:export-hash-chain). GraphExport += content_hash (sha256 over canonical content JSON, stamp excluded — same design fingerprints the same across builds) + prev_content_hash (file lineage at the export-tool write seam; chain advances only on content change so unchanged exports stay byte-identical). compare_designs += ancestry (succeeds/siblings/unknown) + tamper callout in provenance_note; import reports integrity mismatch loudly; reflow2_check FAILS on a doctored committed export (negative-tested: doctored exit 1, clean exit 0). Cross-language canonical hash pinned Rust↔stdlib-Python in smoke. 5 new export tests + 4 compare tests + chain tool test + 3 smoke checks; all 40 workspace suites green. Drift on art:export/compare/service/main/check accepted two-sided (chg:atproto-hash-chain). Export 268n/670e, hashed, chain rooted; gaps 0. Per-claim anchoring (accepts carrying the state hash they saw) stays open under BL-12 — @ajs — 2026-07-21 — (this commit)

- v0.8.0 cut (user-directed): compare_designs + --diff on the surface, no schema change, no upgrade doc. Release layer modelled: rel:v080 deployed active (first manifest carrying compare.rs — the unreleased_component gap closed itself), rel:v070 retired rolled_back. Self-model 261n/654e, gaps 0, live==committed verified by --diff (identical; provenance note correctly names the two writers: 0.8.0 script vs 0.7.0-era session server), reflow2_check OK 0 notes. Release run watched to completion under draft-then-publish — @ajs — 2026-07-21 — (this commit)

- BL-71 rung c done, CLOSING BL-71: the design-vs-design diff. User decided all four axes in the vocabulary session (dec:design-diff-vocabulary): core op (compare.rs) + compare_designs MCP tool + `--diff BASE [OTHER]` CLI; operands two export docs OR live-vs-doc; directional added/removed/changed vs a named base, property-level, absent≠present; banded design content vs supporting layer. "Divergence" not "drift" — drift stays design-vs-reality. 8 core tests + tool test + 5 smoke checks (incl. CLI diffing two files WHILE the server holds the lock — two-file mode never opens the graph); skill_lint += base_path; where-am-i skill opens with it (3 mirrors). build_design_graph: modules absent at a tag are now loudly NOT in that release's manifest (truth, not refusal). Self-model 260n/616e, live==committed verified BY the new tool (identical 260/616); drift on art:report/service/main accepted two-sided (chg:bl71c-compare-designs); reflow2_check OK with the one honest note: cmp:compare ships in nothing until the next cut (0.8.0 — new tool = minor). Session opener housekeeping: live graph caught up to the committed export by delta import (the 0.7.0-release layer), obsolete memory cleared. BL-70 branch comparison and BL-12 merge now have their read primitive — @ajs — 2026-07-21 — (this commit)

- BL-71 rungs a+b done: build_design_graph now imports the committed export FIRST and layers the curated pass onto it (upsert; curated wins on shared ids so deliberate updates take, session layer survives; genesis skipped when a prior export exists), and refuses to write a shrinking export (the silent-loss signature, named as BL-71 in the refusal). Union export 250n/595e — full session layer + 5 Release nodes (v0.7.0 active, v0.4.0–v0.6.1 retired); second run idempotent at 250; gaps 0; reconcile clean; reflow2_check passes. Interim do-not-run rule retired. Rung c (design-vs-design diff) stays open, wants the user. Live .reflow2 picks the releases up on next --import — @ajs — 2026-07-21 — (this commit)

- BL-66 done: consumer CI coherence gate. tools/reflow2_check.py (stdlib, self-contained, in the kit tarball via release.yml) reads the COMMITTED export — CI can't open gitignored .reflow2/, and the export is what the team reviewed — rehashes registered artifacts (sha256 truncated to each registration's own length), reconciles, runs detect_gaps. Exit 1 on unaccepted checksum_change/missing or open anchored gap ≥ 0.8 (--gap-threshold); acknowledge_gap = the sanctioned green, no drift-skip flag exists. Exit 2 loud on can't-run. Three-way verified on reflow2 itself (pass / doctored-file fail / missing-export refuse). ci-gate skill (CI snippet + red-to-green playbook naming the two launderings) + SETUP.md pointer; skill_lint NON_TOOL_TERMS += "path". Graph: art:check REALIZES cap:reconcile-built, accepted two-sided (chg:bl66-ci-gate). Export 247n/486e — @ajs — 2026-07-21 — (this commit)

- BL-63 done: snapshots capture design edges (new optional Snapshot.edges beside state; sorted; bookkeeping neighbours excluded so a snapshot never accumulates its own history) — a lazy reallocation now leaves "A once owned Z" recoverable from the snapshot alone, no hand-authored Decision needed. parse_snapshot_edges/SnapshotEdge in core; pre-BL-63 snapshots read as empty capture, not error. Deviation from the entry's lean, reasoned in backlog: full-capture-with-exclusions, not changed-edges scope. revise-design's "leave a formerly-true edge" workaround replaced with record-first-then-delete; both CRUD skills updated (3 mirrors, kit refreshed to 0.6.1). Schema +1 optional prop → next cut is 0.7.0. Gates: workspace green, clippy/fmt/schema/skill-lint/test_init clean, all instruments at baselines incl. coherent 9/9 (its snapshot reader unaffected — state shape unchanged). Drift accepted two-sided (chg:bl63-snapshot-edges). NOTE: a session started before this build snapshots without edges — restart before relying on it live — @ajs — 2026-07-21 — (this commit)

- BL-69 done: SPOF connectivity moved to the as-built operational network (ops types + realizing artifacts) — the design network's intent edges donated mass (cmp:flow fired on stranding its own capability/artifact/verification cluster) and phantom connectivity (cmp:export, a true cut vertex, hid behind a SATISFIES chain). Candidates enumerate from the same network; all prior selectivity lessons kept (baseline-relative, non-trivial, operational-candidates, library filter). Self-graph measured before→after: {flow, service} → {graph, export, ifc:graph-export, service} — the false positive out, three true findings in (service already accepted via dec:service-spof-accepted; other three await disposition). 2 regression tests + island fixture rebuilt operational; workspace + all instruments at baselines; drift accepted two-sided (chg:bl69-spof-cut-vertices; plus art:artifact's missed BL-26 accept recorded late via chg:bl26-documents-write-side). Export 242n/480e. NOTE: a session started before this build serves the old detector — restart before relying on it live — @ajs — 2026-07-21 — (this commit)

- Design-analysis capture BL-64..68 (concept items, no code): from a session comparing reflow2 against UAF's full acquisition lifecycle and modern commercial practice. BL-64 disposal/retirement (missing P6), BL-65 risk/security as a lifecycle-spanning concern + DevSecOps continuous governance, BL-66 design-coherence as a consumer CI gate (actionable S-M), BL-67 SLO/telemetry reconciliation (as-operating fidelity), BL-68 readiness-driven roadmapping (KEYSTONE — TRL/MRL/-ilities gate achievability; derive the delivery timeline from readiness × the thread; "the roadmap is a risk-burndown schedule"; unifies 64/65/67). All reuse propagate + detect-and-ask + the reconcile seam; the new part in each is vocabulary, a user design decision. Nothing built — @ajs — 2026-07-21 — (this commit)

- BL-58 DONE (all 12 items, for 0.6.1) + versioning policy written into CHANGELOG. Fixes: ingest upsert (BL-46 on ingest path), snapshot BTreeMap determinism, propagate_change existence check, apply_heal one atomic batch (merge_nodes made batch-free — it was the only caller), swallowed edge errors surfaced (detect/ingest/fielded), budget non-finite rejection + provable-overrun + total_cmp, i64::MAX widen guard, truncated_beyond_depth honest doc, drift dangling-edge skip, per-edge gap ids for missing-intermediate, ingest fragment_id reuse refused, node_type_index sorted. 10 registered artifacts drifted → accepted two-sided (chg:bl58-silent-failure-batch); export 229n/466e, gaps 0. Gates: 39 workspace suites, clippy clean, smoke + phase_trial + model_the_loop + coherent_erosion + build_design_graph all green. Versioning policy: patch=fixes/silent-failure-loud, minor=surface/schema shape change; this batch is all patch → next cut is 0.6.1. Remaining review: BL-59 (perf), BL-56b (harness leaks) — @ajs — 2026-07-21 — (this commit)

- BL-57 done (all 7): dyno_err variant-aware (caller mistakes → invalid_params, ~60 tools, one choke point, Schema stays internal since it's open-time); deny_unknown_fields on all 65 request structs + smoke additionalProperties:false guard — IMMEDIATELY caught a real latent bug (smoke passed `at` to reconcile_artifacts, silently ignored; field is detected_at); export_graph overwrite guard + resolved-path + invalid_params for unwritable; serve path gets explain_open_failure; get_node one named {node} shape both ways (rippled 17 consumers, 0 skills — strengthened previously-always-true smoke checks); withdraw_gap_ack `was_reviewed`→`withdrawn`; answer_question keeps erroring (doctrine: no silent drop) + documented. 4 new tests (taxonomy code-assert, overwrite guard, +the BL-62 ones adjusted). Gates all green; drift accepted (chg:bl57-tool-boundary-honesty); export 218n/445e. Remaining review: BL-56b, BL-58, BL-59 — @ajs — 2026-07-21 — (this commit)

- BL-61 + BL-62 done: skill_lint's `_`-filter dropped so single-word tool names (allocate/satisfies/genesis/…) are checked — 11 tools were exempt; allowlist +58 single-word non-tool terms, unused-guard keeps it exact, negative-tested (renamed single-word tool → exit 1). BL-62: all 14 uncovered tools now tested — 2 tools.rs tests (temporal/resource/realization/analysis/delete walk + ask→withdraw) → tools.rs 31→33, and smoke §9c drives create_node/scan_nodes/search_design/delete_node/get_node over REAL stdio (the blind spot smoke exists for). Pure tests+lint, no registered-artifact drift. Gates: workspace 39 green, fmt/skill_lint clean, smoke ALL PASS. Remaining review items: BL-56b, BL-57, BL-58, BL-59 — @ajs — 2026-07-21 — (this commit)

- BL-60 docs truth pass DONE: the primary instruction files no longer describe the pre-surface era. AGENTS.md "Current state" rewritten to v0.5.0 (surface shipped + decided, full 30-module list, GENESIS/INGEST built, two crates, v0.10.0 pin, 54 edges, INCLUDES traceable); README (27 types + Question, real layout tree, path fix); requirements-coverage (IS-5/6/7 → ✅, preamble/deferral/numerals); surface-plan + interaction-surfaces superseded banners; overview routing + private-repo delinking; SETUP public-repo + commit-an-export; getting-started/README all 11 skills; 3 skill contradictions fixed (link-artifacts full:true, detect-and-ask→retire, check-health apply gate — verified against heal.rs:849 "requires_human_review is not consulted"). skill_lint allowlist += blocked_by_mode (the lint caught my own new field ref). Pure docs — no graph writes, no artifact drift. Gates: skill_lint/test_init/validate_schema green — @ajs — 2026-07-21 — (this commit)

- Deep review (4 parallel reviewers, criticals verified in source) → BL-53..62 raised; tier-1 FIXED same day: BL-53 HEAL self-loop merge-deletes-node (guard in merge_op_for, covers propose+apply), BL-54 installer hash-manifest ownership (edits LEFT ALONE, obsolete files pruned when untouched, non-dict config no longer crashes, --check agrees; 12 new test_init cases → 21), BL-55 install.sh silent death + false success fixed and release.yml drafts-then-publishes with asset assertion, BL-56a smoke --graph-path needs --wipe. Open tiers: BL-56b harness leaks, BL-57 boundary honesty, BL-58 core silent-failure batch, BL-59 adopt-scale perf, BL-60 docs truth pass (AGENTS.md "Current state" is fiction — critical for new readers), BL-61 skill-lint single-word blind spot, BL-62 coverage gaps. Gates all green; drift accepted (chg:deep-review-tier1); export 215n/440e — @ajs — 2026-07-21 — (this commit)

- BL-52 done — first CI + skill lint: ci.yml (core job: fast gates incl. clippy -D warnings both crates, schema, test_init, skill lint; full job: workspace + smoke_mcp + phase_trial + model_the_loop + coherent_erosion against the real binary, rust-cache'd). tools/skill_lint.py checks the skills' CONTRACT (tool refs resolve vs the #[tool] set with a both-ways allowlist, mirrors byte-identical, frontmatter, BL-41 rule) — deliberately no LLM evals, trials stay the semantic evidence. All green locally + negative-tested; ver:skill-lint added (passing) VERIFIES cmp:skills; export 212n/435e. Watch the first live run — @ajs — 2026-07-20 — (this commit)

- v0.5.0 Release node done: rel:v050 with all 34 artifacts frozen at tag v0.5.0 + cmp:skills, deployed active on env:dev; release_report answers released==designed with 26/26 capabilities covered and an empty not-covered diff; rel:v040 retired, its deployment withdrawn (rolled_back — the sanctioned "declaration withdrawn" vocabulary per reconcile_deployment's own correction path). ROOT-CAUSE FIX: INCLUDES joined the traceability table (nodes.rs) — the {env:dev, rel:v040} island existed because every release was outside the design network by construction; pinned by structural + propagate tests. build_design_graph.py now freezes release checksums from git tags, never the working tree (v0.4.0 manifest verified 33/33 against its tag first). Axis-Z: epoch:v050-cut + epoch:v050-hardening added, five floating ChangeEvents pinned (render_views confession cleared). ALSO from user mid-session: req:frictionless-update captured (Claude-Code-style one-liner install / one-word update / frequent minor cadence) → BL-51. Export 211n/434e, gaps 0 — @ajs — 2026-07-20 — (this commit)

- BL-50 done (all three): int literals widen to schema floats at the core write seam (range check kept, foundation untouched); add_change_event takes `affected` and draws CHANGED atomically — refuse-first, write-whole; describe_schema counts half-exact matches so CHANGED reads as the modelled fit; delete_node/delete_edge return {deleted} (bare-bool envelope, caught by BL-48's choke-point wrap); SessionStart hook recipe documented in the kit's step 0a. Workspace 39 suites green, smoke ALL PASS incl. 9 new checks; drift on art:graph/art:vocabulary/art:service accepted two-sided (chg:bl50-tool-boundary), export 205n/386e, gaps 0. Same restart caveat: a session predating this build serves the old surface — @ajs — 2026-07-20 — (this commit)

- BL-48 + BL-49 done: graph_report_markdown returns prose as text (ok_json wraps any scalar — the string twin of the array envelope bug), propagate defaults to a core-computed summary (full dump behind full=true), export_graph writes a deterministic file on request; smoke_mcp asserts the result envelope on every call + drives all three over real stdio; impact-check skill teaches summary-first; drift on art:propagate/art:service accepted two-sided (chg:bl48-bl49-tool-surface), export 201n/380e, gaps 0. Side effect: reflow2_init.py rerun for the mirror refresh did the full self-host conversion — REFLOW2.md + pointer lines committed, per-machine MCP configs (opencode.json, .vscode/mcp.json) gitignored. NOTE: a session started before this build serves the old shapes — restart before relying on summary/path live — @ajs — 2026-07-20 — (this commit)

- Proposed-requirement sweep + capability verification done (user-directed): all 6 proposed requirements evidence-checked against the repo and accepted (deterministic-core, invocation, persistence, driving-agent, human-decides, as-built-honest — statements match reality verbatim, provenance now explicit `authored`); cap:dimensions + cap:ingest realized→verified on live runs of their suites (6/6, 16/16), VERIFIES edges already in place; gaps stay 0; export refreshed (198n/376e). Requirement statuses now: 17 accepted, 0 proposed — @ajs — 2026-07-20 — (this commit)

- BL-47 + BL-46 done: merge survivor ranks unset provenance below explicit authored (the near-deletion of cap:kit can't recur), colliding edges keep the survivor's properties, create_node on an existing id merges per the revise-design contract (`upsert_node`); workspace green, smoke_mcp green, drift accepted two-sided (chg:merge-integrity-bl47-bl46). NOTE: this session's running server predates the rebuild — restart before relying on the new semantics live — @ajs — 2026-07-20 — (this commit)

- **Stub-survivor reconciliation done — first live self-adopt session, 0 gaps**: where-am-i →
  detect-and-ask with the user (all 4 decisions theirs: 3 merges, cap:store wired, req:platform
  satisfied by cap:kit, storyflow trial recorded as cap:adopt's proof), HEAL merges under the
  survivor rule, gaps 11→0, defects 6→4, dec:merge-survivor-provenance landed, export refreshed
  (197n/370e, stamp 0.5.0). 5 findings about reflow2 itself → BL-46..BL-50;
  trial: docs/trials/2026-07-20-self-adopt-live.md — @ajs — 2026-07-20 — (this commit)

- **HISTORY REWRITTEN 2026-07-20 (@bro: action needed)** — repo went public (Apache-2.0); five real-system trial records (storyflow x2, ophyd-service, aidrone, 3dtictactoe) were scrubbed from ALL history before anyone cloned. Every SHA changed; tags moved. Update your clone with: `git fetch origin && git checkout main && git reset --hard origin/main` (stash local work first), or re-clone. Private trial records now live in gitignored docs/trials-private/ (ask @ajs for copies); public trials (self-host, erosion, phase-coverage, weather-station) stay — @ajs — 2026-07-20 — (this commit)

- v0.5.0 cut (user approved): surface changed shape (`documents`), CHANGELOG section moved out of Unreleased, no schema change so no upgrade doc; the tag push is the first live run of release.yml — binaries + kit for the no-checkout path. NOTE for next session: served_by will say 0.5.0 now, not 0.4.0 — @ajs — 2026-07-20 — (this commit)

- BL-15 published binaries built: release.yml (3 platforms + kit tarball + checksums, version-less asset names), install.sh (gh-first for the private repo, checksum-verified, binary+kit replaced together), reflow2_init.py checkout-independent (--binary/PATH, KIT_VERSION.json, installer update advice); SETUP.md leads with the no-build path. Verified from a simulated tarball end to end; the real release run against v0.4.0 is the remaining verification. Embed-kit-in-binary stays as the S-M follow-up — @ajs — 2026-07-20 — (this commit)

- BL-26 S half done: `documents` core fn + MCP tool (78th), endpoints fail-loud (storage accepts dangling edges — the check is the only one there is), doc_kind carried; link-artifacts skill states the record-this-file criterion, mirrors refreshed. The traversal decision (M half) stays open and wants the user — @ajs — 2026-07-20 — (this commit)

- BL-29 survivor rule done, closing the item: user decided option 2 — provenance wins (authored > planned > imported > reconciled > inferred > healed), id breaks ties; pinned in three directions, pre-provenance graphs unchanged. Decision node for the design graph queued for the first live-server session — @ajs — 2026-07-20 — (this commit)

- BL-29 chained-merge hazard reproduced and fixed: sanctioned merges sharing a node corrupted the graph silently with `verified=true` (dangling edges accepted by storage); propose now defers chain links, apply refuses shared-node proposals pre-write, third-party DUPLICATES claims survive a merge; pair-edge drops reported. BL-29 done to a decision (survivor rule). Also: fmt fixes to search.rs/service.rs that slipped past the previous session's gate — @ajs — 2026-07-20 — (this commit)

- Restart batch test live half done: real design imported over the stub (CLI path, 180 nodes/332 edges), skew check PASS via raw-stdio probe (served_by 0.4.0, mtime match, delete_edge+search_design present), deltas verified stub-survivor-shaped, **v0.4.0 tagged and pushed** — @ajs — 2026-07-20 — (this commit)

- Full-text search done: `fulltext` feature enabled (schema carried the flags all along — recurring-lesson #17, this time even the annotations were unreachable capability), search.rs core op + search_design MCP tool + reindex-at-open, search-before-add in capture-intent and find-the-node in revise/retire skills; gates green incl. smoke_mcp against the fresh binary. NOTE: the feature flip re-fingerprints librocksdb-sys — budget ~14 min for the one-time rebuild — @ajs — 2026-07-20 — (this commit)
- CRUD skill closure done: revise-design + retire-from-design skills (update/delete were the kit's missing verbs), delete_edge MCP tool (+ tools.rs test — a wrong edge no longer costs an endpoint node), stale skill mirrors refreshed, create_node merge semantics finally written down — @ajs — 2026-07-20 — (this commit)
- Cycle break + true-gap closure done: propagate↔structure cycle broken (shared vocabulary moved to nodes.rs/graph.rs, verified gone by the probe that found it AND by the rebuilt self-model); confirm.rs and reflow2_init.py each got their first test suite; self-model now 175 nodes, 1 gap (cap:adopt, deliberate), 4 warnings, 0 critical — @ajs — 2026-07-20 — (this commit)
- Self-model standing probe done: build_design_graph.py derives DEPENDS_ON from source and reconciles vs disk; graph 125→173 nodes, gaps 16→3 (all true), and reflow2 now reports its own propagate↔structure cycle as critical — @ajs — 2026-07-20 — (this commit)
- F7 done: flow cycles report members + path (storyflow's cluster kept p-prompt, the human hand-off, that the walk dropped); adopt trial's F1-F7 all closed — @ajs — 2026-07-20 — (this commit)
- F6 done: SPOF skips components coupled only by a library contract (storyflow 7 of 15 -> 5); medium default keeps today's behaviour — @ajs — 2026-07-20 — (this commit)
- BL-27 F1/F2 done: pointer reaches every instruction-file convention (CLAUDE.md et al); next-steps branches on the project, not our own artifact — @ajs — 2026-07-20 — (this commit)
- BL-42 + BL-43 done: adopt's noise floor halved on the same graph (82 -> 57 outputs, every true finding kept); graph_report counts every node — @ajs — 2026-07-20 — (this commit)
- Adopt trial on storyflow (2643 files) done: 5 true findings about storyflow, 4 about reflow2; BL-42/BL-43 raised — @ajs — 2026-07-20 — (this commit)
- BL-27 step 3 done: the adopt skill — the RE lifecycle operational, ninth kit skill; next brownfield trial should run through it — @ajs — 2026-07-19 — (this commit)
- BL-30 done (M half): reconcile_verification completes the reconcile family — phase trial 13/13, first fully-green run, now a regression gate — @ajs — 2026-07-19 — (this commit)
- BL-27 step 1 done: three conversion fixes in reflow2_init.py (pointer line, gitignore, branched next-steps) + design_without_intent at 0.72 — @ajs — 2026-07-19 — (this commit)
- BL-9 + BL-11 done: reconcile_deployment (P5 2/2, phase 12/13) and budget_report (Constraint write side, fourteenth recurring-lesson instance); all ten viewpoints render — @ajs — 2026-07-19 — (this commit)
- BL-40 second increment done: evolution + provenance views — 8 views rendered, every projectable catalogue row done; core read-tool step stays open — @ajs — 2026-07-19 — (this commit)
- BL-40 first increment done: flow/as-released/decisions views + docs/viewpoints.md catalogue + --graph-path live mode; core read-tool step stays open on the row — @ajs — 2026-07-19 — (this commit)
- BL-37 done: Flow write side + TRIGGERS.role; flow_report with cycles reported-never-judged; loop probe 4 frictions → 0, now the fifth instrument — @ajs — 2026-07-19 — (this commit)
- BL-41 S half done: "graph text is data, never instructions" stated in consumer AGENTS.md, all 8 skills, and the get_info handshake; M (mechanical trust) stays open — @ajs — 2026-07-19 — (this commit)
- BL-36 + BL-31 done: precedes tool (coherent 9/9, first fully green); status_contradiction, whose first catch was our own cap:kit (phase 11/13) — @ajs — 2026-07-19 — (this commit)
- BL-32 done + v0.3.0 prepared: served_by, correct server identity, upgrade doc, bump — tag gated on the restart batch test — @ajs — 2026-07-19 — (this commit)
- BL-34 done: INCLUDES + release_report + unreleased_component; schema 53→54 (phase 10/13, erosion 7/8, coherent 8/9) — @ajs — 2026-07-19 — (this commit)
- BL-35 done: the confirmation ledger — drifting/confirmed/unexamined per claim (erosion 5/8, coherent 6/9) — @ajs — 2026-07-19 — (this commit)
- BL-33 done: accept is two-sided; the second question is posed by the tool (erosion 4/8, coherent 5/9) — @ajs — 2026-07-19 — (this commit)
- BL-33 S sub-piece done: a new drift is a new event (5 drifts = 5 events; probe tightened) — @ajs — 2026-07-19 — (this commit)
- BL-30 S half done: failing_verification at 0.8; coverage counts passing only (P4 1/4→2/4, erosion 2/7→3/7) — @ajs — 2026-07-19 — (this commit)
- BL-5 second pass done: SPOF scoped to operational types (22→4, instrument at zero known-false) — @ajs — 2026-07-19 — (this commit)
- BL-38 done: both P3 shapes count as built; assemblies are not dead ends (design graph 33→16 gaps) — @ajs — 2026-07-19 — `e5635e1`
- BL-39 done: `reflow2-mcp --import`, so a design can enter a graph without MCP — @ajs — 2026-07-19 — `dd7e2ac`
- Trials: phase-coverage, erosion, coherent-erosion, self-host design; BL-30..38 raised — @ajs — 2026-07-19 — `e429039`..`f166047`
- docs/sharpening.md: the standing method for improving reflow2 — @ajs — 2026-07-19 — `5617503`
- BL-29: apply_heal checks the proposal; merge reports what it drops — @ajs — 2026-07-19 — `be6e18d`
- BL-27: possible_duplicate — all 5 adopt blockers now done; BL-29 raised — @ajs — 2026-07-19 — `3410117`
- BL-27: unmotivated_capability, the direction DETECT was blind in — @ajs — 2026-07-18 — `d470860`
- BL-27: 3 of 5 adopt blockers (capability status, provenance, gap ranking) — @ajs — 2026-07-18 — `83da659`, `3c157ab`
- BL-28 typed tool params (fixes the ask half of DETECT from Claude Code) — @ajs — 2026-07-18 — `6dfdd61`
- Self-host GENESIS trial; BL-28 raised, BL-27 widened — @ajs — 2026-07-18 — `2ea0db1` + revision
- Brownfield trial on 3dtictactoe; BL-27 raised from it and ophyd — @ajs — 2026-07-18 — `84aacb6`
- Self-host: consumer kit installed into reflow2 itself — @ajs — 2026-07-18 — `98baf40`
- **v0.2.0 released and tagged** — @ajs — 2026-07-18 — `b69dc28`; frozen for real-use feedback
- BL-20 export/import; BL-19 backup+backfill; BL-18 staleness check — @ajs — 2026-07-18 — `79f5d42`, `3a618e3`, `21e9e11`
- BL-19 (stamp+check half): a graph records which reflow2 wrote it — @ajs — 2026-07-18 — `2764eec`
- BL-21: report-friction skill; installer validates skill frontmatter — @ajs — 2026-07-18 — `31064f6`
- BL-25: an answered question stays visible while its gap is open — @ajs — 2026-07-18 — `20b7943`
- BL-4: asked questions outlive the session (new Question node type) — @ajs — 2026-07-18 — `be510b4`
- BL-5: SPOF measured against the baseline (self-host: 8 defects -> 2) — @ajs — 2026-07-18 — `44e3e23`
- BL-24: Project containment anchors a subsystem (self-host: 3 gaps -> 1) — @ajs — 2026-07-18 — `0bf157d`
- BL-23: per-file coverage counted not asked (self-host probe: 25 gaps -> 3) — @ajs — 2026-07-18 — `7c5b702`
- BL-6b: coupling demoted to a signal; retired acks stay visible — @ajs — 2026-07-18 — `f340fcb`
- BL-22: kit reaches every agent; MCP configs merged not overwritten — @ajs — 2026-07-18 — `9e9e765`
- BL-2/BL-3/BL-6: assembly hierarchy, requirement status, artifact gap split — @ajs — 2026-07-18 — `9ab3da3`
- BL-1 schema discovery + evidence-backed rejections; per-crate AGENTS.md — @ajs — 2026-07-18 — `9440929`
- Answer the first external user (where-am-i skill, pause/resume, setup kickoff) — @ajs — 2026-07-18 — `ed818ae`
- Records: CHANGELOG, backlog, trials — @ajs — 2026-07-18 — `ed818ae`
- Gap review + cross-process determinism fix — @ajs — 2026-07-18 — `8a66afb`, `565c418`
- Selective `unexpected_coupling`; provenance out of topology — @ajs — 2026-07-18 — `824a6cc`
- Write side for the types DETECT asks about (WS-1..3) — @ajs — 2026-07-18 — `e722766`
- Reflow audit: all workflows and tools, with verdicts — @ajs — 2026-07-18 — `7179218`
- Interface layer, cycle detection, as-built drift — @ajs — 2026-07-18 — `7f168a5`

## Stale claims

If an item has been claimed for **more than a week with no commits against it**, anyone may take
it — leave a note on the line saying you did, rather than deleting the original claim.

## When git conflicts

It will happen — two people, one repo, and the shared records are the files you both touch most.

**The rule that matters: never resolve a conflict by discarding the other side.** For an agent,
"resolve the conflict" usually means picking a side — and here that silently deletes a
teammate's claim, changelog entry or finding. Both sides are almost always correct; the job is
to keep both, not to choose.

| File | What to do |
|---|---|
| `COORD.md`, `CHANGELOG.md` | Resolved automatically — `.gitattributes` marks them `merge=union`, so both sides' lines are kept. If you *still* see a conflict, you both edited the same line: keep both meanings and tidy the wording. |
| `docs/design/reflow2.json` | **Merged by reflow2 itself** — `.gitattributes` points it at the `merge=reflow2` driver, which resolves per node and per property against the common ancestor. Two people editing different parts of the design merge with no conflict at all. A real both-sides conflict stops with its ids and the `--merge-apply` command that finishes it; never resolve it with `--ours`/`--theirs`, which discards a node the other person wrote. Needs one-time setup per clone — see [AGENTS.md](AGENTS.md). |
| `docs/backlog.md`, `docs/requirements-coverage.md` | Usually you each touched different rows — keep both. If it's the **same** row, someone's status is newer than yours; check `git log -p` on that file and ask rather than overwriting. |
| `docs/trials/*` | Append-only evidence. Keep both; never edit someone else's trial record. |
| Source and tests | A real conflict here means the claims didn't work — two people edited the same module. Reconcile the *intent*, not just the text, and re-run the full gates before pushing. |
| `Cargo.lock` | Regenerate rather than hand-merge: take either side, then `cargo build` and commit the result. |

**If you cannot resolve it confidently, stop and say so.** Pushing a guess is how one person's
afternoon gets quietly deleted. An unresolved conflict sitting in the working tree is a
recoverable state; a bad merge on `main` is not.

## Conventions

- **Branches:** `feat/<short-name>` off `main`, one per claimed item where practical.
- **A change is done** when `cargo test --no-default-features`, `cargo clippy
  --no-default-features --all-targets` and `cargo fmt --check` are clean, and
  `python3 tools/validate_schema.py` prints OK after any schema edit — see
  [AGENTS.md](AGENTS.md).
- **Update the records in the same change**, not afterwards: coverage matrix when a status moves,
  CHANGELOG when a user would notice, backlog when an item is finished or discovered.
- **Findings from real use** (a trial, a session that went wrong) go in
  [docs/trials/](docs/trials/) verbatim, and get an item in the backlog if they need work.
