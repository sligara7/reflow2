---
description: What's been decided in the reflow2 design, and why
---
List the decisions recorded in this design (`scan_nodes` for the `Decision` type) and summarize them for me in plain language — *what* was decided and the *reasoning*, not the ids.

Order them so the load-bearing ones come first. This is the "what have we actually concluded?" view — if a decision has open alternatives or is still proposed, say so.
