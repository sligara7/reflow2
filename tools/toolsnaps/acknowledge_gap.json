{
  "annotations": {
    "readOnlyHint": false
  },
  "description": "Accept a gap the user has judged fine, recording WHY. It moves out of `detect_gaps` into `reviewed_gaps` — not deleted, not hidden. Use this once the user has actually decided something, so the open list means \"still needs attention\"; a list that can never reach zero gets skimmed. The reason is stored as a real Decision node in the graph, so it outlives this session. If the gap's affected nodes later change, the review expires and the gap returns for a fresh judgement.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "acted_at": {
        "default": null,
        "description": "When the judgement was made. The core takes no clock.",
        "type": [
          "string",
          "null"
        ]
      },
      "affected_ids": {
        "default": [],
        "description": "The gap's `affected_ids`, so the review is reachable from the design.",
        "items": {
          "type": "string"
        },
        "type": "array"
      },
      "approver": {
        "default": null,
        "description": "WHOSE judgement this is — the `Contributor` who decided the gap is\nacceptable. Draws `AUTHORED_BY role=approver` on the Decision this\nmints.\n\nOPTIONAL, and the absence is REPORTED rather than assumed: a design that\nhas modelled no Contributor would otherwise be unable to acknowledge\nanything. But an acknowledgement IS the owner's word by definition, and\none with no name on it fails `check_intent_authority` — measured\n2026-08-23, acknowledging 50 gaps produced 49 such nodes in one stroke.\n\nA name that matches no Contributor is REFUSED, not ignored: a typo would\notherwise attach the owner's authority to somebody who does not exist.",
        "type": [
          "string",
          "null"
        ]
      },
      "gap_id": {
        "description": "The gap's `id`, exactly as `detect_gaps` reported it.",
        "type": "string"
      },
      "reason": {
        "description": "Why this gap is acceptable. Recorded as the Decision's rationale.",
        "type": "string"
      }
    },
    "required": [
      "gap_id",
      "reason"
    ],
    "type": "object"
  },
  "name": "acknowledge_gap"
}
