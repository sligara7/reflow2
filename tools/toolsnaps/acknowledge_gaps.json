{
  "annotations": {
    "readOnlyHint": false
  },
  "description": "Acknowledge MANY gaps in one call — the bulk form of acknowledge_gap. EACH GAP CARRIES ITS OWN REASON, which is the point: a batch of acknowledgements under one shared reason is exactly the erosion the ask-don't-repair rule exists to prevent, and would make a bulk form worse than the loop it replaces. The round trip collapses; the judgement stays per gap. ALL OF IT OR NONE OF IT — every item is attempted so you learn every failure at once, and if anything failed nothing is acknowledged.",
  "inputSchema": {
    "$defs": {
      "GapAckReq": {
        "additionalProperties": false,
        "description": "One acknowledgement for `acknowledge_gaps`, carrying **its own** reason.",
        "properties": {
          "acted_at": {
            "default": null,
            "description": "When the judgement was made. The core takes no clock.",
            "type": [
              "string",
              "null"
            ]
          },
          "affected_ids": {
            "default": [],
            "description": "The gap's `affected_ids`, so the review is reachable from the design.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "approver": {
            "default": null,
            "description": "WHOSE judgement this is — the `Contributor` who decided the gap is\nacceptable. Draws `AUTHORED_BY role=approver` on the Decision this\nmints.\n\nOPTIONAL, and the absence is REPORTED rather than assumed: a design that\nhas modelled no Contributor would otherwise be unable to acknowledge\nanything. But an acknowledgement IS the owner's word by definition, and\none with no name on it fails `check_intent_authority` — measured\n2026-08-23, acknowledging 50 gaps produced 49 such nodes in one stroke.\n\nA name that matches no Contributor is REFUSED, not ignored: a typo would\notherwise attach the owner's authority to somebody who does not exist.",
            "type": [
              "string",
              "null"
            ]
          },
          "gap_id": {
            "description": "The gap's `id`, exactly as `detect_gaps` reported it.",
            "type": "string"
          },
          "reason": {
            "description": "Why THIS gap is acceptable. One reason per gap — a shared one would be\nthe erosion `dec:ask-not-repair` and `dec:two-sided-accept` forbid.",
            "type": "string"
          }
        },
        "required": [
          "gap_id",
          "reason"
        ],
        "type": "object"
      }
    },
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "gaps": {
        "items": {
          "$ref": "#/$defs/GapAckReq"
        },
        "type": "array"
      }
    },
    "required": [
      "gaps"
    ],
    "type": "object"
  },
  "name": "acknowledge_gaps"
}
