{
  "annotations": {
    "readOnlyHint": false
  },
  "description": "Create an Artifact node — a real deliverable (file/spec/doc) that lives outside the graph, pointed to by `location`. CONTENT FIELDS ARE REQUIRED TO CREATE AND OPTIONAL TO REVISE: call it again with the same id and only what you are changing — omitted fields keep their stored value, so correcting one never means re-sending a 2 KB field you did not touch. Ask for this when you want to register a file, drawing or document in the design.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "artifact_type": {
        "default": null,
        "description": "`code` (default) / `spec` / `document` / `diagram` / `model` / …",
        "enum": [
          "code",
          "config",
          "document",
          "spec",
          "diagram",
          "model",
          "drawing",
          "test",
          "test_result",
          "binary",
          "data",
          "asset",
          null
        ],
        "type": [
          "string",
          "null"
        ]
      },
      "checksum": {
        "description": "A content hash of the file (`sha256:<hex>`) — the baseline that makes a later edit detectable.\nSupply it on creation, as `link_artifact` does: without one, `reconcile_artifacts` can say the file\nvanished but never that its contents changed. On a REVISE of an artifact that already carries one it\nis REFUSED — moving a baseline is a drift disposition, which is `set_artifact_checksum`'s job.",
        "type": [
          "string",
          "null"
        ]
      },
      "description": {
        "default": null,
        "description": "WHAT THIS IS, in prose. Added 2026-09-07: `description` is the only\nprose field this type declares, and no tool offered it, so the surface\nlet a caller NAME the thing and never say what it was — six of eleven\ntypes that declare a description were in that state, because the class\nwas fixed one report at a time and never swept\n(`fact:six-constructors-cannot-write-any-prose-because-the-class-was-fixed-one-report-at-a-time-and-never-swept`).",
        "type": [
          "string",
          "null"
        ]
      },
      "id": {
        "description": "The artifact's id — `art:<slug>` by convention, so the prefix names the type wherever the id appears. Calling again with an EXISTING id REVISES that node: what you pass overwrites, what you omit survives.",
        "type": "string"
      },
      "location": {
        "default": null,
        "description": "Path / URI / content-hash of the real deliverable (lives outside the graph).",
        "type": [
          "string",
          "null"
        ]
      },
      "name": {
        "default": null,
        "type": [
          "string",
          "null"
        ]
      }
    },
    "required": [
      "id"
    ],
    "type": "object"
  },
  "name": "add_artifact"
}
