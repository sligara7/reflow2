{
  "annotations": {
    "readOnlyHint": false
  },
  "description": "Create a Capability node. ⭐ DRAW THE GOLDEN THREAD IN THIS CALL: `satisfies` (the Requirement it serves) and `allocated_to` (the Component that will provide it) each draw their edge here, so the thread costs one call instead of three. An id naming nothing is REFUSED and NOTHING IS WRITTEN — a capability left carrying half a thread to a mistyped id is worse than the retry this removes. `status` defaults to `planned`; set it when recording something that already exists, so adopting a running system does not describe it as entirely unbuilt. CALLING THIS AGAIN WITH AN EXISTING ID REVISES that node: what you pass overwrites, and every field you do NOT pass keeps its current value instead of reverting to a default — so sharpening a description never silently unbuilds a verified capability (BL-183). CONTENT FIELDS ARE REQUIRED TO CREATE AND OPTIONAL TO REVISE: call it again with the same id and only what you are changing — omitted fields keep their stored value, so correcting one never means re-sending a 2 KB field you did not touch.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "allocated_to": {
        "default": null,
        "description": "THE COMPONENT THAT WILL PROVIDE IT — draws the ALLOCATED_TO edge in this\ncall. Second half of the same thread, refused the same way.",
        "type": [
          "string",
          "null"
        ]
      },
      "description": {
        "default": null,
        "description": "What this capability does.",
        "type": [
          "string",
          "null"
        ]
      },
      "distinct_from": {
        "default": null,
        "description": "Ids you read and judged DIFFERENT from this one, when reflow2 has\nalready told you something close exists. Naming them is the deliberate\ndecision: sharpen an existing node by calling with ITS id, or start a\nnew one and say what you rejected. Omit it on a first attempt — the\nrefusal, if any, lists exactly what to put here.",
        "items": {
          "type": "string"
        },
        "type": [
          "array",
          "null"
        ]
      },
      "id": {
        "description": "The capability's id — `cap:<slug>` by convention, so the prefix names the type wherever the id appears. Calling again with an EXISTING id REVISES that node: what you pass overwrites, what you omit survives.",
        "type": "string"
      },
      "is_entry_point": {
        "default": null,
        "description": "True when this capability STARTS a flow. Distinct from `Flow.entry_point`,\nwhich names a capability from the flow's side; this is the flag on the\ncapability itself, carried by 147 of 234 and written by nothing.",
        "type": [
          "boolean",
          "null"
        ]
      },
      "is_exit_point": {
        "default": null,
        "description": "True when this capability ENDS a flow. Sibling of the above.",
        "type": [
          "boolean",
          "null"
        ]
      },
      "name": {
        "default": null,
        "type": [
          "string",
          "null"
        ]
      },
      "satisfies": {
        "default": null,
        "description": "THE REQUIREMENT THIS CAPABILITY SATISFIES — draws the SATISFIES edge in\nthis call. The golden thread's first half, and the reason it is here:\n`add_verification` already takes `verifies` and `add_decision` already\ntakes `related_to`, while the busiest constructor on the thread took\nneither of its two edges and cost three calls where one would do\n(hxm_program F-02).\n\nAn id naming no Requirement is REFUSED and NOTHING IS WRITTEN, the same\nposture `approver` takes on the settling constructors: a typo must not\nleave a capability behind carrying half a thread to something that does\nnot exist.",
        "type": [
          "string",
          "null"
        ]
      },
      "status": {
        "default": null,
        "description": "`planned` (default) / `in_progress` / `realized` / `verified`. Leave it\nunset when designing forwards — a new capability really is planned.\nSet it when recording a capability that already exists, so the graph\ndoes not assert that a shipped system is entirely unbuilt.",
        "enum": [
          "planned",
          "in_progress",
          "realized",
          "verified",
          null
        ],
        "type": [
          "string",
          "null"
        ]
      },
      "tier": {
        "default": null,
        "description": "Where this sits on the strategic / operational / tactical ladder.\nDeclared 2026-09-07: carried by most nodes of this type and settable by\nnothing, one of the fourteen holes the reachability split separated\nfrom the properties an operation writes.",
        "enum": [
          "strategic",
          "operational",
          "tactical",
          null
        ],
        "type": [
          "string",
          "null"
        ]
      }
    },
    "required": [
      "id"
    ],
    "type": "object"
  },
  "name": "add_capability"
}
