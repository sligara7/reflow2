{
  "annotations": {
    "readOnlyHint": false
  },
  "description": "Create a ChangeEvent (seed for propagate_change). Pass `affected` to say in the same call what it changed — a CHANGED edge is drawn to each entry, which is what makes the event propagatable. TWO QUESTIONS, NOT ONE: `change_type` says WHY, and `subject` says WHICH AXIS — `system` (the thing changed) or `record` (the thing did not change and only the design's knowledge of it did, e.g. a first baseline, a re-sync, a question settled). Leaving `subject` out is a true answer and is never inferred from `change_type`, because the mapping is not total. Use `defect_fix` when the design was right and the code was wrong, and `test_failure_fix` only when a check actually caught it — the difference is provenance, and it is the one five sessions each guessed differently before these existed. TEXT GOES IN `summary` (what changed — indexed and searchable) and `rationale` (why, and the lesson). THERE IS NO `description` FIELD: reaching for one is the commonest mistake here, and it is refused rather than stored, so write the two that exist.",
  "inputSchema": {
    "$defs": {
      "AffectedNodeReq": {
        "additionalProperties": false,
        "description": "One node an event changed, for `add_change_event`'s `affected` list.",
        "properties": {
          "action": {
            "default": null,
            "description": "`added` / `modified` (default) / `removed`.",
            "type": [
              "string",
              "null"
            ]
          },
          "node_id": {
            "description": "The changed node's id.",
            "type": "string"
          },
          "node_type": {
            "description": "The changed node's type (e.g. `Requirement`, `Artifact`).",
            "type": "string"
          }
        },
        "required": [
          "node_type",
          "node_id"
        ],
        "type": "object"
      }
    },
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "affected": {
        "description": "What the change touched: a CHANGED edge is drawn from the event to each\nentry. Every entry must name an existing node — the whole call is\nrefused before anything is written if one does not.",
        "items": {
          "$ref": "#/$defs/AffectedNodeReq"
        },
        "type": [
          "array",
          "null"
        ]
      },
      "change_type": {
        "description": "Change type key (e.g. `new_feature`, `scope_change`, `defect_fix`).",
        "type": "string"
      },
      "id": {
        "type": "string"
      },
      "name": {
        "type": "string"
      },
      "rationale": {
        "default": null,
        "description": "WHY the change was made: the reasoning, the lesson, what guards against\nit happening again. The field the skills tell you to write.\n\nTHERE IS NO `description` FIELD ON A ChangeEvent, and reaching for one\nis the commonest mistake here — reported independently by two projects\non 2026-08-19, both of which fell back to a second `create_node` call\nthat hung `description` on the event as an undeclared property. Use\n`summary` for what changed and `rationale` for why.",
        "type": [
          "string",
          "null"
        ]
      },
      "subject": {
        "default": null,
        "description": "WHICH AXIS this event is on — `system` (the thing changed) or `record`\n(only the design's knowledge of it changed). OPTIONAL, and leaving it\nout is a true answer: absent means nobody said, and it is never inferred\nfrom `change_type`, because the mapping is not total — a `resync` can be\neither.",
        "type": [
          "string",
          "null"
        ]
      },
      "summary": {
        "default": null,
        "description": "WHAT CHANGED, in a sentence or two — indexed for full text and used as\nthe embedding field, so this is what `search_design` finds the event by.\nKeep `name` short and put the prose here.",
        "type": [
          "string",
          "null"
        ]
      }
    },
    "required": [
      "id",
      "name",
      "change_type"
    ],
    "type": "object"
  },
  "name": "add_change_event"
}
