{
  "annotations": {
    "readOnlyHint": false
  },
  "description": "Create a Component node. Pass `level` when the part is an assembly rather than a leaf (`subsystem`, `system`, `system_of_systems`, `enterprise`; default `component`), then use contain_component to nest it — that pair is what gives hierarchy_issues something to check. Lands `status: planned` unless you pass one: say `realized` when the part already exists (adopt), so the design does not call a shipped system unbuilt. CONTENT FIELDS ARE REQUIRED TO CREATE AND OPTIONAL TO REVISE: call it again with the same id and only what you are changing — omitted fields keep their stored value, so correcting one never means re-sending a 2 KB field you did not touch.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "description": {
        "default": null,
        "description": "What this part is for.",
        "type": [
          "string",
          "null"
        ]
      },
      "distinct_from": {
        "default": null,
        "description": "Ids you read and judged DIFFERENT from this one, when reflow2 has\nalready told you something close exists. Naming them is the deliberate\ndecision: sharpen an existing node by calling with ITS id, or start a\nnew one and say what you rejected. Omit it on a first attempt — the\nrefusal, if any, lists exactly what to put here.",
        "items": {
          "type": "string"
        },
        "type": [
          "array",
          "null"
        ]
      },
      "id": {
        "description": "The component's id — `cmp:<slug>` by convention, so the prefix names the type wherever the id appears. Calling again with an EXISTING id REVISES that node: what you pass overwrites, what you omit survives. (`sub:` / `sys:` for the higher rungs)",
        "type": "string"
      },
      "kind": {
        "default": null,
        "description": "What KIND of part this is — `service`, `module`, `subsystem`, `assembly`,\n`part`, `section`, `layer` — the discriminator a taxonomy decision names\nfor its category (establish-taxonomy). Optional: unset means nobody said,\nand encoding_undecided reports a type where some instances say and some do\nnot. Never defaulted.",
        "enum": [
          "service",
          "module",
          "subsystem",
          "assembly",
          "part",
          "section",
          "layer",
          null
        ],
        "type": [
          "string",
          "null"
        ]
      },
      "level": {
        "default": null,
        "description": "Axis-Y decomposition rank: `component` (default), `subsystem`,\n`system`, `system_of_systems`, `enterprise`. Set it whenever the part\nis really an assembly — `hierarchy_issues` compares the levels either\nside of a containment, so leaving everything at the default means there\nis no hierarchy to check.",
        "type": [
          "string",
          "null"
        ]
      },
      "name": {
        "default": null,
        "type": [
          "string",
          "null"
        ]
      },
      "status": {
        "default": null,
        "description": "`planned` (the landing status) / `in_progress` / `realized` / `verified`.\nLeave it unset when designing forwards — a new part really is planned.\nSet it when recording a part that ALREADY EXISTS (adopt), so the graph\ndoes not call a shipped system unbuilt. Until 2026-09-19 this\nconstructor refused the field and then returned `status: planned`\n(flo2 F10) — the same contract `add_capability` had, now on every\nconstructor of a type that carries a status. A build status is a\nmeasurement, not settled intent, so there is no `approver` on it.",
        "enum": [
          "planned",
          "in_progress",
          "realized",
          "verified",
          null
        ],
        "type": [
          "string",
          "null"
        ]
      },
      "tier": {
        "default": null,
        "description": "Where this sits on the strategic / operational / tactical ladder.\nDeclared 2026-09-07: carried by most nodes of this type and settable by\nnothing, one of the fourteen holes the reachability split separated\nfrom the properties an operation writes.",
        "enum": [
          "strategic",
          "operational",
          "tactical",
          null
        ],
        "type": [
          "string",
          "null"
        ]
      }
    },
    "required": [
      "id"
    ],
    "type": "object"
  },
  "name": "add_component"
}
