{
  "annotations": {
    "readOnlyHint": false
  },
  "description": "Record an Environment — where a Release runs: a cloud region, a lab bench, a physical site. More than a deploy target; it is the context whose rules the design must satisfy. CONTENT FIELDS ARE REQUIRED TO CREATE AND OPTIONAL TO REVISE: call it again with the same id and only what you are changing — omitted fields keep their stored value, so correcting one never means re-sending a 2 KB field you did not touch.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "description": {
        "default": null,
        "description": "WHAT THIS IS, in prose. Added 2026-09-07: `description` is the only\nprose field this type declares, and no tool offered it, so the surface\nlet a caller NAME the thing and never say what it was — six of eleven\ntypes that declare a description were in that state, because the class\nwas fixed one report at a time and never swept\n(`fact:six-constructors-cannot-write-any-prose-because-the-class-was-fixed-one-report-at-a-time-and-never-swept`).",
        "type": [
          "string",
          "null"
        ]
      },
      "env_type": {
        "default": null,
        "description": "`production` (default) / `development` / `staging` / `field` / `lab` / `physical_site`.",
        "enum": [
          "development",
          "staging",
          "production",
          "field",
          "lab",
          "physical_site",
          "simulation",
          null
        ],
        "type": [
          "string",
          "null"
        ]
      },
      "id": {
        "description": "The environment's id — `env:<slug>` by convention, so the prefix names the type wherever the id appears. Calling again with an EXISTING id REVISES that node: what you pass overwrites, what you omit survives.",
        "type": "string"
      },
      "location": {
        "default": null,
        "description": "Cloud region, host, physical site, or jurisdiction.",
        "type": [
          "string",
          "null"
        ]
      },
      "name": {
        "default": null,
        "type": [
          "string",
          "null"
        ]
      }
    },
    "required": [
      "id"
    ],
    "type": "object"
  },
  "name": "add_environment"
}
