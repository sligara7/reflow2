{
  "annotations": {
    "readOnlyHint": false
  },
  "description": "Create a `DesignEpoch` that HAS HAPPENED — a point on the time axis you are recording, which is what an epoch has always meant here. NOTE THE STORED TYPE NAME is `DesignEpoch`, not `Epoch`: that is the string `get_node` and `scan_nodes` want. For a point that has NOT happened yet, use plan_epoch instead; planning is a deliberate act and reads better as its own verb than as a flag. CONTENT FIELDS ARE REQUIRED TO CREATE AND OPTIONAL TO REVISE: call it again with the same id and only what you are changing — omitted fields keep their stored value, so correcting one never means re-sending a 2 KB field you did not touch. Ask for this when you want to mark a point in time in the design's history.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "checksum": {
        "default": null,
        "description": "Optional hash over the anchored spec set at this epoch — an Anchor is a\nchecksummed baseline epoch, and until 2026-09-07 nothing on the surface\ncould write the field that makes one. Same class as `description` above:\ndeclared, never offered, and invisible while the reachability instrument\nfused reach with adoption.",
        "type": [
          "string",
          "null"
        ]
      },
      "description": {
        "default": null,
        "description": "WHAT THIS EPOCH IS, in prose — the ordering, what it must not include,\nwhy it exists. THIS IS THE TYPE'S EMBEDDING FIELD, so it is what\n`search_design` finds an epoch by; keep `name` a short handle and put\nthe prose here.\n\nDeclared 2026-09-07 after the dev_storyflow agent reported it missing:\n*\"add_epoch lost `description` — the ordering and the must-nots had to\ngo into the name, which is now a paragraph.\"* It was never lost; the\nproperty has been declared since the schema had epochs and no tool ever\noffered it, so the field the search finds an epoch BY could not be\nwritten by the tool that makes one.",
        "type": [
          "string",
          "null"
        ]
      },
      "epoch_type": {
        "default": null,
        "description": "`baseline` | `revision` | `milestone` | `incident_response` | `release_cut`.",
        "enum": [
          "baseline",
          "revision",
          "milestone",
          "incident_response",
          "release_cut",
          null
        ],
        "type": [
          "string",
          "null"
        ]
      },
      "id": {
        "type": "string"
      },
      "name": {
        "default": null,
        "type": [
          "string",
          "null"
        ]
      },
      "sequence": {
        "default": null,
        "format": "int64",
        "type": [
          "integer",
          "null"
        ]
      }
    },
    "required": [
      "id"
    ],
    "type": "object"
  },
  "name": "add_epoch"
}
