{
  "annotations": {
    "readOnlyHint": false
  },
  "description": "Create an Interface node — a contract between parts (an API, event, data feed, CLI, library boundary, physical/human connection point, or a procedural contract between institutions). Model one whenever two Components talk to each other, then pair it with `provides` and `consumes`: that pairing is what makes a change on one side of a boundary surface the other side. CONTENT FIELDS ARE REQUIRED TO CREATE AND OPTIONAL TO REVISE: call it again with the same id and only what you are changing — omitted fields keep their stored value, so correcting one never means re-sending a 2 KB field you did not touch. Ask for this to record where two parts meet — the contract at the seam.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "description": {
        "default": null,
        "description": "WHAT THIS IS, in prose. Added 2026-09-07: an Interface declares\n`description` as the ONLY prose field it has, so until this landed the\nsurface let a caller name an interface and never say what it was\n(`fact:six-constructors-cannot-write-any-prose-because-the-class-was-fixed-one-report-at-a-time-and-never-swept`).\nSince 2026-09-19 this struct has ONE user, `add_interface`; `add_project`\nhas its own (`ProjectReq`), because the two types had come to share a\nstruct where half the fields were \"ignored for the other\".",
        "type": [
          "string",
          "null"
        ]
      },
      "id": {
        "description": "Stable node id (e.g. `ifc:state`).",
        "type": "string"
      },
      "name": {
        "default": null,
        "description": "Human-readable name.",
        "type": [
          "string",
          "null"
        ]
      },
      "spec": {
        "default": null,
        "description": "FREE-TEXT DETAIL the structured fields do not carry: a prose note, a\nlink to an OpenAPI document, a header layout. 13 of 22 interfaces\ncarried one and no tool could set it.",
        "type": [
          "string",
          "null"
        ]
      }
    },
    "required": [
      "id"
    ],
    "type": "object"
  },
  "name": "add_interface"
}
