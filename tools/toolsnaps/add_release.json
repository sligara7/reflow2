{
  "annotations": {
    "readOnlyHint": false
  },
  "description": "Record a Release — a packaged, operable version: a container image, a published package, a manufactured build. Part of answering the `no_deploy_operate` gap. CONTENT FIELDS ARE REQUIRED TO CREATE AND OPTIONAL TO REVISE: call it again with the same id and only what you are changing — omitted fields keep their stored value, so correcting one never means re-sending a 2 KB field you did not touch.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "description": {
        "default": null,
        "description": "WHAT THIS IS, in prose. Added 2026-09-07: `description` is the only\nprose field this type declares, and no tool offered it, so the surface\nlet a caller NAME the thing and never say what it was — six of eleven\ntypes that declare a description were in that state, because the class\nwas fixed one report at a time and never swept\n(`fact:six-constructors-cannot-write-any-prose-because-the-class-was-fixed-one-report-at-a-time-and-never-swept`).",
        "type": [
          "string",
          "null"
        ]
      },
      "id": {
        "description": "The release's id — `rel:<slug>` by convention, so the prefix names the type wherever the id appears. Calling again with an EXISTING id REVISES that node: what you pass overwrites, what you omit survives.",
        "type": "string"
      },
      "name": {
        "default": null,
        "type": [
          "string",
          "null"
        ]
      },
      "unit_type": {
        "default": null,
        "description": "`container` (default) / `package` / `binary` / `bundle` / `physical_build` / `publication`.",
        "enum": [
          "container",
          "package",
          "binary",
          "bundle",
          "physical_build",
          "publication",
          null
        ],
        "type": [
          "string",
          "null"
        ]
      },
      "version": {
        "default": null,
        "type": [
          "string",
          "null"
        ]
      }
    },
    "required": [
      "id"
    ],
    "type": "object"
  },
  "name": "add_release"
}
