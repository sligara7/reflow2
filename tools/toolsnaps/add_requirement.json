{
  "annotations": {
    "readOnlyHint": false
  },
  "description": "Create a Requirement node. A new one lands at `proposed`; only the user's word moves it off, through set_requirement_status. CALLING THIS AGAIN WITH AN EXISTING ID REVISES that node: what you pass overwrites, and every field you do NOT pass keeps its current value instead of reverting to a default — so rewording a requirement never silently un-confirms it (BL-183). CONTENT FIELDS ARE REQUIRED TO CREATE AND OPTIONAL TO REVISE: call it again with the same id and only what you are changing — omitted fields keep their stored value, so correcting one never means re-sending a 2 KB field you did not touch.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "acted_at": {
        "default": null,
        "description": "When the approver acted, as a plain date. Stored on the approver edge.",
        "type": [
          "string",
          "null"
        ]
      },
      "approver": {
        "default": null,
        "description": "The Contributor whose word this is — the OWNER'S SIGNATURE, carried in the\nsame call as the status it signs. Draws `AUTHORED_BY role=approver`, the\nedge `rule:design-intent-moves-only-on-the-owners-word` is checked by.\nREQUIRED when the status is past the landing default; an id naming no\nContributor is REFUSED before anything is written, because a typo would\notherwise attach the owner's authority to a name nobody can check.",
        "type": [
          "string",
          "null"
        ]
      },
      "distinct_from": {
        "default": null,
        "description": "Ids you read and judged DIFFERENT from this one, when reflow2 has\nalready told you something close exists. Naming them is the deliberate\ndecision: sharpen an existing node by calling with ITS id, or start a\nnew one and say what you rejected. Omit it on a first attempt — the\nrefusal, if any, lists exactly what to put here.",
        "items": {
          "type": "string"
        },
        "type": [
          "array",
          "null"
        ]
      },
      "id": {
        "type": "string"
      },
      "name": {
        "default": null,
        "type": [
          "string",
          "null"
        ]
      },
      "statement": {
        "default": null,
        "description": "The requirement statement.",
        "type": [
          "string",
          "null"
        ]
      },
      "status": {
        "default": null,
        "description": "The status to LAND IN when the owner's word is already in hand:\n`proposed` (the default) / `accepted` / `deferred` / `dropped` / `met`.\nA status past `proposed` is REFUSED unless `approver` is named.",
        "enum": [
          "proposed",
          "accepted",
          "deferred",
          "dropped",
          "met",
          null
        ],
        "type": [
          "string",
          "null"
        ]
      }
    },
    "required": [
      "id"
    ],
    "type": "object"
  },
  "name": "add_requirement"
}
