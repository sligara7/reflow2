{
  "annotations": {
    "readOnlyHint": true
  },
  "description": "Which earlier design choices does bad news now put in doubt? Every failed run, check recorded failing, unresolved drift and open defect finding is walked back along the golden thread to the ACCEPTED choices governing what it reaches, listed with the evidence and its path: a road that may be worth re-opening. No threshold and no ranking: every such choice comes back, in id order, and the owner judges. `held_up` beside each counts what held up under it on a real run, computed now and never stored as a score. Nothing is re-opened; that is the owner's act. News reaching no choice is named too. An empty answer on a design that has never fed a run back is not an all-clear; loop_status's loop_closure says which. Ask for this when something went wrong and you want to know which earlier choice it calls into question.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "budget_chars": {
        "default": null,
        "description": "How many characters of JSON this reply may spend before prose is\nwithheld to make it fit (default 30,000).\n\nRAISE IT ONLY IF YOU KNOW THIS CLIENT HAS THE ROOM. The default sits\nbelow the smallest tool-output cap in use, because the failure it\nexists to stop is not a slow call — it is the CLIENT refusing the reply,\nat which point the session sees a wall of harness text and reflow2 never\ngets to suggest narrowing.\n\nEvery trimmed reply says what it withheld, at `budget`; counts and ids\nare never budgeted away, so a shorter answer is never a quieter one.",
        "format": "uint",
        "minimum": 0,
        "type": [
          "integer",
          "null"
        ]
      }
    },
    "type": "object"
  },
  "name": "choices_in_doubt"
}
