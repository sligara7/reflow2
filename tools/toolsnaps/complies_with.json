{
  "annotations": {
    "readOnlyHint": false
  },
  "description": "Record that a design element SATISFIES an EnvironmentRule (COMPLIES_WITH). Pass `verified: true` ONLY when compliance was demonstrated — a calc package, a stamped drawing, a test report — and put what demonstrates it in `evidence`. It defaults to false because a claim is not a check, and the two must stay distinguishable to anyone reading the record later.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "element_id": {
        "description": "The design element that complies. Its type is resolved from the id.\nThe complying node; its type is `element_type`.",
        "type": "string"
      },
      "element_type": {
        "default": null,
        "description": "Optional; resolved from the id when omitted.",
        "type": [
          "string",
          "null"
        ]
      },
      "evidence": {
        "default": null,
        "description": "What demonstrates it — a calc package, a stamped drawing, a test report.",
        "type": [
          "string",
          "null"
        ]
      },
      "rule_id": {
        "description": "The `EnvironmentRule` complied with.\n🛑 NOT a `DesignRule` — see `violates_rule.rule_id`.",
        "type": "string"
      },
      "verified": {
        "default": null,
        "description": "Whether compliance was DEMONSTRATED rather than merely asserted.\nDefaults to false, for the reason every evidence field here does: a\nclaim is not a check.",
        "type": [
          "boolean",
          "null"
        ]
      }
    },
    "required": [
      "element_id",
      "rule_id"
    ],
    "type": "object"
  },
  "name": "complies_with"
}
