{
  "annotations": {
    "readOnlyHint": false
  },
  "description": "Record that a Constraint CONSTRAINS a target, with the target's `contribution` to the budget (in the Constraint's quantity unit) and the `basis` for the number (estimated/evidence/measured). An edge without a contribution is reported by budget_report as unstated — never treated as zero.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "basis": {
        "default": null,
        "description": "`estimated` (default) / `evidence` / `measured`.",
        "type": [
          "string",
          "null"
        ]
      },
      "constraint_id": {
        "type": "string"
      },
      "contribution": {
        "default": null,
        "description": "This target's spend, in the Constraint's quantity unit. Omitted =\nparticipates but unstated; budget_report reports it, never zeroes it.",
        "format": "double",
        "type": [
          "number",
          "null"
        ]
      },
      "measured_at": {
        "default": null,
        "description": "WHEN the contribution was observed. Pass it whenever `basis` is\n`measured` — that is the strongest claim the schema offers and the only\none that goes stale, and `budget_report` lists an undated measurement\nrather than treating it as fresh. An estimate does not decay and needs\nno date.",
        "type": [
          "string",
          "null"
        ]
      },
      "note": {
        "default": null,
        "description": "WHY this Constraint binds this target — the sentence a later reader\nneeds and the one this call could not carry until now.",
        "type": [
          "string",
          "null"
        ]
      },
      "target_id": {
        "type": "string"
      },
      "target_type": {
        "description": "The spender's node type — anything can spend (Component mass,\nInterface latency, Resource cost).",
        "type": "string"
      }
    },
    "required": [
      "constraint_id",
      "target_type",
      "target_id"
    ],
    "type": "object"
  },
  "name": "constrains"
}
