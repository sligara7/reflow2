{
  "annotations": {
    "readOnlyHint": false
  },
  "description": "Record that a Constraint CONSTRAINS a target, with the target's `contribution` to the budget (in the Constraint's quantity unit) and the `basis` for the number (estimated/evidence/measured) and its `source` — the tool, artifact or person the number came from. An edge without a contribution is reported by budget_report as unstated — never treated as zero; a number with no source is named as unsourced. ⭐ THIS IS THE CONTRACT WITH AN EXTERNAL MEASURING TOOL: design the budget here, measure it in the tool (an IFC take-off, a ray trace), write the number back with `basis: measured`, `source` naming the tool, `unit` and `measured_at`, and budget_report says whether it still fits (bhome 2026-09-16: the seam existed and nothing pointed at it). Ask for this when you want to say how much a part contributes to a budget or limit — its share of the mass, latency, cost or other quantity.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "basis": {
        "default": null,
        "description": "`estimated` (default) / `evidence` / `measured`.",
        "enum": [
          "estimated",
          "evidence",
          "measured",
          null
        ],
        "type": [
          "string",
          "null"
        ]
      },
      "constraint_id": {
        "description": "The Constraint (`con:…`) that limits the target — the source of the CONSTRAINS edge.",
        "type": "string"
      },
      "contribution": {
        "default": null,
        "description": "This target's spend, in the Constraint's quantity unit. Omitted =\nparticipates but unstated; budget_report reports it, never zeroes it.",
        "format": "double",
        "type": [
          "number",
          "null"
        ]
      },
      "measured_at": {
        "default": null,
        "description": "WHEN the contribution was observed. Pass it whenever `basis` is\n`measured` — that is the strongest claim the schema offers and the only\none that goes stale, and `budget_report` lists an undated measurement\nrather than treating it as fresh. An estimate does not decay and needs\nno date.",
        "type": [
          "string",
          "null"
        ]
      },
      "note": {
        "default": null,
        "description": "WHY this Constraint binds this target — the sentence a later reader\nneeds and the one this call could not carry until now.",
        "type": [
          "string",
          "null"
        ]
      },
      "source": {
        "default": null,
        "description": "WHO OR WHAT the contribution came from — the measuring tool or Artifact (an IFC take-off,\na ray trace), the check that computed it, or the Contributor who estimated it, by id where\none exists. With `basis: measured` this is a measurement; a number with no source is named\nby budget_report as unsourced.",
        "type": [
          "string",
          "null"
        ]
      },
      "target_id": {
        "description": "The constrained node; its type is `target_type`.",
        "type": "string"
      },
      "target_type": {
        "default": null,
        "description": "The spender's node type — anything can spend (Component mass,\nInterface latency, Resource cost).\nOptional since 2026-09-06: resolved from the id when omitted (the id\nprefix names the type); an id held by more than one type is REFUSED, never\nguessed — pass it then.",
        "type": [
          "string",
          "null"
        ]
      },
      "unit": {
        "default": null,
        "description": "The unit `contribution` is in, so it can be COMPARED with the Constraint's rather than\ntrusted: a contribution in another unit is reported and left out of the total.",
        "type": [
          "string",
          "null"
        ]
      }
    },
    "required": [
      "constraint_id",
      "target_id"
    ],
    "type": "object"
  },
  "name": "constrains"
}
