{
  "annotations": {
    "readOnlyHint": false
  },
  "description": "Record that a Component CONSUMES an Interface — it is the side that depends on the contract. `from_id` is the Component, `to_id` the Interface. Once both sides are recorded, `propagate_change` on either Component reaches the other, and `detect_gaps` reports a contract that is consumed but never provided.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "from_id": {
        "description": "The consuming `Component`.",
        "type": "string"
      },
      "to_id": {
        "description": "The `Interface` it consumes.",
        "type": "string"
      }
    },
    "required": [
      "from_id",
      "to_id"
    ],
    "type": "object"
  },
  "name": "consumes"
}
