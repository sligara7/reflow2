{
  "annotations": {
    "readOnlyHint": false
  },
  "description": "Link a Project to a child node it CONTAINS — project membership, so a Requirement, Capability or Component is counted under the project whose gaps and rollups it belongs to. NOT decomposition: a Component inside another Component is `contain_component` (adds a parent) or `move_component` (moves it on the spine). Most typed constructors draw this edge for you when a project exists; use it directly for nodes created with `create_node`. Ask for this when a node sits in no project and the reports are not seeing it.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "child_id": {
        "description": "The contained node; its type is `child_type`.",
        "type": "string"
      },
      "child_type": {
        "default": null,
        "description": "Child node type (e.g. `Requirement`, `Capability`, `Component`).\nOptional since 2026-09-06: resolved from the id when omitted (the id\nprefix names the type); an id held by more than one type is REFUSED, never\nguessed — pass it then.",
        "type": [
          "string",
          "null"
        ]
      },
      "project_id": {
        "description": "The Project (`proj:…`) that CONTAINS the child. This is project membership, not decomposition — a Component inside another Component is `contain_component` / `move_component`.",
        "type": "string"
      }
    },
    "required": [
      "project_id",
      "child_id"
    ],
    "type": "object"
  },
  "name": "contains"
}
