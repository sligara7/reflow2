{
  "annotations": {
    "readOnlyHint": true
  },
  "description": "What has the design never been told about? You sweep the tree and supply what you saw (reflow2 does no file I/O); this answers with the regions no node claims, rolled up to the shallowest wholly-unclaimed directory and ranked by mass, so the biggest silence sorts first. Every other detector reasons about nodes ALREADY in the graph, so without this a design covering 30% of a system reports the same `0 open gaps` as one covering all of it. Deliberately not a score and never a pass/fail: a registered artifact whose location is a directory claims everything beneath it, so modelling a vendored mass as one opaque unit is correct rather than a hole. THAT SAME RULE IS ALSO HOW THIS GOES FALSELY GREEN, so the answer now says what it is standing on: `opaque_claims` are subtrees claimed ON PURPOSE, `pending_expansion` are PLACEHOLDERS nobody has expanded yet, and until an artifact declares one (set_artifact_intent) the two are indistinguishable — a registration check once read GREEN over 359 individually unreferenceable files. Read them next to `claimed`: that is how you say '53 artifacts, of which 3 stand in for the rest' instead of just 'covered'. Exclusions come back named. Run it at the end of an adopt pass, so a thin pass is measured rather than felt.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "exclusions": {
        "default": [],
        "description": "Paths (or directory prefixes) you deliberately left out — build output,\nvendored trees, generated code. Each excluded path comes back NAMED with\nthe rule that excluded it, because \"we ignored it\" and \"it is covered\"\nmust never look alike.",
        "items": {
          "type": "string"
        },
        "type": "array"
      },
      "observed": {
        "description": "What your sweep saw, one entry per path:\n`{ \"path\": \"src/thing.rs\", \"mass\": 1200 }`. `mass` is your own unit —\nbytes, lines, entries — used only to rank the silences; omit it and\nranking falls back to how many paths a region holds.",
        "items": {
          "additionalProperties": true,
          "type": "object"
        },
        "type": "array"
      },
      "swept_at": {
        "default": null,
        "description": "When the sweep was taken (reflow2 takes no clock). An undated sweep is\nreported as undated rather than assumed current.",
        "type": [
          "string",
          "null"
        ]
      }
    },
    "required": [
      "observed"
    ],
    "type": "object"
  },
  "name": "coverage_report"
}
