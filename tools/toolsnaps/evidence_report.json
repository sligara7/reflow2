{
  "annotations": {
    "readOnlyHint": true
  },
  "description": "Where did each capability's evidence actually come from? Lists the environments its PASSING checks were performed in, and flags the ones proven ONLY in simulation — the risk that simulating first is supposed to buy down, which only works if you can still tell model from reality afterwards. REPORTS, NEVER RANKS: it will not claim lab beats staging beats field, because which of those is 'more real' is domain-specific and a wrong ordering gets worked around rather than corrected. A check naming no environment is counted as UNPLACED, never assumed real — silence is not evidence of the field. ALSO RETURNS A SECOND AXIS THE NAME DOES NOT SUGGEST: `conformance` counts artifacts by whether anyone CONFIRMED they still do what their target requires (`unchecked` / `reviewed` / `verified`) — registering a file and checking it are different acts. `with_unplaced_checks` and `with_unscoped_checks` count capabilities whose evidence names no environment and no swept parameters.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "budget_chars": {
        "default": null,
        "description": "How many characters of JSON this reply may spend before prose is\nwithheld to make it fit (default 30,000).\n\nRAISE IT ONLY IF YOU KNOW THIS CLIENT HAS THE ROOM. The default sits\nbelow the smallest tool-output cap in use, because the failure it\nexists to stop is not a slow call — it is the CLIENT refusing the reply,\nat which point the session sees a wall of harness text and reflow2 never\ngets to suggest narrowing.\n\nEvery trimmed reply says what it withheld, at `budget`; counts and ids\nare never budgeted away, so a shorter answer is never a quieter one.",
        "format": "uint",
        "minimum": 0,
        "type": [
          "integer",
          "null"
        ]
      }
    },
    "type": "object"
  },
  "name": "evidence_report"
}
