{
  "annotations": {
    "readOnlyHint": true
  },
  "description": "The whole design as one portable document — every node and edge, sorted so two exports of an unchanged graph are byte-identical. Use it to back the design up, move it between machines, or migrate it across a reflow2 upgrade (export with the old build, import with the new). It carries a stamp saying which reflow2 wrote it. Pass `path` to write the document to a file instead of returning it — on a large design the payload overflows what a session can read. LINEAGE ANCHORS AT THE COMMITTED RECORD: in a git repository the document chains from this path at the merge-base with the default branch, so every export on a branch chains from the same ancestor and a squash-merge lands ONE hop — `dec:export-once-per-pr` holds by construction. Outside git it chains from the file already there; `chained_from` says which anchor was used and `chain_note` why, because a hash cannot say where it came from. 🛑 NEVER ISSUE THIS IN THE SAME PARALLEL BATCH AS WRITES YOU EXPECT IT TO CONTAIN: calls a harness emits together are unordered and this takes the same lock, so it can run BEFORE them — measured early in 91 of 200 trials, each call on its own task as a real server runs them. THE FAILURE IS SILENT: the export succeeds, it is simply early, and the document you commit carries artifact hashes that will not match disk. Sequence it after the writes return. ⚠ The unexported-work nudge will NOT catch it — it compares NODE COUNTS, so a missed PROPERTY change is invisible (0 of 10 caught).",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "accept_divergence": {
        "default": null,
        "description": "Write even when the export would DELETE design the existing file holds\n— someone else's work, pulled in after this graph last synced. Off by\ndefault and refused loudly, because a stale export is a *complete*\ndocument: it merges cleanly and the missing work simply vanishes\n(`req:stale-seat-knows`). Set this only to discard that work on purpose.",
        "type": [
          "boolean",
          "null"
        ]
      },
      "overwrite": {
        "default": null,
        "description": "Allow `path` to replace an existing file. Off by default: an export\nwrites freely to a new path but refuses to clobber an existing one\nunless you say so, so a stray or injected path cannot silently destroy\na file (BL-57).",
        "type": [
          "boolean",
          "null"
        ]
      },
      "path": {
        "default": null,
        "description": "Write the export to this file (deterministic sorted-key JSON, diffable\nunder git) and return only {path, bytes, nodes, edges, content_hash,\nprev_content_hash, wrote, stamp}. Replacing an existing export links the\nnew document to the old one's content hash (lineage; chain advances only\nwhen content changed). Omit to get the whole document as the result\npayload.\n\n**READ `wrote`** — `created` / `changed` / `unchanged`. The hashes do NOT\nanswer it: an export that changed the file and one that changed nothing\nreturn the same `content_hash` AND the same `prev_content_hash`, so a\nno-op is indistinguishable from a save without this field. On a shared\nserver `unchanged` usually means a peer's export already carried your\nwork, which is worth knowing and reads like a failed save without it.",
        "type": [
          "string",
          "null"
        ]
      }
    },
    "type": "object"
  },
  "name": "export_graph"
}
