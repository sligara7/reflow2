{
  "annotations": {
    "readOnlyHint": false
  },
  "description": "Link a node to the Decision or DesignRule that shapes it (GOVERNED_BY). PASS `ruling: parks` WHEN THE RULING DECLARES THIS NODE'S UNATTACHED OR UNSATISFIED STATE CORRECT — a registered document that deliberately draws no claim edges, a requirement an accepted Decision forbids satisfying. Detectors then report it as PARKED and count it in detect_defects's `swept.parked`, instead of filing a deliberate state as a defect. Measured cost of not having this: a fleet watched defects go 88 -> 97 across ten CORRECT writes, so the right action degraded the instrument and a later reader had an incentive to stop registering documents at all. The ruling must be an ACCEPTED Decision — a `proposed` one is somebody thinking out loud, and a musing must not suppress a finding.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "from_id": {
        "type": "string"
      },
      "from_type": {
        "type": "string"
      },
      "note": {
        "default": null,
        "description": "WHY this node is governed by that ruling — the reasoning a later reader\nneeds and the one this call could not carry until now.",
        "type": [
          "string",
          "null"
        ]
      },
      "ruling": {
        "default": null,
        "description": "What KIND of governance this is. Omit — the ordinary case — and the\ntarget simply shapes the source. Pass `parks` to record that the\nruling declares this node's UNATTACHED or UNSATISFIED state CORRECT AND\nDELIBERATE: structural detectors then report it as parked and COUNT it\nin `detect_defects`'s `swept.parked`, instead of filing it as a defect\nor going quiet about it. The ruling must be an ACCEPTED Decision — a\n`proposed` one is somebody thinking out loud, and a musing must not\nsuppress a finding.",
        "type": [
          "string",
          "null"
        ]
      },
      "to_id": {
        "type": "string"
      },
      "to_type": {
        "description": "Usually `Decision` or `DesignRule`.",
        "type": "string"
      }
    },
    "required": [
      "from_type",
      "from_id",
      "to_type",
      "to_id"
    ],
    "type": "object"
  },
  "name": "governed_by"
}
