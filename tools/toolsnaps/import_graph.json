{
  "annotations": {
    "readOnlyHint": false
  },
  "description": "Load an exported design into this graph. Upsert, not replace: ids already present are overwritten and anything not in the document is left alone, so clear the graph first if you want a clean restore. Atomic — a document that fails validation leaves the graph untouched rather than half-loaded. Reports any edge whose endpoints were missing rather than dropping it.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "document": {
        "additionalProperties": true,
        "default": null,
        "description": "A document previously returned by `export_graph`. Omit when passing\n`path`.",
        "type": [
          "object",
          "null"
        ]
      },
      "path": {
        "default": null,
        "description": "Read the document from this file instead — the committed design export,\nusually. Prefer this to inlining: it avoids carrying a large document\nthrough the conversation, and it records that this seat is now in step\nwith that file, which is what clears a `req:stale-seat-knows` refusal.",
        "type": [
          "string",
          "null"
        ]
      }
    },
    "type": "object"
  },
  "name": "import_graph"
}
