{
  "annotations": {
    "readOnlyHint": false
  },
  "description": "Register a real file against the design WITH provenance, atomically: Artifact + a provenance Fragment (YIELDED) + a REALIZES edge to the Capability/Component it implements. Fails loud if the target is missing. Use after building a file so as-designed vs as-built stays honest. RE-LINKING IS SAFE: `name` and `description` are required only on the FIRST link, and omitting either afterwards LEAVES THE STORED ONE ALONE — so attaching a file to a second target never renames it or drops its prose. Pass them again only when you mean to change them. Ask for this when you want to record that a source file or document implements a capability or part — register the file against the design.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "artifact_id": {
        "type": "string"
      },
      "artifact_type": {
        "default": null,
        "type": [
          "string",
          "null"
        ]
      },
      "checksum": {
        "default": null,
        "description": "Content hash of the file as registered — the baseline `reconcile_artifacts`\ncompares against later. Supply it whenever you can; without it a content\nchange is reported as `no_baseline` instead of being caught.",
        "type": [
          "string",
          "null"
        ]
      },
      "completeness": {
        "default": null,
        "type": [
          "string",
          "null"
        ]
      },
      "conformance": {
        "default": null,
        "description": "`unchecked` (default) / `reviewed` / `verified` — whether anyone\nconfirmed the artifact still does what the target requires. Registering\na file and checking it against its requirement are different acts, and\nonly the second one is evidence.",
        "enum": [
          "unchecked",
          "reviewed",
          "verified",
          null
        ],
        "type": [
          "string",
          "null"
        ]
      },
      "content_ref": {
        "default": null,
        "description": "A pointer to the SOURCE CONTENT — a path or a hash — rather than\ninlining it, recorded on the provenance Fragment this call mints.\nDeclared 2026-09-07 as one of the fourteen holes: 2 of 221 fragments\ncarried one, written through the generic escape hatch.",
        "type": [
          "string",
          "null"
        ]
      },
      "description": {
        "default": null,
        "description": "What the artifact IS, in prose. Accepted here as well as on add_artifact,\nso registering a file and describing it is one call rather than two tools\nwith different field sets. Omitting it preserves any stored description.",
        "type": [
          "string",
          "null"
        ]
      },
      "fragment_id": {
        "default": null,
        "type": [
          "string",
          "null"
        ]
      },
      "location": {
        "default": null,
        "type": [
          "string",
          "null"
        ]
      },
      "name": {
        "default": null,
        "description": "Required on a FIRST link. Omitting it on a re-link PRESERVES the stored\nname rather than blanking it — before 2026-09-07 this was mandatory, so\n\"leave the name alone\" could not be said and every re-link rewrote it.",
        "type": [
          "string",
          "null"
        ]
      },
      "note_kind": {
        "default": null,
        "description": "WHOSE VOICE a note fragment is in: `author` intent or pseudocode,\n`reviewer` feedback, or `director` instruction. Also on the Fragment.",
        "enum": [
          "author",
          "reviewer",
          "director",
          null
        ],
        "type": [
          "string",
          "null"
        ]
      },
      "provenance": {
        "default": null,
        "description": "Provenance stamped on the Fragment (default `authored`).",
        "enum": [
          "authored",
          "planned",
          "inferred",
          "healed",
          "reconciled",
          "imported",
          null
        ],
        "type": [
          "string",
          "null"
        ]
      },
      "target_id": {
        "description": "The node the artifact realizes; its type is `target_type`.",
        "type": "string"
      },
      "target_type": {
        "default": null,
        "description": "Optional since 2026-09-06: resolved from the id when omitted (the id\nprefix names the type); an id held by more than one type is REFUSED, never\nguessed — pass it then.",
        "type": [
          "string",
          "null"
        ]
      }
    },
    "required": [
      "artifact_id",
      "target_id"
    ],
    "type": "object"
  },
  "name": "link_artifact"
}
