{
  "annotations": {
    "readOnlyHint": true
  },
  "description": "List the reflow2 skills this server carries — name and the full description an agent matches on to decide whether a skill applies. Skills are served by the server rather than installed into the project, so this list always matches the running reflow2. Read one with get_skill BEFORE doing the work it covers. Ask for this when you want to see which playbooks, procedures or step-by-step guides are available for working on this design.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "budget_chars": {
        "default": null,
        "description": "How many characters of JSON this reply may spend before prose is\nwithheld to make it fit (default 30,000). See `reply_budget`: counts and\nids are never budgeted away, so a shorter answer is never a quieter one.",
        "format": "uint",
        "minimum": 0,
        "type": [
          "integer",
          "null"
        ]
      }
    },
    "type": "object"
  },
  "name": "list_skills"
}
