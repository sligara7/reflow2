{
  "annotations": {
    "readOnlyHint": false
  },
  "description": "Record whose AREA a node is (OWNED_BY) — durable, standing, and never released. THE THIRD 'WHO' AXIS: `authored_by` says who WROTE it (past tense, never changes), `claim_region` says who is IN it RIGHT NOW (transient, advisory, released at checkout), and this says whose ground it is, which survives every session. Use it for the ordinary case of two people splitting a design — these parts are mine, those are yours. DO NOT use a claim for this: claims are session-scoped by their own description and never expire on a shared server, so standing ones would drown the report that shows who is actively working where. Deliberately NOT a traceability edge, so ownership never enlarges a blast radius — owning something says who ANSWERS for it, not that changing it changes them. `note` is what is actually owned and any bound on it. AN UNOWNED NODE IS NOT A GAP: most of a mature design legitimately has no owner, so absence is never reported. Once recorded, `loop_status` with a `contributor_id` lists the open gaps standing on that person's ground.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "contributor_id": {
        "description": "The `Contributor` whose area this is.",
        "type": "string"
      },
      "from_id": {
        "type": "string"
      },
      "from_type": {
        "description": "Type of the node being owned (e.g. `Component`, `Capability`).",
        "type": "string"
      },
      "note": {
        "default": null,
        "description": "What is actually owned, and any bound on it — the sentence a colleague\nneeds when they find your name on something. \"The ingest half, not the\nexport half\" goes here. An owner with no note is still an owner.",
        "type": [
          "string",
          "null"
        ]
      },
      "since": {
        "default": null,
        "description": "ISO-8601 date ownership was taken, if recorded. The core takes no clock,\nso the caller supplies it.",
        "type": [
          "string",
          "null"
        ]
      }
    },
    "required": [
      "from_type",
      "from_id",
      "contributor_id"
    ],
    "type": "object"
  },
  "name": "owned_by"
}
