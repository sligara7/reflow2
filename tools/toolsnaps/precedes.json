{
  "annotations": {
    "readOnlyHint": false
  },
  "description": "Order one DesignEpoch after another (earlier PRECEDES later) — the chain axis Z exists to record. Epochs also carry a `sequence` integer, but the explicit edge is what makes the history walkable as a graph rather than sortable as a list.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "earlier_epoch": {
        "type": "string"
      },
      "later_epoch": {
        "type": "string"
      }
    },
    "required": [
      "earlier_epoch",
      "later_epoch"
    ],
    "type": "object"
  },
  "name": "precedes"
}
