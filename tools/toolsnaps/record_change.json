{
  "annotations": {
    "readOnlyHint": false
  },
  "description": "Record a change to a node in an epoch (snapshots the prior state). CONVENTION: record the change BEFORE you make it — the snapshot captures the state as it is now, so calling this afterwards preserves what you already replaced. TWO QUESTIONS, NOT ONE: `change_type` says WHY, and `subject` says WHICH AXIS — `system` (the thing changed) or `record` (the thing did not change and only the design's knowledge of it did, e.g. a re-sync or a drift you are accepting). Leaving `subject` out is a true answer and is never inferred from `change_type`. ⭐ CHANGING SOMETHING THAT ALREADY EXISTS? LOAD THE `impact-check` SKILL FIRST (`get_skill`) — it propagates from what you are about to touch and shows the blast radius, so you edit what is actually affected and learn what rotted before the edit, not after.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "action": {
        "description": "`added` | `modified` | `removed`.",
        "enum": [
          "added",
          "modified",
          "removed"
        ],
        "type": "string"
      },
      "change_event_id": {
        "type": "string"
      },
      "change_type": {
        "description": "Change type key (e.g. `new_feature`).",
        "enum": [
          "requirement_creep",
          "new_feature",
          "test_failure_fix",
          "defect_fix",
          "performance_optimization",
          "refactor",
          "documentation",
          "scope_change",
          "constraint_change",
          "environment_change",
          "deprecation",
          "resync",
          "baseline_established"
        ],
        "type": "string"
      },
      "epoch_id": {
        "type": "string"
      },
      "name": {
        "type": "string"
      },
      "repair": {
        "default": null,
        "description": "FOR A REPAIR: `corrected_cause` (the class should not recur) or\n`contained_symptom` (it can, and something is standing in the way).\n\nOPTIONAL, and leaving it out is a true answer — absent means nobody\nsaid, and it is NEVER inferred from `change_type`, because that is the\nwhole reason this field exists: a `test_failure_fix` is equally the\nrecord of a root-cause rewrite and of a shim that made a red test green.\nMeasured on reflow2's own graph 2026-08-17: 472 ChangeEvents, every one\nnaming what MOVED and not one saying whether it was the RIGHT fix.\n\n⚠️ IT RECORDS, IT DOES NOT JUDGE. A workaround is often the correct call\nunder a deadline; the point is that it be VISIBLE, never that it be\nforbidden. `repair_report` reads this back.",
        "enum": [
          "corrected_cause",
          "contained_symptom",
          null
        ],
        "type": [
          "string",
          "null"
        ]
      },
      "stands_in_for": {
        "default": null,
        "description": "What the PROPER fix would be, in a sentence. REQUIRED with\n`repair: contained_symptom` and refused without it.\n\nThis is what turns a patch from an invisible cost into a stated debt\nwith somewhere to be read: a workaround nobody wrote down is\nindistinguishable from a design decision six weeks later.",
        "type": [
          "string",
          "null"
        ]
      },
      "subject": {
        "default": null,
        "description": "WHICH AXIS this change is on — `system` (the thing changed) or `record`\n(only the design's knowledge of it changed). OPTIONAL, and leaving it\nout is a true answer: absent means nobody said, and it is never inferred\nfrom `change_type`, because the mapping is not total — a `resync` can be\neither.",
        "enum": [
          "system",
          "record",
          null
        ],
        "type": [
          "string",
          "null"
        ]
      },
      "target_id": {
        "type": "string"
      },
      "target_type": {
        "default": null,
        "description": "Optional since 2026-09-06: resolved from the id when omitted (the id\nprefix names the type); an id held by more than one type is REFUSED, never\nguessed — pass it then.",
        "type": [
          "string",
          "null"
        ]
      }
    },
    "required": [
      "epoch_id",
      "change_event_id",
      "name",
      "target_id",
      "change_type",
      "action"
    ],
    "type": "object"
  },
  "name": "record_change"
}
