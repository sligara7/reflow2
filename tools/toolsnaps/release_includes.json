{
  "annotations": {
    "readOnlyHint": false
  },
  "description": "Record that a Release ships an Artifact or Component (INCLUDES) — the as-released view. Pass as_checksum to freeze the artifact's content hash as shipped: the artifact node's own checksum is the live drift baseline and moves with every accept, so without the frozen copy a past release's manifest would quietly rewrite itself. A Release with no INCLUDES edges is a version number, not a manifest.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "as_checksum": {
        "default": null,
        "description": "The artifact's content hash AS SHIPPED in this release — frozen at cut\ntime, so later baseline moves do not rewrite what a past release\ncontained.",
        "type": [
          "string",
          "null"
        ]
      },
      "release_id": {
        "description": "The Release (`rel:…`) that ships the item — one INCLUDES edge per artifact or component, which is what makes the as-released view exist.",
        "type": "string"
      },
      "target_id": {
        "description": "The included node; its type is `target_type`.",
        "type": "string"
      },
      "target_type": {
        "default": null,
        "description": "`Artifact` or `Component`.\nOptional since 2026-09-06: resolved from the id when omitted (the id\nprefix names the type); an id held by more than one type is REFUSED, never\nguessed — pass it then.",
        "type": [
          "string",
          "null"
        ]
      }
    },
    "required": [
      "release_id",
      "target_id"
    ],
    "type": "object"
  },
  "name": "release_includes"
}
