{
  "annotations": {
    "readOnlyHint": false
  },
  "description": "Record that a Component or Release needs a Resource (REQUIRES_RESOURCE), with how critical it is — `optional`, `recommended` or `required`. A Resource (`add_resource`) is something the design consumes from outside itself — power, bandwidth, a licence, a person-hour budget, a physical facility — and the `no_deploy_operate` gap asks for these alongside environments and deployments so an operate-phase design does not read as complete with nothing to run on. Ask for this when you want to record what a part or a release depends on having available.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "criticality": {
        "default": null,
        "description": "`optional` / `recommended` / `required`.",
        "enum": [
          "optional",
          "recommended",
          "required",
          null
        ],
        "type": [
          "string",
          "null"
        ]
      },
      "from_id": {
        "description": "The node requiring it; its type is `from_type`.",
        "type": "string"
      },
      "from_type": {
        "default": null,
        "description": "Source node type (e.g. `Component`, `Release`).\nOptional since 2026-09-06: resolved from the id when omitted (the id\nprefix names the type); an id held by more than one type is REFUSED, never\nguessed — pass it then.",
        "type": [
          "string",
          "null"
        ]
      },
      "resource_id": {
        "description": "The Resource (`res:…`, from `add_resource`) the component or release needs.",
        "type": "string"
      }
    },
    "required": [
      "from_id",
      "resource_id"
    ],
    "type": "object"
  },
  "name": "require_resource"
}
