{
  "annotations": {
    "readOnlyHint": true
  },
  "description": "Find design nodes by what they say, when you don't know their ids — 'what does the design say about persistence?', 'is there already a requirement about latency?'. BM25 keyword search over every node's name/statement/description, ranked, optionally scoped to one node type. Search BEFORE creating a node that might already exist, and to map the user's words to the node they mean. Result reports its own bounds: hits.len() == limit means there may be more, and a non-empty `stale` list means the index has drifted from the store.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "limit": {
        "default": null,
        "description": "Maximum hits to return, best first (default 10). The result echoes it —\nhits.len() == limit means there may be more.",
        "format": "uint",
        "minimum": 0,
        "type": [
          "integer",
          "null"
        ]
      },
      "node_type": {
        "default": null,
        "description": "Restrict hits to one node type (e.g. \"Requirement\"); omit for all.",
        "type": [
          "string",
          "null"
        ]
      },
      "query": {
        "description": "Keywords to search for — tokenized BM25 over every node's name,\nstatement and description (not substring or regex). Use the words the\ndesign would use: \"persistence\", \"dedup window\", \"latency budget\".",
        "type": "string"
      }
    },
    "required": [
      "query"
    ],
    "type": "object"
  },
  "name": "search_design",
  "outputSchema": {
    "description": "A ranked list of design nodes matching the query, with the bounds of the search stated so a caller can tell a complete answer from a truncated one.",
    "properties": {
      "hits": {
        "description": "Matching nodes, best first. Scores are comparable WITHIN one result list and not across queries.",
        "items": {
          "properties": {
            "age_days": {
              "description": "Present only on a node carrying a date — how old its claim is. Absent on an undated node.",
              "type": "integer"
            },
            "as_of": {
              "description": "The date the claim was made, when it carries one.",
              "type": "string"
            },
            "name": {
              "description": "The node's name at hit time; empty if it has none.",
              "type": "string"
            },
            "node_id": {
              "type": "string"
            },
            "node_type": {
              "type": "string"
            },
            "score": {
              "description": "BM25 relevance.",
              "type": "number"
            }
          },
          "required": [
            "node_id",
            "node_type",
            "name",
            "score"
          ],
          "type": "object"
        },
        "type": "array"
      },
      "limit": {
        "description": "The limit that bounded this result. hits.len() == limit means there may be more; this is the no-silent-caps rule made visible.",
        "type": "integer"
      },
      "loop_hint": {
        "description": "Present only when the coherence loop is owed something.",
        "type": "string"
      },
      "stale": {
        "description": "Ids the index returned but the store no longer holds. NON-EMPTY MEANS THE INDEX HAS DRIFTED and a reindex is due — it is not a list of results.",
        "items": {
          "type": "string"
        },
        "type": "array"
      }
    },
    "required": [
      "hits",
      "stale",
      "limit"
    ],
    "type": "object"
  }
}
