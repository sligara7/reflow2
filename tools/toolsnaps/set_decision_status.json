{
  "annotations": {
    "destructiveHint": false,
    "readOnlyHint": false
  },
  "description": "Set a Decision's lifecycle status — proposed / accepted / deferred / superseded / rejected (BL-70). Setting it to `proposed` opens it as a decision point: an undecided fork you can register alternatives under. `deferred` SETS IT ASIDE — sound but untimely — and it stops counting as debt in loop_status and what_next; it is the owner's word and wants `approver`, like `accepted`. Every other property is preserved. CARRIES `settled_question_prose` WHEN A DECISION REACHES `accepted` AND SOMETHING IT GOVERNS STILL SAYS THE QUESTION IS OPEN: the block names each such node, the phrase that matched, and quotes the prose around it, so you can judge it here rather than in another call. It never says the text is wrong — prose that QUOTES an old question is a correct node and looks identical. Silent on `rejected`/`superseded`, which retire rather than settle, and on `accepted` -> `accepted`. From a 2026-09-09 incident where a requirement said \"NOT YET DECIDED … his call to make\" three weeks after the call had been made, and the owner was asked the settled question a second time. PASS `chose` TO SAY WHICH OPTION WON, in your own words: the settling act then records what it settled without rewriting the deliberation that produced it. Ask for this to mark a decision accepted with the owner's name.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "acted_at": {
        "default": null,
        "description": "When the approver acted, as a plain date. Stored on the approver edge.",
        "type": [
          "string",
          "null"
        ]
      },
      "approver": {
        "default": null,
        "description": "The Contributor whose word moves it — draws `AUTHORED_BY role=approver`\nin the same call. Optional on this setter because it has consumers, but\nan `accepted` written without one is REPORTED in the reply as carrying\nnobody's name: that is exactly the write the intent-authority gate\nfails a build on. An id naming no Contributor is REFUSED.",
        "type": [
          "string",
          "null"
        ]
      },
      "chose": {
        "default": null,
        "description": "WHAT THIS SETTLEMENT CHOSE, in your own words — which option won, and\nwhy the others did not. Written onto the Decision as `chose`.\n\nIt exists so the act that settles a question can say what it settled\nWITHOUT rewriting the deliberation that produced it. flo2, 2026-09-19,\nsettled two decisions correctly and left both still NAMED\n\"OPEN — is …?\" with bodies ending \"Options, none chosen\", because the\nonly route was re-sending the whole node — the operation measured the\nsame week to silently drop paragraphs. An accepted Decision that still\nreads as an open question is worse than an unsettled one, because a\nlater reader trusts the prose over the status field.\n\nOptional, and absent means nobody said. `collapse_decision` remains the\ntool when the options are REGISTERED alternatives; this is for the\ncommon case where they live in the decision's own prose.",
        "type": [
          "string",
          "null"
        ]
      },
      "decision_id": {
        "description": "The Decision whose lifecycle status moves. `dec:…` by convention; `search_design` finds one by its words.",
        "type": "string"
      },
      "status": {
        "description": "`proposed` (opens a decision point) / `accepted` / `deferred` (set aside,\nnot debt — carries an approver like `accepted`) / `superseded` /\n`rejected`.",
        "enum": [
          "proposed",
          "accepted",
          "deferred",
          "superseded",
          "rejected"
        ],
        "type": "string"
      }
    },
    "required": [
      "decision_id",
      "status"
    ],
    "type": "object"
  },
  "name": "set_decision_status"
}
