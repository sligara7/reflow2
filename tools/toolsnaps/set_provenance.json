{
  "annotations": {
    "readOnlyHint": false
  },
  "description": "Record how a node entered the graph: `authored` (the default, someone stated it) / `planned` / `inferred` (read back out of an existing system) / `healed` / `reconciled` / `imported`. Accepted on Requirement, Capability, Component and Interface. Mark inferred requirements as such — a requirement backed out of the code that implements it is satisfied by construction and cannot contradict anything, and a reader has no other way to tell. For bulk adoption prefer import_graph, which carries this at create time.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "node_id": {
        "description": "The node whose provenance is set; its type is `node_type`.",
        "type": "string"
      },
      "node_type": {
        "default": null,
        "description": "`Requirement`, `Capability`, `Component` or `Interface`.\nOptional since 2026-09-06: resolved from the id when omitted (the id\nprefix names the type); an id held by more than one type is REFUSED, never\nguessed — pass it then.",
        "type": [
          "string",
          "null"
        ]
      },
      "provenance": {
        "description": "`authored` (default) / `planned` / `inferred` / `healed` /\n`reconciled` / `imported`.",
        "enum": [
          "authored",
          "planned",
          "inferred",
          "healed",
          "reconciled",
          "imported"
        ],
        "type": "string"
      }
    },
    "required": [
      "node_id",
      "provenance"
    ],
    "type": "object"
  },
  "name": "set_provenance"
}
