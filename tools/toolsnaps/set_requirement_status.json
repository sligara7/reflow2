{
  "annotations": {
    "readOnlyHint": false
  },
  "description": "Set a Requirement's lifecycle status: `proposed` (the default) / `accepted` / `deferred` / `dropped` / `met`. Every move off `proposed` records the USER's word, never your own judgment: capture at `proposed` and move the status only when the user has actually confirmed, deferred or dropped it — certainty is derived from this status, so promoting it yourself forges their signature (dec:certainty-derived). A `dropped` or `met` requirement stops raising unsatisfied_requirement.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "acted_at": {
        "default": null,
        "description": "When the approver acted, as a plain date. Stored on the approver edge.",
        "type": [
          "string",
          "null"
        ]
      },
      "approver": {
        "default": null,
        "description": "The Contributor whose word moves it — draws `AUTHORED_BY role=approver`\nin the same call. Optional on this setter because it has consumers, but\na status past `proposed` written without one is REPORTED in the reply\nas carrying nobody's name. An id naming no Contributor is REFUSED.",
        "type": [
          "string",
          "null"
        ]
      },
      "requirement_id": {
        "description": "The Requirement (`req:…`) whose lifecycle status moves. Every move off `proposed` records the USER's word — pass `approver`.",
        "type": "string"
      },
      "status": {
        "description": "`proposed` (default) / `accepted` / `deferred` / `dropped` / `met`.",
        "enum": [
          "proposed",
          "accepted",
          "deferred",
          "dropped",
          "met"
        ],
        "type": "string"
      }
    },
    "required": [
      "requirement_id",
      "status"
    ],
    "type": "object"
  },
  "name": "set_requirement_status"
}
