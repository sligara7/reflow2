{
  "annotations": {
    "readOnlyHint": true
  },
  "description": "WHAT THE DESIGN HOLDS ABOUT ONE SUBJECT, read-only, in one call — the /topic view: 'show me something about X'. Searches the design for the phrase, groups the hits by node type, and for each hit reads its status, its connections, and the LATEST DATED change or measurement that touched it. Computed server-side so two agents render one topic the same way. ⭐ `not_found` IS MANDATORY: it says what was searched, which populated node types matched NOTHING, and whether the list was cut — a miss on the caller's phrasing must never read as the design holding nothing. Bounded: `budget` says what was withheld; `count` and `by_type` never are. ⭐ ALSO THE FIRST READ BEFORE YOU ASSERT A PROJECT'S STATUS TO ANYONE — a progress report or deck for management, \"what has this project achieved\", \"where does it stand\": call it on the project's name before you write a word (field report 2026-09-14). Writes nothing.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "budget_chars": {
        "default": null,
        "description": "Characters the reply may spend before detail is withheld (default\n30,000). The reply says which tier it landed in and what it withheld;\n`count` and `by_type` are never trimmed.",
        "format": "uint",
        "minimum": 0,
        "type": [
          "integer",
          "null"
        ]
      },
      "limit": {
        "default": null,
        "description": "Hits to consider (default 20). `count == limit` in the reply means\nthere may be more.",
        "format": "uint",
        "minimum": 0,
        "type": [
          "integer",
          "null"
        ]
      },
      "query": {
        "description": "The subject, in your words or the owner's — \"rainfall totals\",\n\"the export lineage\", \"who can approve\". Keyword search over every\nnode's name, statement and description; a miss is REPORTED in\n`not_found`, never read as absence.",
        "type": "string"
      }
    },
    "required": [
      "query"
    ],
    "type": "object"
  },
  "name": "topic_report"
}
